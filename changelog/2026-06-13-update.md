---
title: 2026-06-13 跨日同步 — hermes-agent 6c752ca3a → 0a865e5（391 commits，~48h 双日窗口；Desktop UX 大改 + WhatsApp Business Cloud 新平台 + Photon spectrum-ts 3.0→3.1 reactions + 安全 fail-closed 默认 + Billing /credits 命令 + Dashboard profile-scoped 全量 + Context Compression 重构波 + xAI 退役迁移 + Bitwarden Secrets Manager + Gateway auto-resume 去重 + 47% 函数调用削减）
created: 2026-06-13
updated: 2026-06-13
type: changelog
tags: [changelog, update, v0.16.0-maintenance, desktop-composer-status-stack, desktop-live-subagent-windows, desktop-editable-prompts, desktop-stick-to-bottom, desktop-jump-to-bottom, desktop-streaming-perf-wave, desktop-incremental-markdown, desktop-worktree-sidebar, desktop-workspace-header, desktop-reorderable-list, desktop-slash-completion-polish, desktop-window-translucency, desktop-rtl-bidi, desktop-large-paste-bypass, desktop-approval-inline-expand, desktop-port-zero-discovery, desktop-served-token-hardening, desktop-mcp-tools-ws-backend, desktop-tool-row-dismiss, desktop-jetbrains-mono, desktop-file-preview-selectable, desktop-message-render-boundary, desktop-composer-reconnect, desktop-windows-bootstrap-recovery, whatsapp-cloud-adapter, whatsapp-cloud-hmac-webhook, whatsapp-cloud-wamid-dedup, whatsapp-cloud-allow-from-env, photon-spectrum-ts-3, photon-markdown, photon-reactions, photon-unreact, photon-telemetry-toggle, photon-dm-key-normalize, photon-sidecar-orphan-fix, telegram-always-on-rich, telegram-rich-stream-final-preserve, matrix-table-markup, slack-video-attachments, slack-reaction-lifecycle, slack-action-handler-api, discord-voice-transcribe, discord-typing-stop-after-reply, discord-typing-429-graceful, wecom-per-group-allowlist, yuanbao-platform-message-id, yuanbao-wechat-forward, signal-document-classifier, simplex-document-classifier, email-document-classifier, teams-document-cache, gateway-auto-resume-duplicate-prevent, gateway-session-slot-claim, gateway-jsonl-deprecated, gateway-sqlite-only-load-transcript, gateway-compression-bloated-binding-reset, gateway-session-split-sync-before-fail, gateway-suppress-duplicate-final-stream, gateway-last-flushed-idx-reset, gateway-observe-location-media, security-fail-closed-own-policy, security-api-status-redact-gated, security-host-derived-api-key, security-api-key-non-authoritative-leak, security-secure-parent-dir, security-write-deny-root-env, kanban-sticky-block-preserve, kanban-stale-no-failure-counter, kanban-respawn-defer-blocker-auth, kanban-events-run-idx-hoist, kanban-tasks-session-idx-hoist, kanban-worker-toolsets-pin, kanban-cleanup-race-lock, billing-credits-command, billing-credits-portal-topup, billing-credits-notices-toggle, billing-credits-suppress-on-purchase, dashboard-clone-profile-from-any-source, dashboard-profile-scoped-sessions-analytics, dashboard-channels-profile-scoped, dashboard-local-file-browser-home, dashboard-windows-11-host-panel, dashboard-skill-install-yes, dashboard-browser-scrollback, dashboard-checkboxes-design-system, dashboard-country-flags-removed, dashboard-kanban-i18n, cli-credits-slash, cli-persist-approval-scrollback, cli-renderer-state-on-resize, cli-windows-subprocess-reexec, cli-setup-config-picker-preserve, cli-show-modified-skills-names, cli-idle-timer-tick, cli-merge-session-only-analytics, cli-config-yaml-wins-model-seed, codex-auth-self-heal, codex-refresh-token-rotation, codex-import-from-codex-home, codex-responses-final-answer, codex-drop-extra-headers-chatgpt, xai-retirement-may-2026, xai-migrate-cli, xai-apply-migration-ruamel, xai-doctor-surface, xai-chat-startup-warn, xai-encrypted-reasoning-replay, xai-web-search-plugin, xai-tts-speech-tag-pauses, bitwarden-secrets-manager, bitwarden-lazy-bws-install, bitwarden-sha256-verify, supports-vision-user-config, supports-vision-override-routing, per-session-json-snapshot-writer, cdp-auto-launch-browser, brave-cdp-detection, browseshsource-skills-catalog, firecrawl-integration-tag, profile-clone-from-full-selector, profile-clone-all-exclude-history, profile-backfill-env, profile-gateway-port-docstrings, tui-reasoning-only-resume-visible, tui-provider-routing-config, tui-wrap-long-approval-commands, context-compression-preserve-recent-turns, context-compression-auto-focus-recent-user, context-compression-flush-cursor-clamp, context-compression-merge-tail-split, context-compression-keep-last-assistant-tail, context-compression-summary-end-marker, context-compression-summary-metadata-flag, context-compression-strip-media-directives, context-compression-feasibility-deferred, perf-agent-loop-47-percent-cut, perf-terminal-adaptive-poll, perf-desktop-incremental-markdown, perf-desktop-isolated-renders, perf-desktop-resume-audiocontext, nous-tags-scope-auxiliary, nous-429-retry-reenter, bedrock-restricted-claude-sampling, gemini-system-instruction-role, gemini-strip-self-prefixes, moonshot-union-type-arrays, codex-responses-incomplete-complete, model-fable-5-removed, model-custom-pool-cross-provider, model-strip-tool-name-strict-providers, model-bare-custom-gateway-picker, model-custom-provider-identity-session, model-bare-billing-provider-skip-restore, model-aux-preserve-base-url, model-custom-match-by-base-url, model-ollama-vllm-llamacpp-aliases, model-kimi-k27-code-slug, skills-hub-clawhub-budget, skills-hub-dedup-identifier, skills-hub-youtube-via-uv, skills-hub-prune-deps-venv, skills-hub-healthy-index-reuse-pages, skills-hub-oauth-urlopen-timeout, computer-use-vision-routing-helper, computer-use-aux-vision-router, computer-use-app-filter-no-match, computer-use-type-text-mcp-name, computer-use-drag-action, computer-use-capture-after-app-context, memory-sync-flatten-multimodal, memory-reuse-summarize-helper, coding-context-env-state-persists, coding-context-no-worktree-root, delegation-default-timeout-removed, plugins-slack-action-handler-api, plugins-shared-hooks-credit, plugins-platform-names-rediscover-clear, plugins-browser-paste-github-url, mcp-capability-gate-tools-list, tools-schema-strip-ref-default, approval-allow-permanent-tui-desktop, approval-allow-permanent-dry, agent-binary-file-helpful, send-file-binary-error, agent-binary-file-ref-actionable, gateway-typing-stop-after-reply, gateway-typing-429-graceful, install-syntax-validate-post-pull, install-auto-rollback-failure, install-unmerged-index-clear, install-windows-quarantine-exe, install-windows-stop-gateways-update, install-no-polkit-prompt-system-gateway, doctor-codex-cli-hint, doctor-npm-audit-fix-no-recommend, doctor-xai-surface, platform-xls-doc-ppt-document, plugins-mcp-stdio-tool, agent-coding-context-terminal-env, agent-coding-context-no-worktree-root, agent-redact-rewrite-hot-path, agent-load-config-readonly, observability-trajectory-metadata-flag, tools-tts-xai-speech-pauses, salvage-may-2026, ci-pytest-timeout-removed, ci-test-durations-only-on-pass, tests-mock-subprocess-popen, tests-conftest-live-system-guard, agent-strip-tool-name-strict, plugins-credit-shared-hook-middleware-tool-names, plugins-clear-platform-names-rediscover, salvage-author-maps]
sources:
  - hermes-agent/main 6c752ca3a..0a865e5
total_commits: "391 个 commit（361 个 non-merge + 30 个 merge 节点；约 48h 双日窗口；committer dates 2026-06-11T20:57 UTC → 2026-06-13T20:49 UTC；含两批历史 salvage 2026-05-19~2026-05-23 167 commit 在本期并入）"
---

# 2026-06-13 跨日同步：hermes-agent main `6c752ca3a → 0a865e5`

> **基线**：wiki `a29cf9f` 对齐 hermes `6c752ca3a5232baa666aa6099dedae1e26484b49`（2026-06-11T13:57 -0700，"refactor(agent): tighten SUMMARY_PREFIX wording and fix stale doc references"，v0.16.0 维护未发新版）
> **目标**：hermes-agent/main HEAD `0a865e5948cb836eba93620e365703ec38cf76e3`（2026-06-13T20:49 UTC，"fix(desktop): bypass Chromium editing pipeline for large paste & select-delete (#45812)"）
> **跨度**：约 48 小时双日窗口（committer dates 2026-06-11T20:57 UTC → 2026-06-13T20:49 UTC，含两批历史 salvage 2026-05-19~05-23 ≈167 commit 在本期并入；author dates 跨度至 2026-04-05，本期实际并入）
> **版本**：仍 **v0.16.0**（`pyproject.toml:10 version = "0.16.0"` + `hermes_cli/__init__.py:17-18 __version__ = "0.16.0"` + `__release_date__ = "2026.6.5"` 不变；本期全为 feat/fix/test/refactor/chore/docs/perf/security/build/ci/style，未发新版）
> **commit 类型分布**：约 **211 fix（+4 🐛 fix） / 39 chore / 35 feat / 21 test / 18 refactor / 10 docs / 9 perf / 1 security / 1 ci / 1 build / 1 style / 1 review / 1 revert / 30 merge / ~10 unscoped**
> **diff 体量**：446 文件改动，**+31366 / −4724** 行
> **验证基线**：`/tmp/hermes-agent` clone @ `0a865e5` 全 checkout，所有 commit hash / file:line / 路径 / 函数名 / 行数经 `grep -n` / `git show <sha>` / `Read` 实证；5 个并行 research agent 分主题分担实证（Desktop / Messaging / Gateway+Security+Kanban / Dashboard+CLI+Billing / Compression+Models+Perf+Misc）

---

## 章节索引

1. [Desktop — Composer status stack + 实时 subagent 窗口 + 可编辑提示（架构主轴 A）](#1-desktop-composer-status-stack)
2. [Desktop — Stick-to-bottom 流式跟随 + 性能 wave（incremental markdown / isolated renders / warm AudioContext）](#2-desktop-streaming-perf)
3. [Desktop — Worktree-aware 侧边栏 + WorkspaceHeader + ReorderableList 抽取](#3-desktop-worktree-sidebar)
4. [Desktop — Slash command completion + window translucency + RTL/bidi + 大粘贴 bypass + approval inline expand](#4-desktop-misc-ux)
5. [Desktop — Backend 端口/令牌硬化（port 0 + served token + MCP tools 启动闸门）](#5-desktop-backend-hardening)
6. [WhatsApp Business Cloud API 适配器（新平台）](#6-whatsapp-cloud)
7. [Photon iMessage — spectrum-ts 3.0→3.1 + 原生 markdown/reactions + telemetry 开关](#7-photon-spectrum-ts-3)
8. [Telegram — 富文本永远 on + 流式 final 保留 + Bot API 10.1 跟进](#8-telegram-rich)
9. [Matrix / Slack / Discord / WeCom — 表格 / 视频 / 语音转写 / 群级 sender allowlist](#9-matrix-slack-discord-wecom)
10. [Gateway — Auto-resume 重复 agent 防御 + 会话槽 sentinel + 全路径回归测试](#10-gateway-auto-resume)
11. [Gateway — JSONL 抄写降级，state.db 为唯一存储（4 commit refactor）](#11-gateway-jsonl-deprecation)
12. [Gateway — 压缩 binding 自重置 + session-split 顺序前置 + 重复 final 抑制 + 缓存 agent flush idx 复位](#12-gateway-misc)
13. [Gateway — Signal/SimpleX/Email/Teams 文档分类 + .xls/.doc/.ppt SUPPORTED_DOCUMENT_TYPES](#13-platform-document-classify)
14. [安全 wave — Own-policy adapter fail-closed（fc46354）+ /api/status 受门时 redact + host-derived API key + secure_parent_dir + write-deny <root>/.env](#14-security-wave)
15. [Kanban — Sticky block 不被自动 unblock + stale reclaim 不计 failure + idx 后置迁移 + worker toolsets 钉 profile](#15-kanban-hardening)
16. [Billing — `/credits` slash command（balance + 顶替门户） + display.credits_notices 抑](#16-billing-credits)
17. [Dashboard — 任意 source 克隆 profile + 会话/分析/Channels 全部 profile-scope + 本地文件浏览 home + Win11 host](#17-dashboard-profile-scope)
18. [CLI — 持久化 approval/clarify scrollback + Codex token 自愈 + Renderer 缩放保活 + 用户模改 skill 名显式](#18-cli-misc)
19. [xAI — May 15 2026 模型退役检测 + `hermes migrate xai` 迁移 + Doctor 提示 + 加密 reasoning replay + Web Search plugin](#19-xai-retirement)
20. [Bitwarden Secrets Manager 集成（lazy bws install + SHA-256 校验）](#20-bitwarden)
21. [Profile — clone-from 全量 source + clone-all 排除历史/备份/快照 + 更新时 backfill .env](#21-profile-clone-improvements)
22. [TUI — Reasoning-only 助手轮在恢复时仍可见 + provider_routing 透传 + config.yaml 胜 env 模型 seed + 长 approval 命令换行](#22-tui-misc)
23. [Auxiliary — Nous tags 仅辅助调用 + 主 provider base_url 保留 + Nous 429 重入重试](#23-auxiliary)
24. [Compression refactor wave — 保留近期轮 + 末尾助手 reply 不入摘要 + SUMMARY_END_MARKER 提至模块级 + 摘要消息 wire-strip 元数据 + MEDIA: 指令剥离 + flush 游标 clamp/snapshot](#24-compression-wave)
25. [Perf wave — agent-loop 47% per-conv 调用削减 + adaptive subprocess poll −195ms/tool + 压缩可行性延后 + redact.py 重写](#25-perf-wave)
26. [Models / Providers — Bedrock 受限 Claude 省采样参数 + Gemini systemInstruction role + Moonshot union 数组 + Codex chatgpt.com 移 extra_headers + 自定义 provider 会话身份保真 + 移除不可用 claude-fable-5](#26-models-providers)
27. [Skills hub — ClawHub 浏览预算允许 unlimited + identifier 级去重 + youtube transcript 经 uv + 依赖/venv 排除 + Pages 部署回退健康索引](#27-skills-hub)
28. [Computer-use — SOM/vision 捕获路由到 auxiliary.vision + app= 过滤无匹配明示 + type_text MCP 名修正 + drag 动作](#28-computer-use)
29. [Plugins / Memory / Delegation / MCP — Slack action handler API + 共享 hook/middleware/tool 多 plugin 名归属 + 多模态扁平化 + 默认子 agent 时钟去除 + MCP tools/list capability-gate + 工具 schema $ref default 剥离](#29-plugins-misc)
30. [Update / Installer / Doctor — Post-pull 语法校验 + unmerged-index clear + 中断 venv 自愈 + 不弹 polkit + 跳 unsafe npm audit fix](#30-update-installer)
31. [CI / Tests — pytest-timeout 改 per-file + 守 `hermes update` 真实 spawn + AUTHOR_MAP 增补](#31-ci-tests)
32. [验证基线](#32-verification)

---

<a id="1-desktop-composer-status-stack"></a>
## 1. Desktop — Composer status stack + 实时 subagent 窗口 + 可编辑提示

`d62979a (#44630)` `feat(desktop): composer status stack, live subagent windows, editable prompts`。新增三件套：

### 1.1 Composer status stack（动态状态合并）

`apps/desktop/src/store/composer-status.ts`（257 行）`$statusItemsBySession` `computed`（L57-80）合并 `$subagentsBySession`、`$backgroundStatusBySession`、`$todosBySession`，按 session id 输出。`dismissedBySession` Map（L37）记录用户已忽略的背景状态。

surface：`apps/desktop/src/app/chat/composer/status-stack/index.tsx`（202 行）+ `status-row.tsx`（155 行）。共享原语 `components/chat/{status-row,status-section,terminal-output,composer-dock}.{tsx,ts}`。

### 1.2 实时 subagent 窗口

subagent row 点击调 `openSessionInNewWindow`（`store/windows.ts:63`）— 父会话顶 dock 列出每个活跃子代理，点击一个新窗口 attach 到该子会话；`status-row.tsx:71` 注释说"livestreamed by the gateway's child-session mirror"，复用 gateway 已有的子会话镜像。

### 1.3 可编辑提示（mid-stream 编辑）

`apps/desktop/src/components/assistant-ui/thread.tsx:976` 把 `<ActionBarPrimitive.Edit asChild>` 包到用户气泡上 — 点击随时打开编辑 composer（即使 turn 流式中）；L974-975 注释："Always editable — clicking opens the edit composer even while a turn streams; sending the edit reverts (interrupt + rewind)."。附件作为同级渲染在气泡下方（L964）。`BrailleSpinner → GlyphSpinner` 改名（文件 rename）。

---

<a id="2-desktop-streaming-perf"></a>
## 2. Desktop — Stick-to-bottom + 性能 wave

### 2.1 Stick-to-bottom + jump-to-bottom

`bbf020e (#45263)` `feat(desktop): follow streaming output at bottom + jump-to-bottom button`：

- `apps/desktop/package.json` 加 `"use-stick-to-bottom": "^1.1.6"`
- `76b9386` 删除 `thread-virtualizer.tsx`（481 行）改用 `components/assistant-ui/thread-list.tsx`（307 行）；钩子 `useStickToBottom`（L13 / L112）配 `{ initial: 'instant', resize: 'instant' }`
- 跳底按钮 `apps/desktop/src/app/chat/scroll-to-bottom-button.tsx`（59 行）由 `store/thread-scroll.ts` 的 `$threadJumpButtonVisible` 驱动；在 `app/chat/index.tsx:461` 接入；CSS 变量 `--composer-measured-height + --status-stack-measured-height + 0.625rem`（L50）布局

`d14f6c9` 去掉 streaming autoscroll bounce（停止内容增长追逐）。`bf090de`/`f23a4b7`/`1891637` 重做队列排干：`use-prompt-actions.ts:136 withSessionBusyRetry` 包 `prompt.submit` 所有调用点（L708、L720、L1497），`shouldAutoDrain` 替 `shouldAutoDrainOnSettle`。

### 2.2 性能 wave（三条 perf commit）

`492c402 (#45343)` `perf(desktop): cut GUI streaming & interaction lag` 拆为三 commit：

- `7c226cc perf(desktop): isolate streaming re-renders & cut layout thrash` — `app/chat/index.tsx:185` 新 `ChatRuntimeBoundary` 隔离 `$messages` 订阅（L194 `useStore($messages)`）；消息行改用稳定 `useAuiState` 选择器（`thread.tsx:248, 256, 259` etc — `s.message.content`/`status` 等粗粒度 fact 用 `computed` atoms）
- `edc36f3 perf(desktop): incremental markdown rendering during streams` — 新 `apps/desktop/src/lib/remend-tail.ts`（108 行）+ 测试（105 行）；`markdown-text.tsx:74-96` 实现 LRU `blockCache`（`BLOCK_CACHE_MAX`），L306 `REVEAL_MIN_COMMIT_MS = 33`，L342 提交节流
- `3cf7d43 perf(desktop): faster session resume & warm AudioContext at idle` — `use-session-actions.ts` 并行化 prefetch+resume；`haptics-provider.tsx` 空闲预热 AudioContext（避免 850ms CoreAudio cold start）

---

<a id="3-desktop-worktree-sidebar"></a>
## 3. Desktop — Worktree-aware 侧边栏 + WorkspaceHeader

`e906726 feat(desktop): worktree-aware sidebar grouping`（+ `dd12a54 refactor: extract shared WorkspaceHeader` + `0595af0 feat: move drag handle into leading icon` + `1a3cd3d refactor: collapse sidebar drag-reorder into ReorderableList` + 合 `a86b7b3`）：

- 新文件：`apps/desktop/electron/git-worktrees.cjs`（174 行）、`hooks/use-worktree-info.ts`（68 行）、`app/chat/sidebar/workspace-groups.ts`（326 行）+ 测试
- `sidebar/index.tsx` 用 `WorkspaceHeader`（L1697）、`ReorderableList`（L166），在 L1285（parents）、L1314/L1322（sessions）、L1585（worktrees）使用；每个列表自有 DndContext 防拖拽冲突；会话按日期排序、永不被手动拖动重新洗牌

`1e755ff (#45404)` 保 recents 排序除非手动重排。

---

<a id="4-desktop-misc-ux"></a>
## 4. Desktop — Slash completion / Translucency / RTL / 大粘贴 bypass / Approval expand

### 4.1 Slash command completion polish

`425e777 (#45760)` `fix(desktop): polish slash command completion (space/tab/click + typed args)` — 单文件 `apps/desktop/src/app/chat/composer/index.tsx`（+68/−3）；统一 Enter/Tab/Space 接受 slash 命令和参数阶段；无参命令抑参数 popover；完整输入参数时提交原文。

### 4.2 Window translucency slider

`46d758b (#45086)` `feat(desktop): window translucency slider in Appearance settings`：

- 滑块：`apps/desktop/src/app/settings/appearance-settings.tsx:192-211`（0-100 range）
- 存储：`apps/desktop/src/store/translucency.ts` — `$translucency` atom，持久化 key `hermes.desktop.translucency.v1`，`subscribe` IPC 镜像（L34）
- 主进程：`apps/desktop/electron/main.cjs:385-433` — `TRANSLUCENCY_CONFIG_PATH = translucency.json`，公式 `windowOpacity() = 1 - (intensity/100)*0.7`（L420），`win.setOpacity` 应用（L431）；IPC handler `hermes:translucency`（L5738）；macOS+Windows 限定，Linux no-op

### 4.3 RTL/bidi 自动检测

`6c00077 feat(desktop): auto-detect RTL/bidi text direction in chat` — `apps/desktop/src/styles.css:858-878` 给聊天 prose 块加 `unicode-bidi: plaintext` + `text-align: start`；行内代码/KaTeX 钉 `direction: ltr; unicode-bidi: isolate`。`user-message-text.tsx` +4 行。App chrome 方向不强制。

### 4.4 大粘贴 / 选区删除 bypass Chromium

`0a865e5 (#45812)` `fix(desktop): bypass Chromium editing pipeline for large paste & select-delete` — `apps/desktop/src/app/chat/composer/rich-editor.ts` 新增：`composerSelectionRange`（L136）、`insertPlainTextAtCaret`（L150）、`deleteSelectionInEditor`（L177）。从 `composer/index.tsx:663`（粘贴）和 `:839`（Backspace/Delete 选区删除）调用，绕过 Chromium contenteditable 流水线。Commit message 引用 profile：粘贴 4474ms→13ms、删除 1304ms→4ms。折叠光标的删除仍走原生路径。

### 4.5 Approval bar inline expand

`266b5a1 feat(desktop): expand the full command inline from the approval bar` — `apps/desktop/src/components/assistant-ui/tool-approval.tsx`（+77/−51）。新增 "Command" 折叠开关展开 `request.command`（复用对话框的 `pre` 样式），默认折叠，仅在 `command` 非空时启用。

### 4.6 其他 UX 修

- `24f74eb (#44648)` — `preview-file.tsx:294, :424` 在 markdown 根和 Shiki 代码列加 `data-selectable-text="true"` 让源 + 渲染都可选
- `6e41ca9 (#44642)` — 捆绑 `JetBrainsMono-{Regular,Bold,Italic}.woff2` 到 `apps/desktop/src/fonts/`；`terminal/use-terminal-session.ts` mount 前 warm 每个字体面，xterm 字体栈首项放它
- `d221e36 (#44493)` — `assistant-ui/message-render-boundary.tsx`（48 行）+ 测试；只吞瞬时 "Index N out of bounds" race，其他全 rethrow；消息签名变更时恢复
- `8cf9d86 (#45488)` — `composer/index.tsx`（+31/−8）让 composer 在 reconnect 期间可用；`gateway-connecting-overlay.tsx` 调整
- `aa53a78 (#45594)` — Windows bootstrap recovery 移交 `main.cjs`（+46），配对 Tauri `update.rs`（+88）
- `b16e22b`/`2e874ef`/`09bcf5a (#44627)` — 工具行 dismissal（`store/tool-dismiss.ts` 新 `$dismissedToolRows` atom + `dismissedAtomForToolRow` `computed` 缓存 L18-25 按稳定 disclosure id 跨虚化保活）+ 把 copy 控件移到展开体内 `tool-fallback.tsx` +17/-3
- `bdd3868 (#45489)` — 颜色拾色器从 context menu 打开时保持打开
- `b82d2e5`/`b15dc58`/`7d183f6 (#45354)` — 图像生成占位圆 + 主题化 + 留在工具槽
- `78ce917` — 终端不透明 xterm canvas
- `1e25358 refactor(desktop)` — 删 `port-pool.cjs`/`.test.cjs`，改用 port 0 临时端口发现

---

<a id="5-desktop-backend-hardening"></a>
## 5. Desktop — Backend 端口 / 令牌硬化 + MCP 工具发现接入 /api/ws

### 5.1 端口分配改 port 0

`1e25358 refactor(desktop): use port 0 for ephemeral port discovery instead of PortPool reservation` — 删 `apps/desktop/electron/port-pool.cjs` + `.test.cjs`；新增 `backend-ready.cjs` 中 `waitForDashboardPort`（L16）解析子进程 stdout 的 `HERMES_DASHBOARD_READY port=<N>`；`main.cjs:4690-4691` `--port 0` 启动，子进程经 `web_server.py` SO_REUSEADDR 预绑定后通告端口。

### 5.2 令牌硬化（served token + 拒绝外部 backend）

- `e96fe06 fix(desktop): use served dashboard token for websocket auth` — 新增 `dashboard-token.cjs`（87 行）做 served-token fallback
- `e3ed772 fix(desktop): refuse a foreign backend's session token after readiness` — 当子进程已死时拒收 served token（防止外部进程冒充 backend）
- `cc726aa refactor(desktop): fold served-token adoption + foreign-backend refusal into one helper` — 折叠为 `adoptServedDashboardToken(baseUrl, spawnToken, {childAlive, label})`（L76）
- `b097d7b refactor(desktop): use native fetch in dashboard-token` — 替 http/https.request 为原生 `fetch` + `AbortSignal.timeout`
- `9ff0ba0 fix(desktop): prevent backend port-squat boot loop and pickPort self-collision`

### 5.3 MCP 工具发现拓展到 /api/ws backend

`7396977 (#44512) fix(desktop): discover MCP tools for dashboard /api/ws backends` — `tui_gateway/server.py` 加 `hermes_cli/main.py` +20 行，启动共享 MCP discovery 线程经 `hermes_cli.mcp_startup`；`_make_agent` 在工具 snapshot 前 join 该线程 — 让 dashboard 经 /api/ws 启的会话也能看见 MCP 工具。

### 5.4 Profile 路由

- `7a2d498 fix(desktop): route profile session reads` — `use-session-actions.ts` +65、`hermes.ts` +3
- `b1fe210 (#44510) fix(desktop): keep named-profile desktop backends per-profile` — backend 侧 `hermes_cli/main.py` 在 `HERMES_DESKTOP=1` 时跳过 machine-dashboard 重定向，让命名 profile backend 留在命名 profile
- `79c3ed3 (#45057) fix(desktop): new chat honours the active profile instead of rubberbanding to default` — `use-session-actions.ts` 把 null `$newChatProfile` 解析为活跃 gateway profile，让 `session.create` 总携带它
- `b6c7ebf (#44953) fix(tui): honor provider_routing config in the desktop/TUI backend`

### 5.5 自定义 OpenAI 端点 key 持久化

`4ddb033 (#43896) fix(desktop): collect + persist API key for custom OpenAI endpoints` — `ApiKeyForm` 从 `desktop-onboarding-overlay.tsx:636` 导出，placeholder `localApiKeyPlaceholder` L792；key 经 `saveOnboardingLocalEndpoint → setModelAssignment` 串联。后端 `web_server.py` +57：`/api/providers/validate` 接 `api_key`（Bearer 探测）；`/api/model/set` 持久化到 `model.api_key` 并注册 `custom_providers` 条目。

---

<a id="6-whatsapp-cloud"></a>
## 6. WhatsApp Business Cloud API 适配器（新平台）

`984e6cb feat(whatsapp): add WhatsApp Business Cloud API adapter`（salvage from #25023）：

- 新 `gateway/platforms/whatsapp_cloud.py`（**1956 行**）
- 共享 `gateway/platforms/whatsapp_common.py`（**367 行**） — 与既有 WhatsApp（多桥）共用 helper
- 注册 `gateway/run.py:6229-6239`（`Platform.WHATSAPP_CLOUD → WhatsAppCloudAdapter`）
- 配置向导 `hermes_cli/setup_whatsapp_cloud.py`（**530 行**）

**特性**：HMAC 验签 webhook + `wamid` 去重 + 原生按钮 + opus 语音经 ffmpeg + 蓝勾 typing 合并 — 群组延后。

`52c7976 fix(whatsapp-cloud): review follow-ups for #43921` — `WHATSAPP_CLOUD_{DM_POLICY,ALLOW_FROM,GROUP_POLICY,GROUP_ALLOW_FROM}` env 重载 + STT 准入门控。

---

<a id="7-photon-spectrum-ts-3"></a>
## 7. Photon iMessage — spectrum-ts 3.0→3.1 + markdown/reactions + telemetry

### 7.1 spectrum-ts 3.0 升级

`573c4e6 feat(photon): upgrade to spectrum-ts 3.0.0 (pinned) with markdown + reactions`：

- `plugins/platforms/photon/sidecar/package.json:15` 钉 `"spectrum-ts": "3.0.0"`（原 `^1.18.0`）
- 添加 `npm ci`
- `resolveSpace` 迁到 v3 `im.space.create/get`
- markdown 经 `markdown()` builder（`PHOTON_MARKDOWN` env 控）
- reactions 经新 sidecar 端点 `/react` & `/unreact`（`PHOTON_REACTIONS` env 控）
- `plugins/platforms/photon/adapter.py` +182 行

`9bfff6e chore(photon): bump spectrum-ts to 3.1.0` — `package.json:15` `"3.0.0" → "3.1.0"`（已验证 HEAD：`"spectrum-ts": "3.1.0"`）。

### 7.2 Agent 面 emoji reactions API

`156f4fb feat(photon): add agent-facing emoji reaction support`：

- 新 API：`plugins/platforms/photon/adapter.py:1038, :1068` `add_reaction` / `remove_reaction`
- `_last_inbound_by_chat` 跟踪器 `:236`（界 200）

`05470aa feat(messaging): expose action='unreact' in send_message + react dispatch tests`：

- `tools/send_message_tool.py:173-177` 把 `action=='react'` 派发到 `_handle_react`、`action=='unreact'` 派发到 `_handle_react(remove=True)`
- 通用 helper `:191`，最终在 `:259` 解析到 `add_reaction` / `remove_reaction`

### 7.3 Photon telemetry CLI 切换

`0a963d8 feat(photon): add telemetry toggle via hermes photon telemetry`：

- CLI 子命令 `plugins/platforms/photon/cli.py:62-89`（subparser）；`_cmd_telemetry` 在 `:353`
- 读写 `~/.hermes/.env` 的 `PHOTON_TELEMETRY` 经 `hermes_cli.config.save_env_value`
- sidecar 尊重 `plugins/platforms/photon/sidecar/index.mjs:66-67, :124`

### 7.4 Photon 其他修

- `2681c5a (#45566)` — `plugins/platforms/photon/cli.py:277` setup 提示改 `hermes gateway start`（去掉 `--platform photon`）
- `b4e95a2` — `adapter.py` 加 Windows-safe `os.kill` 注释 6 行
- `23305cf` — DM key normalization +22 行让 `last_inbound` 跨 GUID vs E.164 一致
- `a652131` — stdin-pipe 生命周期 + 端口回收（`adapter.py` +105 / `sidecar/index.mjs` +20 `PHOTON_SIDECAR_WATCH_STDIN`），防 gateway restart 让 sidecar 在端口上孤儿化
- `a23c0b3 fix(photon): use per-call httpx client in _sidecar_call` — `adapter.py:1284` `_sidecar_call`（`:1273`）内每调一个 `httpx.AsyncClient(timeout=30.0)`；持久 `_http_client` 仍保留在 `:225`

---

<a id="8-telegram-rich"></a>
## 8. Telegram — 富文本永远 on + 流式 final 保留 + Bot API 10.1

### 8.1 富文本永远 on

`a59d5e3 (#45584) feat(telegram): make rich messages always on`：

- `gateway/platforms/telegram.py` −15 行
- `cli-config.yaml.example` −5 行；toggle 完全删除
- prompt builder 复原富文本提示（`agent/prompt_builder.py` +13 行）

### 8.2 流式 final 保留富文本

`7c0605b fix(telegram): preserve rich formatting on stream final`：

- 新属性 `prefers_fresh_final_streaming` 在 `gateway/platforms/telegram.py:~975`
- `_should_attempt_rich(content, metadata)` 在 `metadata.expect_edits` 设置时跳过
- `RICH_MESSAGE_MAX_CHARS=32768`（从 BYTES 改名，alias 保留）
- 接线：`gateway/stream_consumer.py` 新 `_try_fresh_final`（+161 行）+ `gateway/platforms/base.py` +40

### 8.3 Bot API 10.1 跟进

`05b9c84 Add Telegram Bot API 10.1 rich message support` + `652dd9c fix: rich messages follow-ups — reply_parameters, send latch, opt-in default`。

---

<a id="9-matrix-slack-discord-wecom"></a>
## 9. Matrix / Slack / Discord / WeCom 一束

### 9.1 Matrix — 表格 + thread_require_mention

- `45f9099 fix(matrix): preserve markdown table structure` — `gateway/platforms/matrix.py:212` `_MatrixHtmlSanitizer._ALLOWED_TAGS` 加入 `table, tbody, td, th, thead, tr`
- `276e6cc fix(matrix): implement thread_require_mention to prevent multi-agent reply loops` — `matrix.py:853 _thread_require_mention` flag；parser `:1003`；强制 `:2541-2546`

### 9.2 Slack — 视频附件 / 反应生命周期 / action handler API

- `2a5dc0e (#45512) fix(slack): make video attachments available to agents` — `gateway/platforms/slack.py:48, 53` 导入 `SUPPORTED_VIDEO_TYPES` + `cache_video_from_bytes`；缓存 `:2655-2680`；`MessageType.VIDEO` 设 `:2780`；`gateway/run.py` +24 连接视频上下文 note
- `82d5701 fix(slack): ack reaction lifecycle events` — `reaction_added` / `reaction_removed` no-op handler `slack.py:899-904`
- `62e937b feat(plugins): expose register_slack_action_handler API` — `hermes_cli/plugins.py:826` 暴露；`slack.py:968-978` 消费
- `08e8bed fix(gateway): keep plugin action wrapper signature to (ack, body, action)` — 闭包工厂保 slack_bolt `kwargs_injection`

### 9.3 Discord — 语音转写 / lazy view classes / typing 适应

- `448a3f9 fix(discord): transcribe native voice notes` — `plugins/platforms/discord/adapter.py:4029` `_is_discord_voice_message_attachment`；AUDIO→VOICE 切换 `:4992`（注意：Discord 已从 `gateway/platforms/discord.py` 迁到 `plugins/platforms/discord/adapter.py`）
- `5a33176 fix(discord): define view classes after lazy discord.py install` — `adapter.py:5410` `_define_discord_view_classes()`；模块装载 `:6260` + 懒装路径 `:172` 调
- `be0728c fix: handle Discord typing indicator 429 gracefully` — typing loop 429 graceful
- `331cb38 fix: stop Discord typing after replies` — `gateway/platforms/base.py` +64 行 + `tests/gateway/test_keep_typing_timeout.py`

### 9.4 WeCom 群级 sender allowlist

`ad7436a fix(gateway): preserve WeCom per-group sender allowlists` — 新 helper `gateway/authz_mixin.py:124-174` `_adapter_group_has_sender_allowlist(platform, chat_id)` 读 `config.platforms[platform].extra["groups"][<id>].allow_from`（含 `allowFrom`/`*` wildcard）；在 own-policy 群分支 `:349-353` 调用：当 allowlist 存在时返回 `True` 即使 top-level `group_policy == "open"` — fc46354 fail-closed 默认的外科例外。

### 9.5 Yuanbao

- `31a0100 feat(state.db): persist platform_message_id` — `hermes_state.py:567` 加 `platform_message_id TEXT`；unique idx `:1090-1091`；`append_message(..., platform_message_id, ...)` `:2375, :2418, :2435`；`load_transcript` 读并映射到 `message_id` `:2865, :2892-2893`；A1 分支在 `gateway/platforms/yuanbao.py` +34 行
- `971cfaa refactor(yuanbao): migrate recall to load_transcript()` — A1 documented as degraded pending platform_message_id 列
- `0cc1a1d refactor(yuanbao): drop dead branch A1 message_id loop`
- `e20e0bd (#43508) feat(Yuanbao): support wechat forward msg`

---

<a id="10-gateway-auto-resume"></a>
## 10. Gateway — Auto-resume 重复 agent 防御

`6e2fd95 fix(gateway): claim session slot before auto-resume task to prevent duplicate agents` + `63097ee test(gateway): cover auto-resume full-path no-regression` + 文档（merge PR #45753 salvage from kshitijk4poor）：

- `gateway/run.py:4498 _schedule_resume_pending_sessions` 在 `asyncio.create_task` 创建之前（`:4602`）就在 `:4564` 占槽：`self._running_agents[entry.session_key] = _AGENT_PENDING_SENTINEL`（sentinel 定义 `:1279`）
- 包装器 `_guarded_handle_message` 在 `:4577`；释放守卫 `:4599` `if self._running_agents.get(_key) is _AGENT_PENDING_SENTINEL` — 只释放自己放的槽
- 测试 `tests/gateway/test_restart_resume_pending.py` 添加 "EXACTLY ONCE" 全路径回归

---

<a id="11-gateway-jsonl-deprecation"></a>
## 11. Gateway — JSONL 抄写降级，state.db 唯一源（4 commit refactor）

- `b4b118c refactor(gateway): drop _append_to_jsonl from mirror` — 删 `gateway/mirror.py::_append_to_jsonl`
- `351fdcc refactor(gateway): stop writing JSONL in append_to_transcript / rewrite_transcript` — 删 `SessionStore.append_to_transcript` / `rewrite_transcript` 的 JSONL 写入 + `get_transcript_path` 助手
- `024a8e3 refactor(gateway): drop JSONL fallback in load_transcript` — 删 "use whichever source is longer" 的 JSONL fallback，现 SQLite-only
- `1d27be0 test(gateway): pin SQLite-only load_transcript behaviour` — `tests/gateway/test_load_transcript_db_only.py` 钉行为

**保留**：磁盘上既有的 `.jsonl` 文件保留不动；`append_to_transcript` 的 `skip_db` flag 保留（现含义改为"跳过 SQLite"）。

---

<a id="12-gateway-misc"></a>
## 12. Gateway 其他修

### 12.1 压缩 binding 自重置

`16fb573 fix(gateway): clear bloated compression binding on compression-exhaustion auto-reset` — `gateway/run.py` 在 auto-reset 块捕 `new_entry = self.session_store.reset_session(session_key)`；非 None 时 reassign `session_entry = new_entry` 并调 `self._sync_telegram_topic_binding(source, session_entry, reason="compression-exhausted-reset")`。非 topic lane 是 no-op。回归 #9893/#10063；修 #35809。

### 12.2 Session-split 顺序前置

`202e318 fix(gateway): sync compression session splits before failures` — split 检测块（`agent_session_id != session_id`）提到 `if not final_response:` early-return **之前**；失败 return 现携带 `"session_id": effective_session_id` 和 `"history_offset": _effective_history_offset`（split 时为 0）。旧的失败后 sync 块删除。

### 12.3 重复 final stream 抑制

`197337c (#45517) fix(gateway): suppress duplicate final stream sends` — `gateway/stream_consumer.py:1311+`：edit-failure 分支，若 `finalize and is_turn_final` 且 `self._last_sent_text.endswith(self.cfg.cursor)` 且 `self._visible_prefix() == text`，置 `self._final_content_delivered = True`，让 gateway 抑制常规 full-final 发送（Telegram/Discord 装饰性编辑洪流控制，#36965, #25349）。

### 12.4 缓存 agent flush idx 复位

`a942bfd (#44327, #44518) fix(gateway): reset _last_flushed_db_idx when reusing cached agent` — `gateway/run.py:12548+ _init_cached_agent_for_turn`：当 `interrupt_depth == 0` 且 `hasattr(agent, "_last_flushed_db_idx")` 时设 `agent._last_flushed_db_idx = 0`。中断重入时保留。

### 12.5 Bare billing provider 跳过恢复

`e256f4a fix(gateway): don't restore a bare billing provider as the resumed session's provider` — `tui_gateway/server.py:1482 _BARE_BILLING_PROVIDERS = {"auto", "openrouter", "custom"}`。`_stored_session_runtime_overrides` 仅当其 lowercase 不在该集合内时恢复 `billing_provider`（`:1521`）；否则 fall through 到配置默认。修 #44022。

### 12.6 观察/归因扩展到位置 + 媒体

`6c26727 fix(gateway): extend observe+attribution to location and media handlers` — `gateway/platforms/telegram.py:4783 _handle_location_message` 和 `:4956 _handle_media_message`：被跳过路径调 `_should_observe_unmentioned_group_message` + `_observe_unmentioned_group_message`；触发路径调 `_apply_telegram_group_observe_attribution(event)`。回归 a9db0e2c7。

### 12.7 Windows gateway install lifecycle 强化

`d948de3 fix(gateway): harden Windows gateway install lifecycle` — `hermes_cli/gateway.py`/`gateway_windows.py` 拆 prompt 为 `start_now` + `start_on_login`；新 `enable_on_startup` 参数到 `systemd_install` / `install_linux_gateway_from_setup` gate `systemctl enable`。Windows：新 `_is_running_as_admin`、`_ACCESS_DENIED_PATTERN`、`pythonw` 启动（无控制台窗口）、幂等安装、陈旧 Scheduled Task 重建、UAC 移交保留决定经 `--elevated-handoff`。

### 12.8 Windows gateway 更新前先停

`78c11d9 fix(update): stop Windows gateways before mutating install`。

---

<a id="13-platform-document-classify"></a>
## 13. Platform 文档分类拓展

- `8207ae8` + `1e29ab3 fix(gateway): add Signal message type classification for documents` — `gateway/platforms/signal.py:599-612` VOICE/PHOTO/VIDEO 分支 + 捕获式 `MessageType.DOCUMENT`
- `74180eb fix(gateway): classify SimpleX non-image/non-audio files as DOCUMENT` — `plugins/platforms/simplex/adapter.py:603, :630-632` 非图非音媒体（含 `application/octet-stream`）→ DOCUMENT
- `f03f161 fix(gateway): classify email document attachments as DOCUMENT` — `gateway/platforms/email.py:468-481` 混合附件时 DOCUMENT 优先于 PHOTO
- `0fd34e8 (#44778) fix(teams): cache document/video/audio attachments and classify as DOCUMENT`
- `f82cb48 fix(platform): add .xls, .doc, .ppt to SUPPORTED_DOCUMENT_TYPES` — `gateway/platforms/base.py:1131, 1133, 1135`；`1185dfd test: cover legacy Office document extensions` 加 `tests/gateway/test_document_cache.py`

---

<a id="14-security-wave"></a>
## 14. 安全 wave

### 14.1 Own-policy adapter fail-closed

`fc46354 fix(security): fail closed when an own-policy gateway adapter has no allowlist` — `gateway/authz_mixin.py` `_is_user_authorized`：adapter 信任需 `effective_policy == "allowlist"`。新 helper `_adapter_group_policy` 镜像 `_adapter_dm_policy`。影响 WhatsApp、WeCom、Weixin、QQBot、Yuanbao。`"open"`/`"pairing"` 落入默认拒（`GATEWAY_ALLOW_ALL_USERS` / `{PLATFORM}_ALLOW_ALL_USERS` opt-in）。源：`gateway/authz_mixin.py:90-150, 271-301`。

### 14.2 /api/status 受门时 redact

`3380563 fix(security): stop /api/status leaking host paths and PID on gated binds` — `hermes_cli/web_server.py get_status()`：受门路径删 `hermes_home`、`config_path`、`env_path`、`gateway_pid`、`gateway_health_url`。仅当 `if not auth_required:`（loopback / `--insecure`）时回填。与 `should_require_auth` 同一 loopback/受门拆分。

### 14.3 Host-derived API key 链尾兜底

`c6a992e fix(security): derive <VENDOR>_API_KEY from host as final credential fallback`：

- 新 `hermes_cli/runtime_provider.py:103 _host_derived_api_key()` — 用注册标签（倒数第二）剥 `api.`/`www.`，拒 IP/loopback，过滤已处理 vendor `{OPENAI, OPENROUTER, OLLAMA}`
- 加到三条候选链 `:767, :823, :948`
- Lookalike `api.deepseek.com.attacker.test` 解析为 `"attacker"`，不是 `"deepseek"`

### 14.4 防 API key 泄到非权威端点

`5908822 fix(security): prevent API key leakage to non-authoritative custom endpoints` — `_resolve_openrouter_runtime` 把 `OPENAI_API_KEY` gated 到 `openai.com` / `openai.azure.com`；`OPENROUTER_API_KEY` gated 到 `openrouter.ai`；不匹配主机 → `no-key-required`。

`9514ddb fix(security): address review feedback from pmos69` — 同样 host-gate 加到 `_resolve_named_custom_runtime` 直接 alias（`_da_is_openai_url`/`_da_is_openrouter`）和 named-custom（`_cp_*`）路径；OpenRouter 镜像例外当 `OPENROUTER_BASE_URL` env 覆盖时经 `_is_openrouter_context`。

### 14.5 `os.chmod` 父目录守护

`4ead464 fix(security): guard os.chmod(parent) against / and top-level dirs` — `hermes_constants.py:265 secure_parent_dir(path)`：拒 chmod 当 `parent == Path("/")` 或 `len(parent.parts) < 3`。替换 5 个调用点：`agent/google_oauth.py`、`hermes_cli/auth.py`（×3：`_save_auth_store`、`_save_qwen_cli_tokens`、`_write_shared_nous_state`）、`tools/mcp_oauth.py`。

### 14.6 Profile 下 write-deny <root>/.env

`5edb346 security(file-safety): also write-deny <root>/.env when running under a profile` (#15981) — `agent/file_safety.py:19 _hermes_root_path()`（用 `get_default_hermes_root()`）；`build_write_denied_paths` 在 `str(hermes_home / ".env")` 旁追加 `str(hermes_root / ".env")`（HEAD 行 ~31-45）。使用于 `:116, :218, :350, :383`。

---

<a id="15-kanban-hardening"></a>
## 15. Kanban 硬化

- `34120a0 (#28712) fix(kanban): worker-initiated block must not be auto-promoted` — `hermes_cli/kanban_db.py:2843 _has_sticky_block` 读最近 `"blocked"`/`"unblocked"` 行；`recompute_ready`（`:2923`）在 `cur_status == "blocked" and _has_sticky_block(...)` 时跳过。断路器 `gave_up` 事件仍自动恢复。
- `7bcdced (#28683) fix(kanban): respawn guard defers blocker_auth instead of auto-blocking` — `dispatch_once` 不再在 `blocker_auth` 自动 block；追加 `respawn_guarded` 事件 + `{reason}`，任务留在 `ready`。
- `88ee58f (#28680) fix(kanban): stale reclaim must not tick failure counter` — `detect_stale_running` 不再调 `_record_task_failure`；`stale` 事件仅 audit；`KANBAN_GUIDANCE` 写明 1h heartbeat 契约
- `7552e0f fix(kanban): also hoist idx_events_run + drop redundant inner create` — `idx_events_run` 从 `SCHEMA_SQL` 提到 `_migrate_add_optional_columns`（无条件，run_id migration 之后）；移除多余的 `CREATE INDEX idx_tasks_idempotency`
- `7c622b6 fix(kanban): migrate task session index after columns` — `idx_tasks_session_id`、`idx_tasks_tenant`、`idx_tasks_idempotency` 从 SCHEMA_SQL 提出来；在 additive `ALTER TABLE` 之后创建。防止 legacy board 启动失败
- `cb125c2 (#45590) fix(kanban): pin assigned profile toolsets for workers` — `kanban_db.py:6678 _resolve_worker_cli_toolsets` 在 `set_hermes_home_override(hermes_home)` 下加载受配 profile，返 `sorted(_get_platform_tools(cfg, "cli"))`；`_default_spawn`（`:6845`）追加 `--toolsets <csv>` 到 CLI 调用
- `1a7bb98 fix(gateway): harden kanban and provider cleanup races` — `agent/agent_runtime_helpers.py` 提取 `_iter_pool_sockets()`（防御性 httpcore traversal 含 anyio `SocketAttribute.raw_socket`）；`cleanup_dead_connections`/`force_close_tcp_sockets` 共享；`chat_completion_helpers.py::interruptible_api_call` 把 `request_client_holder` 包入 `threading.Lock`，新 `_set_request_client`/`_take_request_client`/`_close_request_client_once(reason)`，让清理 race-safe

---

<a id="16-billing-credits"></a>
## 16. Billing — `/credits` slash command

`7ba5df0 (#44776) feat(billing): /credits command — balance + portal top-up handoff`：

- 注册：`hermes_cli/commands.py:217 CommandDef("credits", "Show Nous credit balance and top up", "Info")`；Slack-only via-/hermes set 在 `commands.py:1056 _SLACK_VIA_HERMES_ONLY = frozenset({"credits"})`
- 共享构建：`agent/account_usage.py:342 class CreditsView`、`:358 build_credits_view()`；门户 URL `hermes_cli/nous_account.py:145 nous_portal_topup_url()`
- CLI handler：`cli.py:8399 _show_credits()`（当 `self._app is None` 时文本 fallback 给 TUI worker / 管道）
- Gateway handler：`gateway/slash_commands.py:2945 _handle_credits_command()`
- TUI：`tui_gateway/server.py:4773 @method("credits.view")`；前端 `ui-tui/src/app/slash/commands/credits.ts`

### 16.1 Credits 通知抑制（充值时不提示）

`c196269 (#44716) fix(credits): suppress usage gauge when top-up funds exist + add display.credits_notices toggle`：

- 抑制：`agent/credits_tracker.py:297 if state.purchased_micros > 0:` 在 `evaluate_credits_notices`
- 开关：`hermes_cli/config.py:1449 "credits_notices": True`
- 缓存读：`run_agent.py:2860 _credits_notices_enabled()`

---

<a id="17-dashboard-profile-scope"></a>
## 17. Dashboard — Profile-scope 全量 + clone-from 任意源

### 17.1 任意 source 克隆 profile

`28bf8fb feat(dashboard): clone profiles from any source` + `9b5f7b6 fix(profile): make clone-from a full source selector` + `cc14b74 docs(profile): update clone-from references`：

- API model：`hermes_cli/web_server.py:8519 class ProfileCreate` 含 `clone_from`、`clone_from_default`、`clone_all`；逻辑 `:8789-8830`（显式 `clone_from` source ≠ "default" 允许）
- 选择器 UI：`apps/desktop/src/app/profiles/create-profile-dialog.tsx`（+ web `web/src/pages/ProfilesPage.tsx`）
- CLI：`hermes_cli/subcommands/profile.py:46` `--clone-from` 接受任意 profile name；`main.py:10053`

### 17.2 会话 / 分析 / Channels 全部 profile-scope

`62b4618 (#45598) fix(dashboard): scope sessions and analytics to selected profile` + `88dbf95 (#44792) fix(dashboard): profile-scope Channels endpoints and seed per-profile .env`：

- 所有端点包 `_profile_scope(profile)` 在 `hermes_cli/web_server.py:9145`；profile 参数 thread 到 GET/PUT/POST
- Channels 端点 `:4741` GET、`:4760` PUT、`:4802` test — 加到受 scope allowlist `web/src/lib/api.ts:66 PROFILE_SCOPED_PREFIXES`（含 `/api/messaging/platforms`）
- 存储 = 每个 profile 的 HERMES_HOME；scope 时无 os.environ fallback

### 17.3 Dashboard 其他修

- `d206e1f fix(dashboard): keep local file browser on home` — `web_server.py:1230 _managed_files_policy()` — 只在实际 install root 是 `/opt/data` 时锁定
- `9102d4a (#44511) fix(dashboard): show Windows 11 in host panel` — `web_server.py:1689 _WINDOWS_11_MIN_BUILD = 22000`；`_windows_build_number` regex on `10.0.<build>`
- `56b79f1 (#29997) fix(dashboard): remove country flags from language picker` — `web/src/components/LanguageSwitcher.tsx`、`web/src/i18n/context.tsx`、`web/src/main.tsx`、`web/package.json`
- `a118b94 (#45150) fix(dashboard): skill installs from the dashboard silently auto-cancel` — `web_server.py:8137 / :8879` 加 `--yes`
- `afffb8d fix(dashboard): use browser scrollback for chat wheel` — `web/src/pages/ChatPage.tsx:301 scrollback: 5000`，`term.scrollLines(...)` 替转义序列
- `709e37e (#28534) fix(dashboard): add scheduled kanban i18n strings` — 16 个 `web/src/i18n/*.ts`
- `edb2d91 (#28814) feat(web): migrate dashboard checkboxes to @nous-research/ui + DS polish`

---

<a id="18-cli-misc"></a>
## 18. CLI 杂修

### 18.1 持久化 approval / clarify 滚屏摘要

`4474873 (#44702) feat(cli): persist resolved approval/clarify prompts in scrollback` — `cli.py:9491 _persist_prompt_summary(icon, label, detail, outcome)`；调用点 `:9549` clarify、`:9669` approval。Gate：`cli.py:461 / hermes_cli/config.py:1435 "persist_prompts": True`。

### 18.2 Codex token 自愈

`aa07983 fix(auth): self-heal missing Codex access tokens` + `bd66e7e fix(auth): self-heal Codex refresh_token rotation by reimporting from ~/.codex` + `311ff96 review: validate refresh_token, path-agnostic recovery log, map author email`：

- `hermes_cli/auth.py:3527 _recover_codex_tokens_from_cli(reason)` — 必须同时含 access + refresh token 才持久化
- 接到 `:3671 _refresh_codex_auth_tokens` — 仅当 `exc.relogin_required` 为真时触发；429 配额仍原样 re-raise
- token import：`:3715 _import_codex_cli_tokens()` 读 `~/.codex/auth.json`（CODEX_HOME 感知 → recovery log 路径无关）

### 18.3 渲染状态在 resize 时保活

`2abcae9 fix(cli): preserve renderer state on resize` — `cli.py:3729 _recover_after_resize`、`:3757 _schedule_resize_recovery`（保 prompt_toolkit screen cache）。

### 18.4 Windows dashboard profile 子进程 re-exec

`343803b (#44282, #44446) fix(cli): use subprocess on Windows for dashboard profile re-exec` — `hermes_cli/main.py:10371-10380` Windows 上改 `subprocess.Popen(reexec_argv).wait()`（避 Py 3.14 `os.execvpe` STATUS_ACCESS_VIOLATION）。

### 18.5 用户改过的 skill 名显式

`58591d9 feat: show names of user-modified skills in bundled skill sync summary` — `tools/skills_sync.py:423-430` `MAX_SHOW = 5`，`+N more` 后缀；`f"{len(names)} user-modified (kept): {shown}"`。

### 18.6 CLI 其他

- `6724daa (#45592) fix: keep CLI idle timer ticking` — `cli.py` +7
- `0333a99 (#45582) fix: merge session-only model analytics rows` — `hermes_cli/web_server.py` +66 + 测试 +50
- `6b40736 fix(tui): config.yaml wins over env model seed in per-turn sync` — `_config_model_target()` 先读 config.yaml
- `b3f5e17` / `81cdbbd` / `d6df38b fix(cli/tui): wrap long approval commands` — `ui-tui/src/components/prompts.tsx`（b3f5e17 `wrapAnsi`）+ CLI 同步（81cdbbd/d6df38b）
- `e13f242 fix(cli): preserve setup config picker writes` — model picker 写后 `hermes_cli/setup.py` 重新同步 config
- `425aba7 fix(cli): ignore stale HERMES_TUI_RESUME env`
- `8c3b065 fix(cli): show active profile in TUI prompt`
- `f1254b1 (#28688) fix(cli): exit prompt_toolkit cleanly on SIGTERM/SIGHUP`
- `2667601 fix(tui): keep reasoning-only assistant turns visible on session resume` — `tui_gateway/server.py:3679 _history_to_messages` 当 reasoning 存在时即使 text 空也保留 assistant 轮

---

<a id="19-xai-retirement"></a>
## 19. xAI — May 15 2026 退役迁移

新模块 `hermes_cli/xai_retirement.py`（已验证存在）：
- `RETIREMENT_DATE = "May 15, 2026"`（已验证 `:16`）
- `_RETIRED_MODELS` 映射（`:23`）：`grok-4`、`grok-4-0709`、`grok-4-fast{,-reasoning,-non-reasoning}`、`grok-4-1-fast` 变体、`grok-code-fast-1`、`grok-3`、`grok-imagine-image-pro` → `grok-4.3` + `reasoning_effort`

新 CLI：`hermes_cli/migrate.py cmd_migrate_xai`（已验证存在；dry-run / `--apply` / `--no-backup`）；接到 `hermes_cli/main.py` 子解析器。

`9ff98da feat(xai): apply_migration — rewrite config.yaml in-place via ruamel round-trip` — ruamel.yaml 保格式 in-place 重写。

`b4ba425 feat(doctor): surface xAI model retirement in hermes doctor` — `hermes_cli/doctor.py run_doctor` xAI 段。

`a8a05c8 feat(cli): warn about retired xAI models at chat startup` — `hermes_cli/main.py cmd_chat` 启动告警。

`b4afc65 fix(xai): restore encrypted reasoning replay across turns` — `agent/codex_responses_adapter.py:290 replay_encrypted_reasoning`（killswitch 注释 `:306-323`）+ `agent/transports/codex.py`。

`64a9a19 (#28952) fix(xai-oauth): pin inference base_url to x.ai origin` — `hermes_cli/auth.py _xai_validate_inference_base_url()`。

`a0c0312 feat(web): add xAI Web Search provider plugin` — 新 `plugins/web/xai/{__init__.py, plugin.yaml, provider.py}`（provider.py 560 行）；与 firecrawl/tavily/exa/brave/searxng/ddgs 并列注册；用 `tools/xai_http.py`。

`ca192cf Add opt-in xAI TTS speech tag pauses`。

---

<a id="20-bitwarden"></a>
## 20. Bitwarden Secrets Manager 集成

`552e9c7 (#30035) feat(secrets): Bitwarden Secrets Manager integration with lazy bws install`：

- 新 `agent/secret_sources/bitwarden.py`（515 行；HEAD 体积 25512 字节，已验证存在）
  - `_BWS_VERSION="2.0.0"`（L59）
  - 钉下载 `:62` `https://github.com/bitwarden/sdk-sm/releases/download/bws-v...`
  - `install_bws(force=False)` `:290` 懒自动下载到 `~/.hermes/bin/bws`（SHA-256 校验）
  - `fetch_bitwarden_secrets()` `:439`
  - `apply_bitwarden_secrets()` `:591`
- CLI 表面：`hermes_cli/secrets_cli.py` — `setup/status/sync/disable/install`
- env loader：`hermes_cli/env_loader.py`（+77 LOC）
- config schema：`hermes_cli/config.py`（+31）

---

<a id="21-profile-clone-improvements"></a>
## 21. Profile clone 改进

- `7a318aa (#45246) fix(profiles): exclude session history, backups, and snapshots from --clone-all` — `hermes_cli/profiles.py:116 _CLONE_ALL_HISTORY_EXCLUDE_ROOT = frozenset({state.db, sessions, backups, state-snapshots, checkpoints})`；任意 source 在 `:186` 应用
- `135fe90 (#45247) fix(profiles): backfill .env for pre-existing profiles on hermes update` — `hermes_cli/profiles.py:980 backfill_profile_envs()`；接到 `hermes_cli/main.py:8958`
- `5b85720 fix(profiles): correct misleading per-profile gateway port docstrings` — `hermes_cli/service_manager.py _render_run_script` + `_maybe_register_gateway_service`
- `fa2aba9 docs(docker): explain per-profile gateway ports for multi-profile setups`

---

<a id="22-tui-misc"></a>
## 22. TUI 杂修

- `2667601` reasoning-only resume（详见 18.6）
- `b6c7ebf (#44953) fix(tui): honor provider_routing config in the desktop/TUI backend` — `tui_gateway/server.py:1726 _load_provider_routing()`；`_make_agent` `:3376-3410` 转发六项偏好
- `b3f5e17 fix(tui): wrap long approval commands in the Ink overlay`（`wrapAnsi` 在 `ui-tui/src/components/prompts.tsx`）

---

<a id="23-auxiliary"></a>
## 23. Auxiliary — Nous tags 范围限定 + 主 provider base_url 保留

- `819def4 fix(agent): scope Nous tags to Nous auxiliary calls` — `agent/auxiliary_client.py:5008` 仅在辅助路径合并 `_nous_portal_tags()`；`NOUS_EXTRA_BODY` 快照 `:480`
- `4b646bc (#45587) fix(auxiliary): preserve main provider base url` — `agent/auxiliary_client.py` 在选 aux provider 时保留主 provider 的 `base_url`
- `2714fc8 fix(agent): re-enter retry loop on genuine Nous 429 so fallback guard runs` — `agent/conversation_loop.py:2644 retry_count = max(0, max_retries - 1)`；genuine-rate-limit 分支 `:2598-2637`

---

<a id="24-compression-wave"></a>
## 24. Context Compression refactor wave

11 commit 在 `agent/context_compressor.py` 跨多个轮次重塑摘要 + 压缩边界 + replay 顺序。

### 24.1 保留近期轮 + 用户聚焦

- `aec3885 fix(agent): preserve recent turns during compression` — `agent/context_compressor.py` +31 行 + 测试
- `434c684 fix(agent): focus automatic compression on recent user turns` + `c7bee8f refactor(agent): drop unused tail_start param from _derive_auto_focus_topic` — `agent/context_compressor.py:1685 _derive_auto_focus_topic`

### 24.2 Flush 游标 clamp/snapshot

- `5d0408d fix(agent): clamp flush cursor after repair_message_sequence compaction (#44837)`
- `8905ee6 fix(agent): rewind flush cursor exactly when repair compacts before the cursor` — 替换 clamp 为按身份的快照 helper `repair_message_sequence_with_cursor`（`agent/agent_runtime_helpers.py:448-472`）；调用点 `agent/conversation_loop.py:601-602`；fallback clamp 保留在 `run_agent.py:1543-1564`

### 24.3 末尾助手 reply 不入摘要 / 合并 tail 拆分

- `691ff7c fix(compressor): keep last visible assistant reply out of compaction summary + label handoffs (#29824)` — `agent/context_compressor.py:1910 _ensure_last_assistant_message_in_tail`（`:2128` 调），镜像 `_ensure_last_user_message_in_tail`（`:1968` / `:2121`）
- `2fef3e2 fix(webui): split merge-into-tail compaction so reply renders as its own bubble (#29824)` — `agent/context_compressor.py:2339-2390 _merge_summary_into_tail` flag
- `68536d4 test(compressor)` — 21 case 在 `tests/agent/test_context_compressor.py`

### 24.4 SUMMARY_END_MARKER 提至模块级

- `0db5cb8 refactor(agent): hoist summary end marker to _SUMMARY_END_MARKER; strip it on rehydration` — `agent/context_compressor.py:92 _SUMMARY_END_MARKER`；`_strip_summary_prefix` 在 `:1637` 剥尾部 marker（`:1654-1655`）（已验证 HEAD：92 / 1654 / 1655 / 1682）
- `749b721 fix(compression): always append END OF CONTEXT SUMMARY marker to standalone summaries regardless of role` — 末 marker 在 `agent/context_compressor.py:2369` 给两种 role 都加

### 24.5 摘要消息 wire-strip 元数据

- `956af7f fix(agent): add metadata flag to context compression summary messages (#38389)` — `agent/context_compressor.py:85 COMPRESSED_SUMMARY_METADATA_KEY = "_compressed_summary"`（下划线前缀让 wire 自动剥）；helper `_has_compressed_summary_metadata` `:1672-1682`；插入 `:2375` / `:2389`（已验证 HEAD）
- `7e46533 test: compressed-summary metadata flag set in-process, stripped on wire` — `tests/agent/test_compressed_summary_metadata.py`（+93）

### 24.6 MEDIA: 指令剥离

- `286ecd2 fix(agent): strip MEDIA directives from compressor summarizer input (#14665)` + `8e5b759 refactor(agent): hoist MEDIA-directive regex to module level` — `agent/context_compressor.py:186 _MEDIA_DIRECTIVE_RE`，在 `_serialize_for_summary` `:1048` 应用

### 24.7 可行性检查延后

`6cb9917 perf(compression): defer feasibility check to first compression attempt (#28957)` — `_compression_feasibility_checked` guard 初始化 `agent/agent_init.py:1678`；just-in-time 检查 `agent/conversation_compression.py:305-318`。

---

<a id="25-perf-wave"></a>
## 25. Perf wave

### 25.1 Agent-loop 47% per-conv 调用削减

`544c31b (#28866) perf(agent-loop): cut 47% of per-conversation function calls via 3 targeted hot-path optimizations` — 三热点：(a) `hermes_cli/config.py` `load_config_readonly()` 快路径（+50 行）接到 `hermes_cli/timeouts.py`（8 行）；(b) `agent/redact.py` 重写（+137）；(c) `run_agent.py`（+16）— 即 `_resolved_api_call_timeout`。Commit message：~47% 函数调用削减；`load_config` 21→缓存调用。

### 25.2 Adaptive subprocess poll

`6bd4311 (#29006) perf(terminal): adaptive subprocess poll cuts ~195ms off every tool call` — `tools/environments/base.py:654-718 _poll_sleep = 0.005`（`:654`），自适应 `* 1.5` 封顶 `0.2`（`:716-718`）。每短命令工具调用省 ~195ms。

### 25.3 其他 perf

- `6cb9917`（详见 24.7）
- `edc36f3`/`7c226cc`/`3cf7d43`（详见 2.2）
- `c29b4f5`/`6dbbf20 perf(termux): speed up tui cold start` + 非 tui cold start
- `784febe (#28864) perf(cli): defer openai._base_client import via sys.meta_path finder`

---

<a id="26-models-providers"></a>
## 26. Models / Providers

- `957a8ff fix(bedrock): omit sampling params for restricted Claude models` — Opus 4.7/4.8 受限；Bedrock kwargs builder 复用 Anthropic-native guard
- `4fd9397 fix(codex): drop extra_headers for chatgpt.com backend` — `agent/transports/codex.py` −20 + 测试
- `7d11fa4 fix(codex-responses): let final_answer complete top-level incomplete responses` — `agent/codex_responses_adapter.py` +102 行测试
- `5acd185 fix(moonshot): handle union type arrays in tool schemas` — `agent/moonshot_schema.py` +9
- `eed61a1 fix(gemini): add role field to systemInstruction` — `agent/gemini_native_adapter.py:2`
- `c8e5f34 fix(gemini): strip native self prefixes before generateContent (#36141)` — 剥 `google/`、`gemini/` 自前缀
- `bc060c7 (#45492) fix(models): remove unavailable claude-fable-5` — `hermes_cli/models.py` + 模型目录 JSON
- `3803e5f (#45289) fix(agent): don't treat custom:<name> pools as cross-provider mismatch`
- `2589656 fix(chat_completions): strip tool_name from messages for strict providers`
- `74c5158 (#45597) fix(model): show bare custom endpoints in gateway picker`
- `643dc82 Fix custom provider identity loss in session persistence` — `_runtime_model_config` 持久化命名 provider 身份
- `d9829ab fix(model): match custom provider by active base url` — `hermes_cli/main.py` +162 + 测试 +112
- `6a159be (#27132) fix(runtime): treat 'ollama'/'vllm'/'llamacpp' aliases like 'custom' for base_url trust`
- `e256f4a` 跳过 bare billing provider 恢复（见 12.5）
- `9688c1a (#45283) chore: add Kimi K2.7 code catalog slug`
- `ebe0b77 fix(model-switch): mark bare custom provider as current`

---

<a id="27-skills-hub"></a>
## 27. Skills hub

- `2ee69d0 (#44500) fix(skills): let ClawHub index build walk past the 12s browse budget` — `CATALOG_WALK_BUDGET_SECONDS` 仅在 `max_items > 0` 时生效；unlimited walk 不再被 12s 锁
- `fc7e04e fix(skills-hub): deduplicate search results by identifier, not name` + `8f92327 fix(skills-hub): fix dedup in browse_skills() programmatic API` + `c6a380e fix(skills-hub): widen identifier-dedup to GitHubSource`
- `1899c8f fix(skills): run youtube transcript helper through uv` — `skills/media/youtube-content/scripts/fetch_transcript.py`
- `3fde8c1 (#30042) fix(skills): prune dependency/venv dirs from all skill scanners`
- `4373e80 (#45616) fix(docs): reuse healthy skills index during Pages deploys` — `.github/workflows/deploy-site.yml` +99 行
- `87c6edc fix(skills): add timeout to Google OAuth urlopen calls` — `plugins/platforms/google_chat/oauth.py:3` + GWS bridge
- `5e9d7a766` salvage — 已并入

---

<a id="28-computer-use"></a>
## 28. Computer-use 视觉路由

- `e02a7e5 (#24015) fix(computer_use): route SOM/vision captures via auxiliary.vision` + `531efe7 fix(computer_use): add helper to decide capture vision routing` — helper `tools/computer_use/vision_routing.py:164 should_route_capture_to_aux_vision`；`tools/computer_use/tool.py:629-642` 消费
- `5aa4727 (#24170 bug 1) fix(computer-use): surface app=… filter no-match instead of silently using frontmost`
- `4cc1887 (#24170 bugs 2 & 5) fix(computer_use): preserve app context for capture_after; fix element label parsing`
- `18cd1e5 fix(computer_use): correct type_text MCP tool name and implement drag action`

---

<a id="29-plugins-misc"></a>
## 29. Plugins / Memory / Delegation / MCP

### 29.1 Slack action handler API

`62e937b feat(plugins): expose register_slack_action_handler API` — 见 9.2。

### 29.2 共享 hook / middleware / tool 多 plugin 归属

`44bd478 fix(plugins): credit shared hook/middleware/tool names to every plugin` + `889a136 fix(plugins): clear _plugin_platform_names on force-rediscover`。

### 29.3 Plugins 浏览器粘贴 URL 规范化

`08890d7 (#33539) fix(plugins): normalize browser-pasted GitHub repo URLs`。

### 29.4 Memory 多模态 flatten

- `87893fe fix(memory): flatten multimodal content before provider sync` — `_sync_external_memory_for_turn` 前展平
- `15439be refactor(memory): reuse _summarize_user_message_for_log instead of forking it`

### 29.5 Delegation default timeout 移除

`bba9b51 (#45149) fix(delegation): remove the default subagent wall-clock timeout` — `tools/delegate_tool.py:567 DEFAULT_CHILD_TIMEOUT: Optional[float] = None`（已验证 HEAD）。Docstring `:566, :575` 注释 "off by default"。配置读 `delegation.child_timeout_seconds` `:401-439`。

### 29.6 MCP capability-gate

`5affecb (#44550) fix(mcp): capability-gate tools/list so prompt-only MCP servers can connect` — `tools/mcp_tool.py:1206-1210 supports_tools` 检 `InitializeResult.capabilities.tools`（注释 `:1208`，已验证 HEAD）。

### 29.7 工具 schema $ref default 剥离

`b2d151a fix(tools): strip default from $ref nodes in tool schemas` — Fireworks/Kimi `{"$ref": "...", "default": null}` 剥。

### 29.8 Approval allow_permanent 统一 TUI/Desktop

`81436e1 fix(approval): carry allow_permanent to TUI + desktop approval prompts` + `55a18e6 chore(approval): tighten allow_permanent comments + DRY the no-always opt set`。

### 29.9 Agent — 二进制 @file ref actionable + send --file binary 错误

- `fa5e98f (#45116) fix(send): helpful error when --file gets a binary; document MEDIA: attachments`

### 29.10 Coding context

- `ab06ef8 fix(coding): teach agents terminal env state persists` — 回归测试 LocalEnvironment env-snapshot
- `96cc7ee fix(coding): don't provide worktree root in context`

---

<a id="30-update-installer"></a>
## 30. Update / Installer / Doctor

### 30.1 Post-pull 语法校验 + 回滚

`aedb8ac (#28669) feat(update): syntax-validate critical files post-pull, auto-rollback on failure`：

- `hermes_cli/main.py:4325 _capture_head_sha()`、`:4340 _validate_critical_files_syntax()` 返 `(ok, failing_path, syntax_error)`
- 接到 `_cmd_update_impl` 在 `:8651`（pre-pull SHA）+ `:8687`（校验 + 回滚）

### 30.2 Unmerged index clear

`573b964 fix(installer): clear an unmerged git index before stashing on update` + `c814d3d test(installer): regression for unmerged-index update failure` — `scripts/install.sh:1115-1125`（`git ls-files --unmerged` → `git reset` 后 `git stash`）；`scripts/install.ps1` 镜像；`tests/test_install_unmerged_index.py`。

### 30.3 不弹 polkit

`9b01c4d (#45145) fix(update): never spawn an interactive polkit prompt when restarting a system-scope gateway` — `hermes_cli/main.py:9293-9563` `--no-ask-password` for systemctl。

### 30.4 Windows hermes.exe 隔离

`2a7308b (#26670, #26677) fix(update): quarantine hermes.exe vs concurrent Windows instance`。

### 30.5 Doctor

- `905ed41` + `bea6c1c` + `a5e9b17 fix(doctor): stop recommending the npm-crashing audit fix, explain build-tool advisories`
- `12c3983 fix(doctor): attach codex CLI hint to OpenAI Codex auth warning for #27975`

---

<a id="31-ci-tests"></a>
## 31. CI / Tests

- `c41a653 fix(tests): mock subprocess.Popen in all _handle_update_command tests` + `6ff39c3 fix(tests): guard against real 'hermes update' subprocess spawns in conftest` — `tests/conftest.py _live_system_guard`
- `2f9d187 fix(ci): remove pytest-timeout, use per-file timeout only`
- `8044bf0 fix(ci): only save test durations when tests pass`
- `4d68984 fix(tests): remove no-longer-needed forensics`
- `a0bd11d (#28626) fix(tests): catch up 25 stale tests after recent merges`
- `48be2e0 (#29016) test: use subprocesses for each test file`
- `e2fd462 (#28861) ci(tests): add pytest-timeout 60s hard cap to break suite-teardown deadlock`

并 AUTHOR_MAP 增补：liuhao1024、WompaJango、kdunn926、bcsmith528、itsflownium、Glucksberg、nycomar、yichengqiao21（→ YarrowQiao）、erikengervall、xxxigm 等 10 条 `chore(release)` / `chore` 映射。

---

<a id="32-verification"></a>
## 32. 验证基线

- **Clone**：`/tmp/hermes-agent` blobless clone @ `0a865e5948cb836eba93620e365703ec38cf76e3`，全 checkout
- **Commit 列表**：`git log --no-merges 6c752ca3a..HEAD --format="%h|%cI|%s"` 361 行；按 scope 分到 5 个独立 commit 包文件分发给 5 个 research agent
- **直接 spot check（本会话主线程实证）**：
  - `pyproject.toml:10` `version = "0.16.0"` ✓
  - `hermes_cli/__init__.py:17-18` `__version__ = "0.16.0"` / `__release_date__ = "2026.6.5"` ✓
  - `plugins/platforms/photon/sidecar/package.json:15` `"spectrum-ts": "3.1.0"` ✓
  - `hermes_cli/commands.py:217` `CommandDef("credits", "Show Nous credit balance and top up", "Info")` ✓
  - `hermes_cli/commands.py:1056` `_SLACK_VIA_HERMES_ONLY = frozenset({"credits"})` ✓
  - `agent/context_compressor.py:85` `COMPRESSED_SUMMARY_METADATA_KEY = "_compressed_summary"` ✓
  - `agent/context_compressor.py:92` `_SUMMARY_END_MARKER = (` ✓
  - `agent/context_compressor.py:1654-1655 / :1682` 剥 marker / 元数据查询 ✓
  - `gateway/authz_mixin.py:93 _adapter_group_policy` / `:124 _adapter_group_has_sender_allowlist` / `:351-353` 调用点 ✓
  - `hermes_cli/web_server.py:1679-1681` redact `hermes_home`/`config_path`/`env_path` ✓
  - `tui_gateway/server.py:1482 _BARE_BILLING_PROVIDERS = {"auto", "openrouter", "custom"}` ✓
  - `agent/secret_sources/bitwarden.py` 存在（25512 字节）✓
  - `hermes_cli/xai_retirement.py:16 RETIREMENT_DATE = "May 15, 2026"` / `:23 _RETIRED_MODELS` ✓
  - `hermes_cli/migrate.py` 存在 ✓
  - `gateway/platforms/whatsapp_cloud.py` 1956 行 / `gateway/platforms/whatsapp_common.py` 367 行 ✓
  - `tools/delegate_tool.py:567 DEFAULT_CHILD_TIMEOUT: Optional[float] = None` ✓
  - `tools/mcp_tool.py:1208` "Per the MCP spec, `InitializeResult.capabilities.tools` is non-None" 注释 ✓
- **5 个 research agent 分担实证**（每个返回 file:line 报告，主线程汇总）：
  - Agent A（Desktop）：46 commit / d62979a 状态栈 + bbf020e/76b9386 stick-to-bottom + 7c226cc/edc36f3/3cf7d43 perf wave + e906726/dd12a54/0595af0/1a3cd3d 工作区抽取 + 425e777 slash 完成 + 46d758b 半透明 + 6c00077 RTL + 7a2d498/b1fe210/79c3ed3/b6c7ebf profile 路由 + 0a865e5 大粘贴 bypass + 266b5a1 approval expand + 1e25358/9ff0ba0/e3ed772/e96fe06/cc726aa/b097d7b 端口令牌硬化 + 7396977 MCP 工具发现 + 4ddb033 自定义 OpenAI key + 24f74eb/6e41ca9/d221e36/8cf9d86/aa53a78 杂修 + b16e22b/2e874ef/09bcf5a 工具行
  - Agent B（Messaging）：573c4e6/9bfff6e Photon spectrum-ts 3.0→3.1 + 156f4fb/05470aa reactions + 0a963d8 telemetry + 2681c5a/b4e95a2/23305cf/a652131/a23c0b3/52c7976 Photon 杂 + a59d5e3 Telegram 富文本 + 7c0605b Telegram stream 富文本 + 45f9099/276e6cc Matrix + 2a5dc0e/82d5701/62e937b/08e8bed Slack + 448a3f9/5a33176 Discord + 984e6cb WhatsApp Cloud + 31a0100/971cfaa/0cc1a1d Yuanbao + ad7436a WeCom + 8207ae8/1e29ab3/74180eb/f03f161 文档分类
  - Agent C（Gateway/Security/Kanban）：6e2fd95/63097ee auto-resume 去重 + fc46354 own-policy fail-closed + 3380563 /api/status 受门 redact + 5908822/c6a992e/9514ddb API key 链 + 4ead464 chmod 守护 + 5edb346 write-deny .env + 16fb573/202e318/197337c/e256f4a/a942bfd gateway 杂 + b4b118c/351fdcc/024a8e3/1d27be0 JSONL 抄写降级 + 34120a0/7bcdced/88ee58f/7552e0f/7c622b6/cb125c2/1a7bb98 kanban + 6c26727 telegram observe + d948de3 Windows 安装 + ad7436a WeCom 群级 allowlist
  - Agent D（CLI/Profile/Dashboard/Billing）：7ba5df0/c196269 /credits + 28bf8fb/9b5f7b6 任意 source 克隆 + 62b4618/88dbf95 profile-scope + d206e1f/9102d4a/56b79f1/a118b94/afffb8d 仪表板 + 4474873 scrollback 持久 + aa07983/bd66e7e/311ff96 Codex auth 自愈 + 552e9c7 Bitwarden + 9ff98da/6f3a020/a8a05c8/b4afc65/64a9a19 xAI + 697d38a/6a6766f CDP + a0c0312 xAI Web Search + 57145ca BrowseShSource + 2abcae9/343803b/58591d9/e13f242 CLI 杂 + aedb8ac/573b964 update 守护
  - Agent E（Compression/Models/Perf/Misc）：aec3885/434c684/c7bee8f/8905ee6/5d0408d/691ff7c/2fef3e2/68536d4/0db5cb8/749b721/956af7f/7e46533/286ecd2/8e5b759 压缩 wave + 6cb9917/544c31b/6bd4311 perf + 2714fc8/819def4/24c7ce0/32aea11/1c76689 nous/vision + 957a8ff/4fd9397/7d11fa4/5acd185/eed61a1/c8e5f34/bc060c7/3803e5f/2589656/74c5158/643dc82/4b646bc/d9829ab/6a159be/b4afc65/9688c1a 模型 + 2ee69d0/fc7e04e/8f92327/c6a380e/1899c8f/3fde8c1/4373e80/87c6edc skills + 81436e1/55a18e6 approval + e02a7e5/531efe7/5aa4727/4cc1887/18cd1e5 computer-use + 87893fe/15439be memory + ab06ef8/96cc7ee coding + bba9b51 delegation + 9b01c4d/c41a653/6ff39c3/aedb8ac/2a7308b 更新 + f82cb48/1185dfd platform + fa5e98f send + e20e0bd yuanbao + 5affecb MCP + b2d151a $ref + 44bd478/889a136 plugins + be0728c/331cb38 typing + 6724daa/0333a99 analytics + 16fb573/05b9c84/652dd9c 链接
- 验证文件名 / 行号 / 函数名均与 HEAD `0a865e5` 一致；no Unverifiable 项遗留（仅 `544c31b` 的 "3 hot-paths" 由 commit message 未明列 redact.py 具体改动，但 +137/−x 差量已经 stat 核实）。

---

> **下一步**：本同步聚焦 changelog；既有 concept 页面（context-compressor-architecture、security-defense-system、gateway-session-management、messaging-gateway-architecture、credential-pool-and-isolation、skills-system-architecture、multi-agent-architecture）所涉新事实点已通过 changelog 横向覆盖，后续按需细化对应 concept 页面。
