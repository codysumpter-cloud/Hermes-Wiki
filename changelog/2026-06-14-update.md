---
title: 2026-06-14 单日同步 — hermes-agent 0a865e5 → 2a14e8957（83 commits，~24h 单日窗口；SSL CA fail-fast guard + MCP stdio exfil-shape 拦截 + 敏感文件 in-place 写门 + Read 原生抽取 .ipynb/.docx/.xlsx + Desktop 原生 OS 通知 + 完成音效 + 折叠指示器 + 模型 refusal → content_filter 直透 + Codex reasoning thinking-only + Gateway 中断 replay 清理 + 启动 auto-resume 串行 + repaired-turn 持久 + intentional silence token + Profile HERMES_REAL_HOME 隔离 + Skills 跨进程缓存失效 + 全局/平台 disabled 并集 + Telegram rich messages opt-in 还原 + Email SMTP_SSL port 465 + ZAI GLM-5.2 1M + Kimi K2.7 Code + OpenRouter 元数据磁盘缓存 + atomic_replace EXDEV 兜底）
created: 2026-06-14
updated: 2026-06-14
type: changelog
tags: [changelog, update, v0.16.0-maintenance, ssl-ca-bundle-fail-fast-guard, ssl-guard-escape-hatch-env, ssl-guard-doctor-section, ssl-config-error-typed, ssl-guard-macos-fallback, ssl-guard-cabundle-env-vars, mcp-stdio-exfil-shape-block, mcp-stdio-pre-probe-filter, mcp-stdio-shell-interpreters-frozenset, mcp-stdio-egress-pattern, mcp-loop-preserve-on-probe, mcp-event-loop-only-if-idle, approval-cp-mv-install-sensitive-paths, approval-sed-perl-ruby-i-sensitive-files, approval-resolved-home-fold, file-safety-user-write-deny-relax, file-safety-shell-rc-removed, read-tool-extract-notebook-office, read-tool-ipynb-docx-xlsx-stdlib, read-tool-extraction-error-fallback, read-tool-xlsx-row-cap, desktop-native-os-notifications, desktop-notifications-per-type-toggles, desktop-completion-sound-picker, desktop-completion-sound-variants, desktop-notification-action-buttons, desktop-notifications-settings-tsx, desktop-clarify-ux-polish, desktop-compaction-summarizing-indicator, desktop-compaction-scrollback-preserve, desktop-copy-id-context-menu-item, desktop-thinking-shimmer-label-only, desktop-offscreen-approvals-jump-to-bottom, desktop-profile-home-normalize-bootstrap, desktop-spa-utf8-read, agent-refusal-surface-not-retry, agent-content-policy-blocked-builder, agent-content-policy-recovery-hint, transports-refusal-not-sole-payload, transports-content-filter-passthrough, codex-reasoning-thinking-only, codex-reasoning-replay-disable, codex-reasoning-encrypted-content-drop, gateway-interrupted-tool-tail-strip, gateway-auto-continue-noise-strip, gateway-preserve-new-input-on-replay, gateway-startup-auto-resume-serialize, gateway-startup-restore-queue, gateway-repaired-turn-persist, gateway-flushed-db-ids-identity-set, gateway-intentional-silence-tokens, gateway-silent-reply-token-exact-match, gateway-silence-token-docs, gateway-early-duplicate-guard-pid-scope, gateway-supervised-conflict-guard, gateway-replied-to-media-attach, gateway-sudo-profile-system-install, profile-hermes-real-home-env, profile-subprocess-home-policy-explicit, profile-terminal-home-mode-env, profile-resumed-cwd-profile-db, tui-resumed-cwd-profile-helper, skills-cross-process-agent-cache-invalidate, skills-agent-cache-message-count-rebaseline, skills-platform-disabled-prompt-filter, skills-global-platform-disabled-union, skills-raw-config-cache, skills-bundled-update-crash-safe, telegram-rich-messages-opt-in-default-off, telegram-rich-message-fallback-harden, telegram-rich-details-math-crash-guard, telegram-rich-final-duplicate-prevent, email-smtp-ssl-port-465, email-smtp-ipv4-fallback-supported-sockets, model-zai-glm-5-2-1m-context, model-kimi-k27-code-native-picker, openrouter-metadata-persist-disk-cache, openrouter-metadata-cache-ttl, perf-webhook-incremental-prune, perf-process-completion-event-wake, perf-execute-code-stop-event-idle, perf-api-server-linear-normalize, perf-image-shrink-provider-limit, image-error-max-dimension-parse, atomic-replace-exdev-copy-fallback, anthropic-proxy-v1-endpoint, dispatch-session-id-forward, provider-oauth-account-removal, provider-oauth-disconnect-guard, search-keep-partial-on-timeout, search-timeout-marker-strip, messaging-weixin-personal-relabel, mcp-security-py, mcp-supply-chain-action, author-map-3-salvage, doctor-ssl-section, salvage-46095-cross-process-cache, salvage-disabled-skills-union, salvage-refusal-content-filter, salvage-bb-desktop-notifications, salvage-bb-summarize-gui]
sources:
  - hermes-agent/main 0a865e5..2a14e8957
total_commits: "83 个 commit（78 个 non-merge + 5 个 merge；~23.5h 单日窗口；committer dates 2026-06-13T21:35 UTC → 2026-06-14T21:01 UTC）"
---

# 2026-06-14 单日同步：hermes-agent main `0a865e5 → 2a14e8957`

> **基线**：wiki `fd5a12c` 对齐 hermes `0a865e5948cb836eba93620e365703ec38cf76e3`（2026-06-13T20:49 UTC，"fix(desktop): bypass Chromium editing pipeline for large paste & select-delete (#45812)"，v0.16.0 维护未发新版）
> **目标**：hermes-agent/main HEAD `2a14e8957d7552631b4e33611a828e63f773449a`（2026-06-14T21:01 UTC，"fix(kimi): surface K2.7 Code in native picker (#46309)"）
> **跨度**：约 23.5 小时单日窗口（committer dates `2026-06-13T21:35 UTC → 2026-06-14T21:01 UTC`）
> **版本**：仍 **v0.16.0**（`pyproject.toml:10 version = "0.16.0"` + `hermes_cli/__init__.py:17-18 __version__ = "0.16.0"` + `__release_date__ = "2026.6.5"` 不变；本期全为 fix / perf / feat / test / chore / refactor / docs / style，未发新版）
> **commit 类型分布**：**54 fix / 6 perf / 6 feat / 5 test / 3 chore / 1 refactor / 1 docs / 1 style / 1 unscoped / 5 merge**
> **diff 体量**：166 文件改动，**+8907 / −692** 行
> **验证基线**：`/tmp/hermes-agent` clone @ `2a14e8957` 全 checkout，所有 commit hash / file:line / 路径 / 函数名 / 行数经 `grep -n` / `git show <sha>` / `Read` 实证；4 个并行 research agent 分主题分担实证（Security / Desktop+Read / Agent+Gateway+Codex / Skills+Models+Perf+Misc）

---

## 章节索引

1. [SSL CA bundle fail-fast guard（agent + doctor 6 commit 串）](#1-ssl-ca-bundle-fail-fast-guard)
2. [MCP stdio config exfil-shape 拦截 + loop 守 idle 才停（3 commit）](#2-mcp-stdio-security)
3. [Approval — 敏感文件 cp/mv/install + in-place sed/perl/ruby 写门 + 绝对 HOME 折叠 + user-write deny 松绑（4 commit）](#3-approval-and-file-safety)
4. [Read 工具 — 原生抽取 `.ipynb` / `.docx` / `.xlsx`（stdlib-only）](#4-read-tool-document-extraction)
5. [Desktop — 原生 OS 通知 + 完成音效 + 通知设置面板（3 commit merge）](#5-desktop-notifications)
6. [Desktop — Clarify UX + Compaction 汇总指示 + 滚屏保活 + Copy ID 勾 + 思考 shimmer 收敛 + 离屏审批跳底（5 commit merge + 4 misc）](#6-desktop-polish)
7. [Agent — 模型 refusal 不重试改直透 content_filter + content_policy_blocked builder + 恢复 hint（4 commit merge）](#7-agent-refusal-handling)
8. [Codex — Reasoning 项 thinking-only 收敛 + replay 路径绑死 Codex（2 commit）](#8-codex-reasoning)
9. [Gateway — 中断 replay 清理 + 启动 auto-resume 串行 + repaired-turn 持久 + intentional silence token + supervised guard +回复媒体附件（8 commit）](#9-gateway-core)
10. [Profile / HOME 隔离 — `HERMES_REAL_HOME` 子进程 env + `TERMINAL_HOME_MODE` 显式策略 + 恢复 cwd 落 profile DB（5 commit）](#10-profile-home-isolation)
11. [Skills — Agent 缓存跨进程失效 + message_count 每轮 rebase + 全局/平台 disabled 并集 + raw config 缓存 + bundled-update 备份崩溃安全（7 commit）](#11-skills-cluster)
12. [Telegram — Rich Messages opt-in 还原（default off）+ math crash 守 + 重复预览避 + 端点 404 退路硬化（4 commit）](#12-telegram-rich-messages)
13. [Email — SMTP_SSL port 465 + IPv4 兜底 socket（2 commit）](#13-email-smtp)
14. [Models — ZAI GLM-5.2 1M 上下文 + Kimi K2.7 Code 原生 picker（2 commit）](#14-models)
15. [Performance — OpenRouter 元数据磁盘缓存 + Webhook 增量 prune + Process 完成事件唤醒 + execute_code idle 停 + api-server 线性化 + 图像按 provider 限制缩放（6 commit）](#15-performance-wave)
16. [Misc — atomic_replace 跨设备 EXDEV 兜底 + Anthropic proxy `/v1` + dispatch session_id + Provider 账号移除 + Search 超时保留部分 + Weixin 个人账号 relabel + Author maps + UTF-8 SPA（9 commit）](#16-misc)

---

## 1. SSL CA bundle fail-fast guard

**6 个 commit（PR #37026 salvage）**：`a218a0f15` → `b42c5bf65` → `af5b52647` → `dc90ca4e1` → `7aaae7acd` → `73d135774`

### 1.1 上线 `a218a0f15` fix(agent,gateway,doctor): add SSL CA cert bundle fail-fast guard

**触发场景**：部分 `hermes update` 中断后 certifi CA bundle 半残；首次出站 HTTPS 请求拿到 raw traceback，gateway 永久卡在 retry 循环。

**新增文件**：
- `/tmp/hermes-agent/agent/errors.py` —— 类型化的 `SSLConfigurationError`。
- `/tmp/hermes-agent/agent/ssl_guard.py` —— pre-flight 校验 CA bundle 存在、size>0、能 build SSLContext；macOS 在 bundle 为空但系统信任存在时回退（覆盖企业代理 / MDM）。

**Doctor section**：`hermes_cli/doctor.py:316` 新 "SSL / CA Certificates" 节调用 `verify_ca_bundle_with_fallback()`。

**Escape hatch**：`HERMES_SKIP_SSL_GUARD=1`。

### 1.2 至 1.4 调试与硬化 `b42c5bf65 / af5b52647 / dc90ca4e1`

`b42c5bf65 test(ssl_guard): fix macOS fallback test that passed for the wrong reason`：mock 只盖 fallback 调用（无 cafile），让真 SSL stack 拒坏 PEM；并把坏 PEM padding 过 1KB size 门防短路。

`af5b52647 fix(ssl): validate CA bundle paths before provider calls`：重写 `agent/ssl_guard.py`（+/-116 行）；下沉到 provider 调用前。**关键常量与函数**：
- `agent/ssl_guard.py:14` `from agent.errors import SSLConfigurationError`。
- `agent/ssl_guard.py:18-23` 检查 4 个 env：`HERMES_CA_BUNDLE / SSL_CERT_FILE / REQUESTS_CA_BUNDLE / CURL_CA_BUNDLE`。
- `agent/ssl_guard.py:28` `_skip_ssl_guard_enabled()` 读 `HERMES_SKIP_SSL_GUARD`，识别 `_SKIP_VALUES = {"1","true","yes","on"}`。
- `agent/ssl_guard.py:32` `_repair_hint()` 出错时给修复提示。
- `agent/ssl_guard.py:40` `_ssl_err(message)` 包成 `SSLConfigurationError(f"{message}\n{_repair_hint()}")`。
- `agent/ssl_guard.py:45` `_validate_bundle_path()` 校验路径存在、is_file、`require_substantial` 1024 byte 最小；build `SSLContext` 并断言 `get_ca_certs()` 非空。
- `agent/ssl_guard.py:61` `verify_ca_bundle()` 主入口。
- `agent/ssl_guard.py:87` `verify_ca_bundle_with_fallback()` 向后兼容包装。

并增加 `certifi` 为显式依赖（`pyproject.toml`）。

`dc90ca4e1 fix(ssl): run CA guard during agent initialization`：调用点从 `gateway/run.py` / `run_agent.py` 上移到 `agent/agent_init.py:903-905` —— `_create_openai_client` setup 内调 `verify_ca_bundle_with_fallback()`。

### 1.5 至 1.6 文档与导入顺序 `7aaae7acd / 73d135774`

`7aaae7acd` 对齐 guard 文档与 escape hatch；保留 `verify_ca_bundle_with_fallback()` 名（实际无平台 fallback 差异，仅向后兼容包装），更新 `docs/rca-ssl-cacert-post-git-pull.md`。

`73d135774 style(agent): keep run_agent import order stable`：`run_agent.py:34-40` 把 `import logging; logger = logging.getLogger(__name__)` 塞回字母序 stdlib 块（`json` 与 `os` 之间）。+2/-3 行。

> 参见 [[security-defense-system]] —— SSL guard 是新增的第 N 层启动期防线，与现有 file-safety / approval / shell-escape denylist 互补。

---

## 2. MCP stdio config exfil-shape 拦截 + loop 守 idle 才停

**3 个 commit**：`972a9885e` `a27d7e68c` `4936a49a0`

### 2.1 `972a9885e` fix(mcp): block exfil-shaped stdio server configs (#46083)

**新模块**：`/tmp/hermes-agent/hermes_cli/mcp_security.py` 96 行。

**模式匹配（白名单 + 黑名单组合）**：
- `mcp_security.py:15` `_SHELL_INTERPRETERS` frozenset：`bash, sh, zsh, dash, fish, cmd, cmd.exe, powershell, powershell.exe, pwsh, pwsh.exe`。
- `mcp_security.py:29` `_EGRESS_PATTERN`：`curl|wget|nc|ncat|socat|/dev/tcp/|Invoke-WebRequest|Invoke-RestMethod|System.Net.WebClient`。
- `mcp_security.py:38` `_EXFIL_HINT_PATTERN`：`.env | --data-binary | --data-raw | -X POST | POST | <file`。
- `mcp_security.py:64` `validate_mcp_server_entry(name, entry)` 返回 issue list。判定**双门**：command basename 是 shell 解释器 **AND** args 含 egress 工具时才算可疑；hint pattern 命中追加 "and exfiltration-shaped arguments" 后缀。

**接入**：`hermes_cli/mcp_config.py:_save_mcp_server` 与 `tools/mcp_tool.py:_load_mcp_config` 过滤可疑项（`logger.warning("Skipping suspicious MCP server '%s': %s", ...)`）。同步加 doctor 节、GitHub Action `.github/workflows/mcp-supply-chain.yml`、131 行测试 `tests/hermes_cli/test_mcp_security.py`。

### 2.2 `a27d7e68c` fix(mcp): block suspicious stdio configs before probe (#46112)

`tools/mcp_tool.py` +33/-20：抽出 `_filter_suspicious_mcp_servers()` 到 `:2674`，从 `_load_mcp_config` **和** `register_mcp_servers` 双路径调用（line ≈3681 —— 任何 stdio spawn **前**），并补 probe-time 校验 `hermes_cli/mcp_config.py` +10。

### 2.3 `4936a49a0` fix(mcp): preserve loop during probes

**问题**：dashboard / CLI probe 与 live agent tool 共享进程级 MCP event loop；probe finally 块 unconditional `_stop_mcp_loop()` 会让 agent 报 "MCP event loop is not running"。

**修复**：
- `tools/mcp_tool.py:3949` 改名 `_stop_mcp_loop_if_idle()` 包装。
- `:~4087` 新 helper：`return _stop_mcp_loop(only_if_idle=True)`。
- `:~4097` `_stop_mcp_loop()` 加 `only_if_idle` kwarg；当 True 时检查 `_servers` 或 `_server_connecting` 非空就 bail，log "Leaving MCP event loop running; active servers are registered or connecting"。
- `hermes_cli/mcp_config.py:_probe_single_server` finally 改 `_stop_mcp_loop_if_idle()`。

> 参见 [[mcp-and-plugins]] —— stdio security gate 与 capability-gate（上期 `5affecb #44550`）是 MCP 接入层的两道独立闸；loop preservation 让 dashboard probe 与 live agent 共存。

---

## 3. Approval — 敏感文件 cp/mv/install + in-place sed/perl/ruby 写门 + 绝对 HOME 折叠 + user-write deny 松绑

**4 个 commit**：`da28d5d11` `abd69b811` `2b67e96ae` `81e42335a`

### 3.1 `da28d5d11` fix(security): gate cp/mv/install into ~/.ssh, credential, and shell-rc files

`tools/approval.py:455` 新 `DANGEROUS_PATTERNS` 条目，正则：

```
\b(cp|mv|install)\b.*\s["\']?{_SENSITIVE_WRITE_TARGET}[^\s"\']*["\']?{_COMMAND_TAIL}
```

锚定到命令尾，所以**从**敏感路径**读出**的 `cp`/`mv` 不会触发；测试覆盖 5 个正例（`authorized_keys` / SSH 私钥 mv / `.netrc` install / `.bashrc` / `~/.hermes/config.yaml`）+ 2 个负例（从 SSH 复制出去、无关 copy）。

### 3.2 `abd69b811` fix(approval): detect absolute home shell rc writes

`tools/approval.py:577` 新 `_rewrite_resolved_user_home()`：在 detect 阶段把已解析的绝对 HOME（及 symlink 解析后形式）折回 `~/`，使 `/home/alice/.bashrc` 与 `~/.bashrc` 同样命中。Wired in `_normalize_command_for_detection` at `:561-565`。HOME 异常防御：要求绝对路径 + `/`-count ≥ 2。

### 3.3 `2b67e96ae` fix(approval): gate in-place edits to sensitive user files

`tools/approval.py:208` 新 `_USER_SENSITIVE_WRITE_TARGET` 正则 = `_SSH_SENSITIVE_PATH | _SHELL_RC_FILES | _CREDENTIAL_FILES`（不含 Hermes 路径 —— 那块有专门规则）。新 DANGEROUS_PATTERNS 三条：`sed -i …`、`sed --in-place …`、`perl|ruby -i …`。测试 +36 行。

### 3.4 `81e42335a` fix(file-safety): relax user-write deny policy (#45947)

**反方向调整**：`agent/file_safety.py:49-53` 从 `build_write_denied_paths` 删 `.bashrc / .zshrc / .profile / .bash_profile / .zprofile`，并删 `is_write_denied` 内 `control_file_names = ("auth.json", "config.yaml", "webhook_subscriptions.json")` 块。HEAD 状态 `is_write_denied` 只保 `mcp-tokens/` 与 `pairing/` 子目录守护（`agent/file_safety.py:113-125`）。

**仍硬阻**：SSH keys、`.env`、`.anthropic_oauth.json`、`.netrc / .pgpass / .npmrc / .pypirc / .git-credentials`、`/etc/sudoers|passwd|shadow`。

**整体效果**：把"无脑全 deny" 收紧为"明确危险 deny + 显眼操作走 approval"。Approval-gate（3.1-3.3）补足了 shell rc / SSH keys / credential 的写入路径。

> 参见 [[security-defense-system]] —— approval 与 file_safety 的分工：**file_safety 是直接 deny**（无 prompt），**approval 是 prompt + 用户决策**。两者从硬封禁→分级共存。

---

## 4. Read 工具 — 原生抽取 `.ipynb` / `.docx` / `.xlsx`

**1 个 commit（重大新功能）**：`817f39231 feat(read): extract notebook and office documents (#37082)`

### 4.1 新模块 `tools/read_extract.py` 248 行 stdlib-only

**关键点**：**完全不依赖外部库**（没有 python-docx / openpyxl）；只用 stdlib 的 `json`、`zipfile`、`xml.etree.ElementTree`（`tools/read_extract.py:9-13`）。`pyproject.toml` **没有**新依赖。

**入口**：
- `tools/read_extract.py:18` `EXTRACTABLE_EXTENSIONS = frozenset({".ipynb", ".docx", ".xlsx"})`。
- `tools/read_extract.py:38-46` `extract_document_text(path)`、`is_extractable_document(path)`。
- 分发 `_extract_notebook` / `_extract_docx` / `_extract_xlsx`。

**xlsx 资源限制**（`tools/read_extract.py:19-21`）：
- `MAX_XLSX_BYTES = 50 * 1024 * 1024`（50 MB）
- `_MAX_XLSX_ROWS_PER_SHEET = 5000`
- `_MAX_XLSX_COLS = 256`

### 4.2 集成到 `read_file` 工具 `tools/file_tools.py:763-808`

在二进制 guard **前**插一段抽取分支：调 `extract_document_text()`，命中就返回 JSON `{extracted_document: True, ...}`；malformed → 抛 `ExtractionError`，外层 try 兜底回退到普通二进制处理。

工具 schema 在 `tools/file_tools.py:1476` 公告 `.ipynb / .docx / .xlsx` 自动抽取。

测试 `tests/tools/test_read_extract.py` 295 行新文件。

> 参见 [[tool-registry-architecture]]、[[parallel-tool-execution]] —— Read 工具的新抽取分支不需要注册新工具，是 `read_file` 内的前置 dispatch。

---

## 5. Desktop — 原生 OS 通知 + 完成音效 + 通知设置面板

**3 个 commit（合并 PR #45866 `cdf30a7ac`）**：`b4ba3f5e3` `630a4ef03` `b0288ae9b`

### 5.1 `b4ba3f5e3` feat(desktop): add curated completion cue for agent turn completion (#42480)

**音效库**：`apps/desktop/src/lib/completion-sound.ts` **519 行**，**14 个 Web Audio 合成预设**（不放音频文件资产）。

**状态源**：`apps/desktop/src/store/completion-sound.ts` —— `$completionSoundVariantId` + `resolveCompletionSoundVariantId`。

**触发点**：`apps/desktop/src/app/session/hooks/use-message-stream.ts` 在每个 `message.complete` 上 fire。

**初始 picker**：`apps/desktop/src/app/settings/appearance-settings.tsx`（5.3 中迁走）。

### 5.2 `630a4ef03` feat(desktop): native OS notifications with per-type toggles

**通知引擎**：`apps/desktop/src/store/native-notifications.ts` **203 行** 新文件。

**5 种类型 + 节流**：
- `approval`（审批请求）
- `input`（clarify / sudo / secret.request）
- `turnDone`（轮完成）
- `turnError`（轮错误）
- `backgroundDone`（后台任务完成）
- 1s 节流（每 `(kind, session)` 对）+ localStorage 偏好。

**门控**："backgrounded" = `document.hidden || !document.hasFocus()`。

**设置 UI**：`apps/desktop/src/app/settings/notifications-settings.tsx` 新 98 行 —— 主开关 + 每类型切换 + 测试按钮。

**IPC**：`apps/desktop/electron/main.cjs:5611-5635` 扩展 `hermes:notify` handler，加 action 按钮（`Approve` / `Reject`）、点击 emit `hermes:focus-session`、action emit `hermes:notification-action`。preload `apps/desktop/electron/preload.cjs` +10 行。

**触发点**：`apps/desktop/src/app/session/hooks/use-message-stream.ts`（`message.complete`、`error`、`approval` / `clarify` / `sudo` / `secret.request`）+ composer-status `running→exited` 转移。

**i18n**：`settings.notifications` keys 加 en/ja/zh/zh-hant。

### 5.3 `b0288ae9b` feat(desktop): move completion-sound picker into Notifications settings

把 picker 从 `apps/desktop/src/app/settings/appearance-settings.tsx`（-55 行）搬到 `notifications-settings.tsx`（+54 行）。i18n keys 从 `settings.appearance.*` 迁到 `settings.notifications.*`。

---

## 6. Desktop — Clarify UX + Compaction 汇总指示 + 滚屏保活 + Copy ID 勾 + 思考 shimmer 收敛 + 离屏审批跳底

**5 个 commit 合并 PR #46029 `526a1e24b`** 加 **4 个 misc commit**。

### 6.1 `9cbb91abd` fix(desktop): clarify UX — loading, enter-to-send, radio align (#46014)

只动 `apps/desktop/src/components/assistant-ui/clarify-tool.tsx`（+51/-28）：
- 新 `CLARIFY_SHELL_CLASS` 常量。
- OPTION_ROW_CLASS 由 `items-center` → `items-start`。
- 自由文本答按主 composer 的 enter-to-send 契约。
- `clarify.request` 到达前居中显示 `Loader2`。

### 6.2 `715b69172` fix(desktop): show summarizing indicator during auto-compaction

**Agent → Gateway → Desktop 三段链**：
- `agent/conversation_compression.py:43-49` 新 `COMPACTION_STATUS_MARKER = "Compacting context"`，由 `agent._emit_status` emit。
- Gateway 接收侧：`tui_gateway/server.py` `_status_update` 识别 marker → `kind="compacting"`。
- Desktop store：`apps/desktop/src/store/compaction.ts` 新文件，`$compactingSessions`、`setSessionCompacting`。
- 表层：`apps/desktop/src/components/assistant-ui/thread.tsx` shimmering "Summarizing thread"。

### 6.3 `49dd91d68` fix(desktop): show copied checkmark on session Copy ID (#46030)

`apps/desktop/src/components/ui/copy-button.tsx:13` 新 `'context-menu-item'` appearance 变体加到 `CopyButtonAppearance` 类型；按 appearance 渲染 `ContextMenuItem` vs `DropdownMenuItem`。`apps/desktop/src/app/chat/sidebar/session-actions-menu.tsx` 走 `CopyButton`。

### 6.4 `1eb13744b` fix(desktop): polish compaction indicator and preserve scrollback

`compaction.ts` 类型简化 `Record<string, string>` → `Record<string, true>`；`$compactionStatus` 改名 `$compactionActive`；`setSessionCompacting(sessionId, active: boolean)`。

`thread.tsx` 显示 shimmering "Summarizing thread" label。**关键**：`use-message-stream.ts` 检测到 compaction 触发后**跳过 post-turn hydrate**（保 scrollback）。

### 6.5 misc 4 个 commit

- **`08d89e7ab` fix(desktop): limit thinking shimmer to the disclosure label (#46197)**：`apps/desktop/src/components/assistant-ui/thread.tsx:588-594`（`ReasoningTextPart`）从 `containerClassName` 删 `shimmer text-muted-foreground/55`，只留 `text-xs leading-snug text-muted-foreground/85`。-3 行。
- **`6b76284c7` fix(desktop): surface off-screen approvals via the jump-to-bottom control (#45853)**：`apps/desktop/src/app/chat/scroll-to-bottom-button.tsx` +26 行 —— pill 浮现 "Approval needed"，复用 `requestScrollToBottom()`（use-stick-to-bottom 路径），避免 `el.scrollIntoView()` 弄乱布局。新 67 行测试 `scroll-to-bottom-button.test.tsx`。i18n 加 en/ja/zh/zh-hant。
- **`0428945b5` fix(desktop): keep profile homes out of bootstrap (#46073)**：`apps/desktop/electron/backend-env.cjs:70-78` 新 `normalizeHermesHomeRoot(hermesHome)` 剥尾 `/profiles/<name>`，让 backend bootstrap 用根 Hermes home 而非 profile 子目录。`backend-env.cjs:110` 导出；`apps/desktop/electron/main.cjs` 两处消费。测试 `backend-env.test.cjs` +16 行。`hermes_cli/setup.py` +8/-3、`tests/hermes_cli/test_telegram_managed_bot.py` 联动。
- **`89bdb1e54` fix: read dashboard spa assets as utf-8**：`hermes_cli/web_server.py:10659` 与 `:10709` 两处 `read_text()` 都加 `encoding="utf-8"`。测试 `tests/hermes_cli/test_web_server.py` +39 行。

> 参见 [[messaging-gateway-architecture]]、[[cli-architecture]] —— Desktop 通知与 5.x 系列一起把"用户离开窗口"的回填全部走 native channel。

---

## 7. Agent — 模型 refusal 不重试改直透 content_filter + content_policy_blocked builder + 恢复 hint

**4 个 commit 合并 PR #46013 `c8ad2ca99`**：`bb46bf8ce` `ab26541b9` `12c84d6c7` `10bd01972`

### 7.1 `bb46bf8ce` fix(agent): surface model refusals instead of retrying them as errors

**Anthropic transport** `agent/transports/anthropic.py`：`validate_response` 允许 `stop_reason == "refusal"` 时 content 为空，让流通过 `normalize_response` 映射 refusal → content_filter。

**Chat Completions transport** `agent/transports/chat_completions.py:679-699`：promote `message.refusal` 到 content + `finish_reason = "content_filter"`。

**Loop 处理**：`agent/conversation_loop.py` 在 `finish_reason == "content_filter"` 时 fire `api_request_error` hook、尝试 fallback 一次，否则终止 refusal。**不再无脑重试**。

### 7.2 `ab26541b9` test(transports): lock in content_filter passthrough for OpenRouter

只有 regression test，加到 `tests/agent/transports/test_chat_completions.py`。

### 7.3 `12c84d6c7` fix(transports): only treat a refusal as terminal when it is the sole payload

`agent/transports/chat_completions.py:684-699`：只在 `not _has_text and not _has_tool_calls` 时 promote 为终止 `content_filter`；说明文本始终记 `provider_data["refusal"]`。

### 7.4 `10bd01972` refactor(agent): share the content_policy_blocked result builder + recovery hint

**共享 builder**：
- `agent/conversation_loop.py:411-433` `_content_policy_blocked_result()` 返回 `{final_response, messages, api_calls, completed:False, failed:True, error:"content_policy_blocked: <detail>"}`。
- `agent/conversation_loop.py:405-408` `_CONTENT_POLICY_RECOVERY_HINT` 常量，文本：`"Try rephrasing the request, narrowing the context, or adding a fallback provider with \`hermes fallback add\`."`。
- 原因通过 `FailoverReason.content_policy_blocked.value`（`agent/conversation_loop.py:1428`）。

> 参见 [[agent-loop-and-prompt-assembly]]、[[interrupt-and-fault-tolerance]] —— refusal 从 retryable error 变成"终止 + 提示"，是 reliability 模式的一次重要分类细化。

---

## 8. Codex — Reasoning 项 thinking-only 收敛 + replay 路径绑死 Codex

**2 个 commit**：`069bfd654` `1d584a301`

### 8.1 `069bfd654` fix(agent): keep Codex reasoning replay on Codex path

`run_agent.py:905-936` 新方法 `AIAgent._disable_codex_reasoning_replay()`：从每条 assistant message 弹出 `codex_reasoning_items`，并设 `self._codex_reasoning_replay_enabled = False`。被 `codex_responses_adapter._chat_messages_to_responses_input` 与 `transports/codex.py` 消费，drop `reasoning.encrypted_content`。

### 8.2 `1d584a301` fix(agent): treat Codex reasoning items as thinking-only

`run_agent.py:3278-3343` `_is_thinking_only_assistant()` 加 Codex Responses encrypted-reasoning 识别（`:3337-3342`）经新 kwarg `drop_codex_reasoning_items`。**只把 `item.get("type") == "reasoning"` 的列表项**当 thinking-only；空 / junk list 落回常规判定。

> 参见 [[provider-transport-architecture]]、[[context-compressor-architecture]] —— Codex reasoning replay 之前混在 thinking detection 里时常误判，本 cluster 把两个相邻语义切干净。

---

## 9. Gateway 核心 — 中断 replay 清理 + 启动 auto-resume 串行 + repaired-turn 持久 + intentional silence token + supervised guard + 回复媒体附件

**8 个 commit**：`5191c1c2c` `2c174bce2` `10bad2faf` `2b4873f7f` `293c04fef` `0e22bf643` `7433d5f0e` `143679305` `efbe1635d`

### 9.1 中断 replay 清理（2 commit）

**`5191c1c2c` fix(gateway): stop replaying interrupted tool-call tails and auto-continue notes**：
- `gateway/run.py:767-776` `_is_interrupted_tool_result()` 识 `"[command interrupted]"` 或 exit_code `130/-1` + `"interrupt"`。
- `gateway/run.py:779-822` `_strip_interrupted_tool_tails()` 删被中断的 assistant→tool 段。
- `gateway/run.py:825-837` 常量 `_AUTO_CONTINUE_NOTE_PREFIX = "[System note: Your previous turn"`、`_AUTO_CONTINUE_FALLBACK_PREFIX = "[System note: A new message"`，helper `_is_auto_continue_noise()`。
- 由 `_build_gateway_agent_history`（`:633`）调用。

**`2c174bce2` fix(gateway): preserve new input on interrupted replay cleanup**：重构 `_build_gateway_agent_history`（`:633`）使清扫中断尾时**保留新用户输入**；新 `_strip_auto_continue_noise()`（`gateway/run.py:840-854`）剥用户 message 内的 prefix。

### 9.2 启动 auto-resume 串行 `10bad2faf` fix(gateway): serialize startup auto-resume before inbound (#46074)

`gateway/run.py:4642-4699` 新 `_queue_startup_restore_event()` / `_drain_startup_restore_queue()` / `_finish_startup_restore()`。`_startup_restore_in_progress` 期间 inbound 入 `_startup_restore_queue`；待 `_startup_restore_tasks` 完成后 drain。`_schedule_resume_pending_sessions()` 在 `:4701`（与上期 `_AGENT_PENDING_SENTINEL` 防御共栈）。

### 9.3 Repaired-turn 持久 `2b4873f7f` fix(agent): persist repaired-turn responses (#46071)

**根因**：上一版 `_flush_messages_to_session_db` 用位置切片 `len(conversation_history)`；`repair_message_sequence` 缩短历史时新尾被丢。

**修复**：`run_agent.py:1548-1641` `AIAgent._flush_messages_to_session_db()` 改身份追踪 —— `_flushed_db_message_ids`（`id(msg)` 的 set）+ `_flushed_db_message_session_id`。修复 #46053。

### 9.4 Intentional silence tokens（2 commit）

**`293c04fef` fix(gateway): suppress exact silence tokens without mutating history**：
- `gateway/response_filters.py:13` `SILENT_REPLY_TOKEN = "NO_REPLY"`。
- `gateway/response_filters.py:18-23` `LIVE_GATEWAY_SILENT_MARKERS = frozenset({"[SILENT]", "SILENT", "NO_REPLY", "NO REPLY"})`。
- `gateway/response_filters.py:30-44` `is_intentional_silence_response()` 要求精确匹配（≤64 字符，经 `_canonical_silence_candidate` 规范化）。
- `gateway/response_filters.py:47-53` `is_intentional_silence_agent_result()` 排除失败轮。

**`0e22bf643` docs(gateway): document exact silence tokens (#46105)**：`website/docs/user-guide/messaging/index.md:109-131` 新"Intentional Silence Tokens"节，列出 4 个 token，说明**全响应等于 token** 才静默 + 静默轮仍留 transcript。

### 9.5 Scoping 与 supervisor 守护（2 commit）

**`7433d5f0e` fix(gateway): scope early duplicate guard to pid file**：`hermes_cli/gateway.py:3861-3880` `_guard_existing_gateway_process_conflict()` 改用 `from gateway.status import get_running_pid`（PID 文件）而非扫所有 gateway 进程（`get_gateway_runtime_snapshot()`）—— 跨 HERMES_HOME 不再误报。

**`143679305` fix(gateway): block shell gateway run when a service supervises the profile**：
- `hermes_cli/gateway.py:3798-3818` `_running_under_gateway_supervisor()` 检 `INVOCATION_ID`（systemd）、`HERMES_S6_SUPERVISED_CHILD`（s6）、`XPC_SERVICE_NAME != "0"`（launchd）。
- `hermes_cli/gateway.py:3821-3858` `_guard_supervised_gateway_conflict()`：当 `snapshot.service_installed and snapshot.service_running` 且**未**在 supervisor 下时 exit；`--force` 旁路。

### 9.6 回复消息媒体附件 `efbe1635d` fix(gateway): include replied-to media attachments (#46107)

`gateway/platforms/telegram.py:5563-5606` `TelegramAdapter._cache_replied_media()`：从 `msg.reply_to_message` 下载媒体、`cache_media_bytes` 缓存、追加到 `event.media_urls` / `media_types`、首项设 `event.message_type`、文本附 `[Replied-to <kind> '<name>' saved at: <path>]`。在 text / command handler 两处 wired（`:5796`、`:5811`）。`plugins/platforms/discord/adapter.py` 同步修。

> 参见 [[gateway-session-management]]、[[interrupt-and-fault-tolerance]] —— 9.x 这一组把"中断遗留 → 后续轮 replay 误抓 / 启动早抢 / 静默 token 蒙混 / shell vs service 竞争"四类竞争收敛。

---

## 10. Profile / HOME 隔离 — `HERMES_REAL_HOME` + `TERMINAL_HOME_MODE` 显式策略 + 恢复 cwd 落 profile DB

**5 个 commit**：`b00060ce5` `723c2331b` `d842155da` `9f33d673e` `1f5eef809`

### 10.1 子进程 env：`HERMES_REAL_HOME` `b00060ce5`

`hermes_constants.py` 引入 `get_real_home()` helper；`tools/code_execution_tool.py`、`tools/environments/local.py`、`agent/copilot_acp_client.py` 注入 `HERMES_REAL_HOME`。

### 10.2 显式 HOME 策略 `723c2331b` fix: make profile subprocess HOME policy explicit

`hermes_constants.py`：
- `:311-339` `_iter_real_home_candidates()`。
- `:342-359` `get_real_home()` 返回 OS 用户 home，避开 profile home。
- `:362-395` `get_subprocess_home()` 看 `TERMINAL_HOME_MODE` env：
  - `auto`（默认）
  - `real`（aliases：`host`、`user`、`real_home`）
  - `profile`（aliases：`isolated`、`profile_home`）—— 选 `{HERMES_HOME}/home`。
- `:398-405` `apply_subprocess_home_env()` 在 env dict 中设 `HERMES_REAL_HOME` 与 `HOME`。

### 10.3 恢复 cwd 入 profile DB `d842155da Keep resumed profile cwd scoped to profile DB`

`tui_gateway/server.py:3469-3481` 改用传入的 `session_db`（或 `_get_db()`），通过 `db.update_session_cwd(key, ...)` 持久化。Profile resume cwd 解析点 `:4082, :4133, :4288`，经 `_profile_configured_cwd()`（`tui_gateway/server.py:700`）。

### 10.4 `9f33d673e` fix(tui): persist resumed profile cwd updates to profile db

`tui_gateway/server.py:1222-1236` `_set_session_cwd()` 改用 `with _session_db(session) as db:`（profile-scoped helper `:1195`），不再 `_get_db()`，确保 profile-bound cwd 更新落在正确的 DB。

### 10.5 `1f5eef809` test(tui): tolerate resume init kwargs in protocol tests

只有 `tests/tui_gateway/test_protocol.py`（2 行）。

> 参见 [[configuration-and-profiles]]、[[credential-pool-and-isolation]] —— `HERMES_REAL_HOME` + `TERMINAL_HOME_MODE` 把"shell 子进程 HOME 该指向哪"从隐含变成显式配置矩阵。

---

## 11. Skills — Agent 缓存跨进程失效 + message_count 每轮 rebase + 全局/平台 disabled 并集 + raw config 缓存 + bundled-update 备份崩溃安全

**7 个 commit**（含 2 个 merge：`6c34088a1` PR #46237、`fc2b8b3d3` PR #46236）：`7f245b003` `3bc4a2ff7` `7bbe7024c` `ce19fdb7c` `4e6d05c6a` `3581131e7` `9a2b97632`

### 11.1 跨进程缓存失效 `7f245b003` fix(gateway): invalidate agent cache on cross-process session writes (#45966)

`gateway/run.py:14376-14414`：dispatch 时对比缓存 `message_count`（`cached[2]`）与 on-disk `_sess_row["message_count"]`；不同则 log "Agent cache invalidated for session ... message_count changed (X -> Y), possible cross-process write"，evict `self._agent_cache.pop(session_key, None)` 后 `_cleanup_agent_resources()`。缓存条目 3-tuple `(agent, _sig, _current_msg_count)` 存于 `gateway/run.py:14463`。

### 11.2 message_count 每轮 rebase `3bc4a2ff7` fix(gateway): re-baseline agent-cache message_count after each turn

`gateway/run.py:12801-12850` `_refresh_agent_cache_message_count(self, session_key, session_id)`：本轮写入 user / assistant 行**之后**，重 snapshot `session_db.get_session(session_id)["message_count"]`，更新 `_cache[session_key] = (cached[0], cached[1], _live)`。fail-safe：缺 DB、falsy session_id、非 3-tuple、`_AGENT_PENDING_SENTINEL` 均跳。调用点 `:8888`。

### 11.3 平台-disabled 过 `<available_skills>` `7bbe7024c` fix: filter platform-disabled skills from <available_skills> prompt (#46201)

`agent/skill_utils.py:318` `get_disabled_skill_names(platform: str | None = None)` 现返回**全局并集平台**两个 list 的并；`agent/prompt_builder.py:2` 透传 `_platform_hint`。

### 11.4 disabled 并集到所有解析点 `ce19fdb7c` fix(skills): apply global|platform disabled union to all resolution sites

- `hermes_cli/skills_config.py:27-42` `get_disabled_skills(config, platform) = global_disabled | set(platform_disabled)`。
- `tools/skills_tool.py:573-594` `_is_skill_disabled(name, platform)` 返回 `name in platform_disabled or name in global_disabled`。

### 11.5 Raw config 缓存 `4e6d05c6a` perf(skills): share raw config cache in skill utils (#46149)

- `agent/skill_utils.py:275` `_RAW_CONFIG_CACHE: Dict[Tuple[str, int, int], Dict[str, Any]] = {}`（key：path, mtime_ns, st_size）。
- `:283-315` `_load_raw_config()` mtime + size keyed；`:278` `_raw_config_cache_clear()` 测试 hook。

### 11.6 Bundled-update 备份崩溃安全（2 commit）

**`3581131e7` fix(skills): make bundled-update backup handling crash-safe and idempotent**：
- `tools/skills_sync.py:509-525` —— 在 manifest 分类**前**做孤儿 `.bak` 复原。
- `tools/skills_sync.py:590-595` —— `shutil.move(dest, backup)` **前**清陈旧 `.bak`。
- `tools/skills_sync.py:608-625` —— restore-from-backup **前**清部分 `dest`。

**`9a2b97632` test(skills): add regression tests for bundled-update backup recovery**：`tests/tools/test_skills_sync.py` +121 行。

> 参见 [[skills-system-architecture]]、[[memory-system-architecture]] —— Skills 在 v0.16 后端越来越多走"跨进程共享 + 平台过滤 + raw config 缓存"，11.x 把这条链上的不一致一次性收敛。

---

## 12. Telegram — Rich Messages opt-in 还原（default off）+ math crash 守 + 重复预览避 + 端点 404 退路硬化

**4 个 commit**（按 committer 时序）：`57c2a55be` `12682d96b` `9459057d7` `a1f51feb7`

### 12.1 `57c2a55be` fix(telegram): harden rich message fallback handling

`gateway/platforms/telegram.py`：
- `_is_rich_capability_error()` —— 按类名识 `EndpointNotFound` / `InvalidToken`；收紧 "method/endpoint ... not found / does not exist" 匹配。
- `_is_rich_fallback_error()` —— 加 `"unsupported"` / `"not implemented"` 子串。
- `_try_send_rich` —— 禁链接预览时加 `link_preview_options={"is_disabled": True}`；删 `return_type=Message` 给 `do_api_request("sendRichMessage", ...)`；`(msg.get("result") or {}).get("message_id")` 安全解析。

### 12.2 `12682d96b` feat(telegram): restore rich messages opt-out（默认 True，本轮内瞬时态）

最初恢复 PR #45840 的 client-compatibility opt-out，commit message 说"默认 enabled via `telegram.extra.rich_messages: true`"，diff 中 `_coerce_bool_extra("rich_messages", True)`。

> ⚠️ **本轮内被后续 commit 翻转** —— `a1f51feb7`（12.4）把默认改回 False。

### 12.3 `9459057d7` fix(telegram): guard rich details math crash (#46102)

`gateway/platforms/telegram.py:955-964` 类正则 `_RICH_DETAILS_RE`、`_RICH_MATH_IN_DETAILS_RE`；`:966-980` `_has_telegram_desktop_details_math_crash_shape()` 守 `_should_attempt_rich`（`:991`）与 `_should_attempt_rich_draft`（`:1217`）。

### 12.4 `a1f51feb7` fix(telegram): avoid rich final duplicate previews (#46206)

**HEAD 状态决定者**：
- `gateway/platforms/telegram.py:423` `_rich_messages_enabled` 默认改 `False`。当前 HEAD 即此（验证 `sed -n '420,430p' gateway/platforms/telegram.py` —— `self._coerce_bool_extra("rich_messages", False)`）。
- `gateway/platforms/telegram.py:996-1008` `prefers_fresh_final_streaming()` 无条件 `return False`（原前 `self._should_attempt_rich(content)`）。

**配置**：`hermes_cli/config.py:1993 "rich_messages": False,  # Opt in to Bot API 10.1 rich messages; default uses legacy MarkdownV2`；`cli-config.yaml.example:727 # rich_messages: false`。

### 12.5 净效应（上期 + 本期）

| 时间 | 默认 | commit |
|------|------|--------|
| 上上期 | False（opt-in，初始上线） | `05b9c84ca` |
| 上期（2026-06-13） | **True**（永远 on） | `a59d5e37e #45584` |
| 本期 12.2 | True（restore opt-out 关键字 = 配置面恢复） | `12682d96b` |
| 本期 12.4（HEAD） | **False**（opt-in 默认关） | `a1f51feb7 #46206` |

> 参见 [[messaging-gateway-architecture]] —— Rich Messages 在 24 小时窗口内经历"配置 → 默认翻转"两轮调整，是本期单平台改动最密集的子项。

---

## 13. Email — SMTP_SSL port 465 + IPv4 兜底 socket

**2 个 commit**：`04d4471d7` `cf7d5932f`

### 13.1 `04d4471d7` fix(email): use SMTP_SSL for port 465 and fall back to IPv4 on timeout

`gateway/platforms/email.py:354-394` `_connect_smtp(self) -> smtplib.SMTP` 合并 4 处重复连接逻辑（call sites `:419, :659, :780, :858`）。

**内层** `_connect(ipv4_only=False)`：
- `port == 465` → `smtp_ssl_cls(...)`。
- 否则 → `SMTP(...) + starttls()`。

**外层 try/except**：捕 `socket.timeout / TimeoutError / ConnectionError / OSError`，retry `_connect(ipv4_only=True)`。`ssl.SSLError` 不吞，直接 re-raise。

### 13.2 `cf7d5932f` fix(email): make IPv4 SMTP fallback use supported sockets

`gateway/platforms/email.py:110` `class _IPv4SMTP_SSL(smtplib.SMTP_SSL)`：自定 `_get_socket` 调 `socket.create_connection(host, port, AF_INET, SOCK_STREAM)`。

---

## 14. Models — ZAI GLM-5.2 1M 上下文 + Kimi K2.7 Code 原生 picker

**2 个 commit**：`bff78a34d` `2a14e8957`

### 14.1 `bff78a34d` feat(zai): add GLM-5.2 with verified 1M context window

**上下文 1M（1,048,576 token）** 经实证（needle-in-a-haystack 789,240 prompt token 0 错 0 截断）。**关键点**：不加此条目则落到 generic `"glm"` key（202,752 token），错误压缩。

- `agent/model_metadata.py:270` `"glm-5.2": 1_048_576`（位于 `"glm": 202752` `:271` 前以正确优先）。
- `hermes_cli/models.py:260` `"glm-5.2"` 加进 zai curated list。
- `hermes_cli/setup.py:96` `"zai": ["glm-5.2", "glm-5.1", ...]`。
- `hermes_cli/auth.py:619-620` 加 coding-global / coding-cn endpoint probe。
- `plugins/model-providers/zai/__init__.py:14` `"glm-5.2"` 加 fallback_models。

### 14.2 `2a14e8957` fix(kimi): surface K2.7 Code in native picker (#46309)

HEAD commit。让 Moonshot K2.7-Code slug 在 `hermes` 命令交互 picker 中可见。

- `hermes_cli/models.py:60` `("moonshotai/kimi-k2.7-code", "")` 入 model catalog。
- `hermes_cli/models.py:181` preferred-models 入。
- `hermes_cli/models.py:285` `"kimi-k2.7-code"` 居 `kimi-coding` `_PROVIDER_MODELS` 首位。
- `hermes_cli/models.py:2373-2382` `provider_model_ids()` 对 `{"kimi-coding", "kimi-coding-cn"}` 把 curated list 与 live `_p.fetch_models()` **合并**——即便 live API 不返 K2.7-code 也保留 curated 项。

> 参见 [[smart-model-routing]]、[[provider-plugin-system]] —— GLM-5.2 的 1M context 与 K2.7-code 的 curated 保留体现 "metadata > live"（防 provider 漏报）。

---

## 15. Performance — OpenRouter 元数据磁盘缓存 + Webhook 增量 prune + Process 完成事件唤醒 + execute_code idle 停 + api-server 线性化 + 图像按 provider 限制缩放

**6 个 commit**：`13a1bd0f8` `afc861550` `110687914` `8f278403d` `5105c3651` `4b5ba112a`

### 15.1 `13a1bd0f8` perf(model-metadata): persist OpenRouter metadata cache (#46114)

- 缓存文件：`get_hermes_home() / "cache" / "openrouter_model_metadata.json"`（`agent/model_metadata.py:116-119`）。
- 写入：紧凑 JSON 经 `atomic_json_write(..., indent=0, separators=(",", ":"))`（`:154-164`）。
- TTL：`_MODEL_CACHE_TTL = 3600`（1 小时，`:110`）。
- `fetch_model_metadata()`（`:681`）：内存缓存 → 磁盘 age 检 → live `/api/v1/models` fetch → 写盘。异常路径回退到 内存→磁盘（`:722-734`）。

### 15.2 `afc861550` perf(webhook): prune request caches incrementally (#46065)

`gateway/platforms/webhook.py`：
- `:271` `_prune_delivery_info(now)`：`Deque[tuple[float, str]] _delivery_info_order`；超 cutoff `popleft`，**懒** O(stale) 而非每 POST O(n)；不同步时 rebuild deque。
- `:293` `_prune_seen_deliveries(now)`：用 `_seen_deliveries_next_prune_at` 节流；下次 prune 排在 `now + min(60.0, max(1.0, _idempotency_ttl/10))`。
- `:303` `_record_rate_limit_hit(route, now)`：每路由独立 deque，popleft 超 `_RATE_WINDOW_SECONDS = 60.0`。
- `:318` `_record_delivery_id(...)` 整合 seen-set 记账，`:327` 调 `_prune_seen_deliveries`。

### 15.3 `110687914` perf(process): wake waiters on background completion (#45831)

- `tools/process_registry.py:132` `ProcessSession` 新 `_completion_event: threading.Event = field(default_factory=threading.Event, repr=False)`。
- `:874` 入 `_finished` 后 `session._completion_event.set()`。
- `:1098-1127` wait 循环原 `time.sleep(1)` 改 `session._completion_event.wait(timeout=min(1.0, remaining))`；`_refresh_detached_session` 返 None → `not_found` 短路。

### 15.4 `8f278403d` perf(execute-code): stop waiting on idle RPC accept (#45948)

- `tools/code_execution_tool.py:475` `_rpc_server_loop(..., stop_event: threading.Event)`：`settimeout` 5 → 0.05；`while not stop_event.is_set(): accept()`；`socket.timeout` continue；`conn is None` 早归。
- `:1168` `execute_code` 内 `stop_event = threading.Event()`；polling 收紧 `poll_interval = 0.005`；加 `touch_activity_if_due` 活动 ping。

### 15.5 `5105c3651` perf(api-server): normalize chat content linearly (#46079)

`gateway/platforms/api_server.py` `_normalize_chat_content`：累加 `total_len` 与 `parts: List[str]`；替代每轮 `sum(len(p) for p in parts) >= MAX_NORMALIZED_TEXT_LENGTH`（O(n²)）为 `total_len >= MAX_NORMALIZED_TEXT_LENGTH`（O(1)/轮）。复杂度 O(n²) → O(n)。

### 15.6 `4b5ba112a` fix: shrink images to reported provider dimension limit (#45979)

- `agent/conversation_loop.py:74-100` `_image_error_max_dimension(error)`：正则 `r"max allowed size(?:\s+for [^:]+)?:\s*(\d{3,5})\s*pixels?"` 解析 provider 错误文本；clamp 到 `[512, 8000]`。
- 调用 `agent/conversation_loop.py:2238-2242` `image_max_dimension = _image_error_max_dimension(api_error) or 8000`，作为 `max_dimension=` kwarg 传 `agent._try_shrink_image_parts_in_messages(api_messages, max_dimension=image_max_dimension)`。
- `agent/conversation_compression.py:642` `try_shrink_image_parts_in_messages` 签名加 `max_dimension`。
- `run_agent.py` +11/-1 透传。
- 测试 `tests/run_agent/test_image_shrink_recovery.py` 71 行新文件。

> 参见 [[prompt-caching-optimization]]、[[parallel-tool-execution]]、[[large-tool-result-handling]] —— 15.x 一组都是"局部数据结构升级，外层语义不变"的纯性能改造。

---

## 16. Misc — atomic_replace 跨设备 EXDEV 兜底 + Anthropic proxy `/v1` + dispatch session_id + Provider 账号移除 + Search 超时保留部分 + Weixin 个人账号 relabel + Author maps + UTF-8 SPA

**9 个 commit + 3 个 chore**

### 16.1 `bf8effad0` fix(utils): copy fallback for atomic replace across devices (#43852)

`utils.py:63` `atomic_replace(tmp_path, target) -> str`：`os.replace` 失败时捕 `OSError`；若 `exc.errno in (errno.EXDEV, errno.EBUSY)` →（1）`shutil.copyfile(tmp, real_path)`（2）`shutil.copystat`（3）`os.fsync(open(real_path, "rb").fileno())`（4）`os.unlink(tmp)`。先以 `os.path.realpath` 保 symlink 语义。

### 16.2 `85e6232a0` fix(providers): support anthropic proxy v1 endpoints

- `agent/anthropic_adapter.py` `build_anthropic_client()`（≈`:751`）剥 `base_url` 尾 `/v1` 或 `/v1/` 后再构造 client。
- `agent/auxiliary_client.py:1144` `_endpoint_speaks_anthropic_messages()` 现识 `/anthropic` 与 `/anthropic/v1` 两种后缀（`urlparse(...).path.rstrip("/")`）。
- 加检测 helper 到 `hermes_cli/models.py`、`hermes_cli/runtime_provider.py`。

### 16.3 `8d5d36d79` fix(dispatch): forward session_id into registry.dispatch (#28479)

`model_tools.py:1115-1125` `handle_function_call()` 把 `session_id=session_id` 透到两条 `registry.dispatch(...)` 路径（常规 tool 路径 + execute_code 路径），对齐已有 `task_id` 模式。

### 16.4 Provider 账号移除（2 commit）

**`e986e3fc6` fix: add provider account removal**：`apps/desktop/src/hermes.ts`（≈`:396`）加 TS client `disconnectOAuthProvider(providerId)` → `DELETE /api/providers/oauth/<id>`；UI `apps/desktop/src/app/settings/providers-settings.tsx`。

**`1b16c4817` fix: guard OAuth account removal**：`hermes_cli/web_server.py:5181-5229` `@app.delete("/api/providers/oauth/{provider_id}")` `disconnect_oauth_provider`。守：`_require_token(request)`；拒未知 id；先调 `_oauth_provider_disconnect_hint(provider, {})`、resolve status 后再调一次（hint 返回时即拒绝，e.g. 外部 CLI）；anthropic 时 unlink `_HERMES_OAUTH_FILE` 并 `clear_provider_auth("anthropic")`。

### 16.5 `1fa761f8d` fix(search): keep partial results on search timeout (#36142)

- `tools/file_operations.py:291` `_SEARCH_TIMEOUT_MARKER_RE = re.compile(r"\n?\[Command timed out after \d+s\]\s*$")`。
- `:294-298` `_search_stdout_and_limit(result)`：`exit_code == 124` 时剥 marker，返 `(stdout, "search_timeout")`。
- `SearchResult` 加 `limit_reason: Optional[str]`（`:244`），在 `to_dict()` 公开（`:260-261`）。
- 应用 `_search_files`、`_search_files_rg`、内容搜索路径。

### 16.6 `288f7026e` fix(messaging): correct Weixin personal account labeling

- `hermes_cli/web_server.py:3856-3864` `_PLATFORM_OVERRIDES["weixin"]` "WeChat (Official Account)" → "Weixin / WeChat (Personal)"；docs_url 由 `developers.weixin.qq.com` 改 `hermes-agent.nousresearch.com/docs/user-guide/messaging/weixin/`。
- `:4029-4045` `_MESSAGING_ENV_FALLBACKS` 的 `WEIXIN_ACCOUNT_ID/TOKEN/BASE_URL` 改用 "iLink Bot" 术语。
- 联动 `apps/desktop/src/app/messaging/index.tsx`、`apps/desktop/src/i18n/zh.ts`。

### 16.7 Author maps（3 commit chore）

- `0f3670ba7` `scripts/release.py` `AUTHOR_MAP`：`"dkobi16@gmail.com": "Diyoncrz18"`。
- `4026f526d` `AUTHOR_MAP`：`"max@c60spaceship.com": "MaxFreedomPollard"`。
- `af1995a83` `AUTHOR_MAP`：`"chromalinx@users.noreply.github.com": "chromalinx"`（带 `# PR #37026 salvage (SSL CA guard)` 注）。

### 16.8 Gateway 杂

- `d76a58bd1` fix(gateway): resolve sudo profile system installs：`hermes_cli/main.py:357-385` `_resolve_sudo_user_profile_env()`，`os.geteuid()==0` 且 `SUDO_USER` 存在（非 root）时解 `~SUDO_USER/.hermes/profiles/<name>`；在 `_apply_profile_override()` `:475` 作 `FileNotFoundError` fallback 调。
- `7b9dc7cd0` test(gateway): align web profile wrapper expectation：单测 `tests/hermes_cli/test_web_server.py` 2 行。

---

## 元信息

### 验证统计

- **Clone**：`/tmp/hermes-agent` clone @ `2a14e8957d7552631b4e33611a828e63f773449a`（HEAD），全 checkout。
- **本主线程实证 anchor**：
  - `pyproject.toml:10 version = "0.16.0"` + `hermes_cli/__init__.py:17-18 __version__ = "0.16.0"` + `__release_date__ = "2026.6.5"` —— 版本未变。
  - `agent/ssl_guard.py:14, 18-23, 28, 32, 40, 45, 61, 87` —— SSL guard 模块结构 + env 名 + 函数名。
  - `agent/ssl_guard.py:94 行` + `hermes_cli/doctor.py:316-318 verify_ca_bundle_with_fallback`。
  - `tools/read_extract.py:248 行`、`EXTRACTABLE_EXTENSIONS = frozenset({".ipynb", ".docx", ".xlsx"})`。
  - `hermes_cli/models.py:60, 181, 260, 285` —— Kimi K2.7-Code + GLM-5.2。
  - `agent/model_metadata.py:270 "glm-5.2": 1_048_576`。
  - `gateway/platforms/telegram.py:426 _coerce_bool_extra("rich_messages", False)` —— HEAD rich-messages 默认 off。
  - `hermes_cli/config.py:1993 "rich_messages": False` —— config 默认 off。
- **4 个并行 research agent 实证**：
  - **Agent A（安全 cluster）**：SSL guard 6 commit + MCP stdio security 2 commit + MCP loop preservation 1 commit + Approval 3 commit + file-safety relax 1 commit。
  - **Agent B（Desktop + Read）**：Read 工具 office/notebook 抽取 1 commit + Desktop notifications cluster 3 commit + polish cluster 4 commit + misc 4 commit + image shrink 1 commit。
  - **Agent C（Agent / Gateway / Codex）**：Refusal 4 commit + Codex 2 commit + Gateway 中断 replay 2 commit + auto-resume 1 commit + repaired-turn 1 commit + silence tokens 2 commit + scoping 2 commit + replied-to media 1 commit + Profile HOME 3 commit + TUI cwd 2 commit + Anthropic proxy 1 commit + dispatch session 1 commit + provider account removal 2 commit + Telegram opt-out 1 commit。
  - **Agent D（Skills / Models / Perf / Misc）**：Skills cluster 5 commit + bundled-update 2 commit + Models 2 commit + OpenRouter cache 1 commit + Telegram rich-final 3 commit + Email 2 commit + Search partial 1 commit + Webhook prune 1 commit + Process wake 1 commit + Execute-code idle 1 commit + API-server linear 1 commit + atomic_replace 1 commit + Weixin label 1 commit + AUTHOR_MAP 3 commit + style 1 commit。

### Cross-link

- [[security-defense-system]] —— SSL guard / MCP stdio security / approval / file_safety 共同构成本期安全主线。
- [[mcp-and-plugins]] —— MCP stdio 拦截 + loop preservation 与既有 capability-gate 三道闸门。
- [[messaging-gateway-architecture]] —— Telegram rich 二次翻转 + replied-to 媒体 + Email SMTP_SSL 在 gateway 平台层集中表达。
- [[agent-loop-and-prompt-assembly]] —— Refusal → content_filter 直透 + content_policy_blocked builder 共享。
- [[interrupt-and-fault-tolerance]] —— 中断 replay 清理 + 启动 auto-resume 串行 + repaired-turn 持久。
- [[gateway-session-management]] —— intentional silence token + shell-supervisor 守 + repaired-turn 持久。
- [[skills-system-architecture]] —— 跨进程 agent 缓存失效 + 全局/平台 disabled 并集 + raw config 缓存。
- [[configuration-and-profiles]] —— Profile HOME 隔离 + TUI 恢复 cwd 落 profile DB。
- [[smart-model-routing]] —— GLM-5.2 1M context + K2.7-code curated 保留。
- [[provider-plugin-system]] —— Anthropic proxy `/v1` + xAI 退役（上期）连贯。
- [[prompt-caching-optimization]] —— 15.x 性能 wave，外层语义不变。
- [[tool-registry-architecture]] —— Read 工具新抽取分支 + dispatch session_id 透传。
