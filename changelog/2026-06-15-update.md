---
title: 2026-06-15 单日同步 — hermes-agent 2a14e8957 → cffd6e3c8（77 commits，~31.5h 单日窗口；async background delegate_task + hindsight observation scopes + /status 显 model+context + display 显 delegate goals + s6 profile gateway 5 修硬化 + xAI OAuth 跨 profile 解析 + write-through global root + access token skew 120→3600s + MCP OAuth fail-fast noninteractive + 文件工具 session cwd 第二步 + disk-cleanup protected walks 显式 frozenset + cron jobs.json 跨进程 fcntl/msvcrt 锁 + Process completion 五态 + Bedrock boto3 ≥1.34.59 守 + Anthropic refusal / debug dump redact + Notion ntn_ + Telegram WhatsApp group JID 路由修 + @assistant-ui/store 0.2.13 pin + macOS Electron pack 重命名前补缺 + Discord slash 100 cap + npm bundled-node prefix 重路 + websockets 入核 + Teams SDK installable extra + Desktop tsconfig ES2023 + 远端 profile 切 image.attach_bytes resync + dashboard chat sidebar profile-scoped + 显式 model 切换绕过坏 provider + HERMES_HOME 从 Windows registry 读 + esbuild/vite 锁回升 + nix CI 拆 + Parallel keyless search 撤回）
created: 2026-06-15
updated: 2026-06-15
type: changelog
tags: [changelog, update, v0.16.0-maintenance, async-delegation-background-mode, delegate-task-background-true, async-delegation-registry, async-delegation-completion-queue, async-delegation-watcher, async-delegation-detach-from-active-children, async-delegation-format-process-notification, async-delegation-max-async-children, async-delegation-stop-command, async-delegation-agents-command, hindsight-observation-scopes-configurable, hindsight-retain-batch-only, status-show-model-and-context, display-delegate-task-goal-parts, s6-profile-gateway-register-without-start, s6-gateway-logs-parent-writable, s6-gateway-desired-state-persist, s6-gateway-clear-stale-log-lock, s6-default-gateway-no-follow-active-profile, xai-oauth-cross-profile-resolution, xai-oauth-write-through-to-global-root, xai-oauth-access-skew-120-to-3600, mcp-oauth-fail-fast-noninteractive, file-tools-session-cwd-second-step, file-tools-task-overrides-helper, file-tools-sentinel-free-abs-cwd, disk-cleanup-protected-top-level-frozenset, disk-cleanup-sweep-prune-dirs-frozenset, disk-cleanup-post-order-dfs, cron-jobs-json-cross-process-lock, cron-jobs-fcntl-flock-msvcrt-locking, cron-jobs-rlock-reentrancy, process-completion-reason-enum, process-termination-source-enum, process-format-notification-routes, gateway-scoped-lock-pid-start-time-impostor-evict, gateway-cmdline-readable-stale-marker, bedrock-min-boto3-1-34-59-guard, debug-dump-redact-secrets, debug-dump-ntn-pattern, redact-notion-ntn-prefix, atomic-json-write-redacted-payload, telegram-whatsapp-group-jid-route-target, whatsapp-channel-aliases-overlay, whatsapp-group-type-tag, discord-slash-100-cap, discord-slot-cap-reserve-skill-group, npm-global-prefix-bundled-node-redirect, npm-install-managed-node-npmrc, assistant-ui-store-0-2-13-pin, vite-build-pr-ci-typecheck, npm-deps-hash-refresh, macos-electron-pack-restore-binary, electron-builder-mac-patch-script, websockets-core-dep, dev-setuptools-relax-to-81, teams-sdk-installable-extra, lazy-deps-platform-teams, desktop-hover-reveal-edge-gutter, desktop-tsconfig-es2023, desktop-profile-switch-connection-resync, dashboard-chat-sidebar-profile-scope, desktop-explicit-model-switch-escape-config, desktop-hermes-home-windows-registry, windows-user-env-reader-helper, esbuild-vite-audit-loop-clear, nix-ci-rip-out, docs-standard-installer, docs-desktop-download-site-root, web-revert-parallel-keyless-search, dashboard-machine-root-reroute-pin, dashboard-container-skip-reconcile, container-restart-skip-is-container-check, windows-install-kill-hermes-before-venv, profile-clone-migrate-config-silent, hermes-debug-effective-terminal-backend, browser-inactivity-timeout-from-config, hermes-state-skip-trigram-backfill-before-v11, salvaged-session-browser-harden, minimax-m3-reasoning-extra-body, minimax-profile-extra-body-for, doctor-nvidia-vendor-slug-accepting, inventory-dedup-user-vs-aggregator-models, author-map-4-salvage, salvage-46552-file-tools-session-cwd, salvage-46614-xai-oauth-profile-writethrough, salvage-46836-macos-electron-pack, salvage-46085-bundled-node-npm-prefix, salvage-46078-discord-slash-100-cap]
sources:
  - hermes-agent/main 2a14e8957..cffd6e3c8
total_commits: "77 个 commit（72 个 non-merge + 5 个 merge；~31.5h 跨夜窗口；committer dates 2026-06-14T10:01 UTC → 2026-06-15T20:35 UTC）"
---

# 2026-06-15 单日同步：hermes-agent main `2a14e8957 → cffd6e3c8`

> **基线**：wiki `1b4414b` 对齐 hermes `2a14e8957d7552631b4e33611a828e63f773449a`（2026-06-14T21:01 UTC，"fix(kimi): surface K2.7 Code in native picker (#46309)"，v0.16.0 维护未发新版）
> **目标**：hermes-agent/main HEAD `cffd6e3c8d0bf6a180ed19a919fab05f44def311`（2026-06-15T20:35 UTC，"Merge pull request #46078 from xxxigm/fix/discord-slash-command-100-cap"）
> **跨度**：约 31.5 小时（committer dates `2026-06-14T10:01 UTC → 2026-06-15T20:35 UTC`，跨夜单日窗口）
> **版本**：仍 **v0.16.0**（`pyproject.toml:10 version = "0.16.0"` + `hermes_cli/__init__.py:17-18 __version__ = "0.16.0"` + `__release_date__ = "2026.6.5"` 不变；本期为 fix / feat / test / chore / docs / refactor / revert / 🐛 fix / ci / nix / change / redact 混合，未发新版）
> **commit 类型分布**：**44 fix / 7 test / 4 feat / 4 chore / 3 docs / 2 🐛 fix / 2 refactor / 1 revert / 1 redact / 1 nix / 1 ci / 1 change / 1 unscoped / 5 merge**
> **diff 体量**：**164 文件改动，+18569 / −17867** 行
> **验证基线**：`/tmp/hermes-agent` clone @ `cffd6e3c8` 全 checkout，所有 commit hash / file:line / 路径 / 函数名 / 行数经 `grep -n` / `git show <sha>` / `Read` 实证；4 个并行 research agent 分主题分担实证（A: Auth / Security / Privacy / Gateway lock / Bedrock，B: s6 / cron / process / docker / dashboard / hindsight，C: Desktop / Install / Deps / CI / Docs，D: Tools / Models / Delegation / Misc）

---

## 章节索引

1. [⭐ feat — 异步后台子代理 `delegate_task(background=true)`（PR #40946 单 commit 大新增）](#1-async-background-delegation)
2. [⭐ feat — Hindsight observation scopes 可配置 + `/status` 显 model+context + Display 显 delegate_task goals（3 feat salvage）](#2-feat-cluster)
3. [Auth — xAI OAuth 跨 profile 解析 + write-through global root + access skew 120→3600s（5 commit + 1 follow-up）](#3-xai-oauth)
4. [Auth — MCP OAuth noninteractive fail-fast（1 commit）](#4-mcp-oauth-noninteractive)
5. [Security — Disk-cleanup protected walks 显式 frozenset + 后序 DFS（3 commit）](#5-disk-cleanup)
6. [Privacy — API 请求 debug dump 全字段 redact + Notion `ntn_` 模式（2 commit）](#6-debug-dump-redact)
7. [Gateway — scoped lock impostor 进程检测（PID+start_time 一致但非 gateway 时清）（1 commit）](#7-gateway-scoped-lock)
8. [Models — Bedrock `boto3>=1.34.59` 守 + MiniMax M3 reasoning extra_body 入 ProviderProfile（3 commit）](#8-models)
9. [s6 / Supervisor — Profile gateway register-without-start + logs 父目录 chown + desired_state 持久 + 清陈旧 lock + reserved-default 不跟 active_profile（5 commit）](#9-s6-supervisor)
10. [Container / Dashboard — 跨 dashboard 重启绕 is_container + dashboard container autodetect 跳 reconcile + machine-dashboard reroute 钉机器根 + Windows 重建 venv 前 kill hermes（4 commit）](#10-container-dashboard)
11. [Tools — 文件工具尊重 session cwd（PR #46552 salvage 5 commit）](#11-file-tools-session-cwd)
12. [Cron — `jobs.json` 跨进程 fcntl/msvcrt 锁 + RLock 重入（2 commit）](#12-cron-jobs-lock)
13. [Process — completion 五态枚举 + termination_source + `format_process_notification` 路由（1 commit）](#13-process-completion-causes)
14. [Messaging — WhatsApp group JID 路由 target 而非 home DM + channel_aliases.json overlay（2 commit）](#14-whatsapp-group-jid)
15. [Desktop — `@assistant-ui/store` 0.2.13 pin + vite build CI + macOS Electron pack 重命名前补缺 + 远端 profile 切 `$connection` resync + dashboard chat sidebar profile-scoped + 显式 model 切换绕过坏 provider + HERMES_HOME 从 Windows registry 读 + tsconfig ES2023 + hover-reveal scrollbar inset（10 commit）](#15-desktop-cluster)
16. [Install / Deps — npm bundled-Node global prefix 重路 + `websockets` 入核 + Teams SDK installable extra + esbuild/vite 锁回升（5 commit）](#16-install-deps)
17. [CI — Nix CI 临时拆 + Desktop renderer build on PRs + nix npmDepsHash 双刷 + esbuild audit loop（4 commit）](#17-ci)
18. [Plugins — Discord slash 100 cap + reserve `/skill` group（PR #46078 3 commit）](#18-discord-slash-cap)
19. [Plugins — `revert(web)` Parallel keyless search 撤回（1 commit）](#19-parallel-revert)
20. [Misc — Profile clone migrate config + hermes debug 显终端 backend override + browser inactivity timeout 入 config + state v11 跳 v10 trigram 回填 + doctor 加 nvidia vendor-slug-accepting + inventory dedup + 4 条 AUTHOR_MAP + Docs（10 commit）](#20-misc)

---

## 1. ⭐ async background delegation

**单 commit 大新增**：`c66ecf0bc` `feat(delegation): async background subagents via delegate_task(background=true) (#40946)`

新增**异步后台子代理**模式：父代理 `delegate_task(background=true)` 立即返回 dispatched 句柄，子代理在 daemon ThreadPoolExecutor 跑，完成后通过 `process_registry.completion_queue` 注入合成消息进父对话。

### 1.1 新文件 `tools/async_delegation.py`（386 行）

- `:96-97 _records: dict[delegation_id, record]` 注册表
- `:124 active_count()` 当前活跃数
- `:130 _new_delegation_id()` 单调 ID 生成
- `:152 dispatch_async_delegation(...)` 核心调度：
  - `:215-226` **原子容量检查** —— record 插入 + capacity check 在同一 lock 临界区（修 TOCTOU race，让 N 个并发 `delegate_task(background=true)` 不会超过 `max_async_children`）
  - 把 `_async_runner` 提交给 daemon `ThreadPoolExecutor`
- `:266 _finalize` 完成回调：
  - `:285-328` 推 `type="async_delegation"` event 进 `process_registry.completion_queue`，payload 含 `delegation_id` / `goal` / `context` / `toolsets` / `role` / `model` / `status` / `summary` / `error` / `api_calls` / `duration_seconds` / `dispatched_at` / `completed_at` / `session_key`
- `:337 list_async_delegations()` 枚举
- `:362 interrupt_all(reason)` 群体打断（每条调自带 `interrupt_fn`）

### 1.2 `tools/delegate_tool.py` 接入

- `:403 _get_max_async_children()` 读 `delegation.max_async_children`
- `:2053` `background: bool` 参数新增
- `:2090-2095` **batch + background 互斥**：批量委派禁用 background，明确报错
- `:2243-2317` 主 dispatch 路径：
  - 父线程上调 `get_current_session_key()` 捕获 gateway 路由键（daemon worker 没有 contextvar，必须在父线程取）
  - **`:2261-2270` 从 `parent_agent._active_children` 解绑** ⚡ —— 这是 review follow-up 的关键修：让父轮中断、缓存 evict、`/new` 不再杀死后台子代理（默认 `delegate_task` 同步语义下子代理强绑父）
  - 调 `dispatch_async_delegation(...)` 含 runner closure `_async_runner` 与 `_async_interrupt`（落 `child.interrupt("Async delegation cancelled")`）
  - 返回 JSON `{"status":"dispatched","delegation_id":...,"mode":"background","note":"…just continue"}`，明指示模型**不要轮询**
- `:3037-3054` tool schema 加 `background: { type: "boolean", default: false }`
- `:3099` 注册 handler

### 1.3 `tools/process_registry.py` 完成事件渲染

- `:1549-1616 _format_async_delegation(evt)` 渲染自包含的 `[ASYNC DELEGATION COMPLETE — <id>]` 文本块：
  - dispatched 时戳 + age
  - 原 goal + context + toolsets + role + model
  - status + API calls + duration_seconds
  - 完整 result（success / interrupted / error 三态分支）
- `:1619 format_process_notification` 加路由：`evt_type == "async_delegation"` → `_format_async_delegation`（`:1647-1648`）

### 1.4 Gateway watcher

- `gateway/run.py:5393` 启动时 schedule `_async_delegation_watcher` 后台 task
- `gateway/run.py:12337-12382` watcher 实现：
  - peek `completion_queue` 顶
  - **非 async event 立即 requeue**（不偷别人的 drain；watcher 只消费自己的 type）
  - `:12316-12335 _enrich_async_delegation_routing(evt)` 解析 `session_key` 拿 platform / chat_id
  - `_inject_watch_notification(...)` 把格式化文本作为 system note 注入对话
  - 2s 轮询间隔

### 1.5 生命周期 & 配置

- `cli.py:981-982` CLI shutdown 调 `interrupt_all(reason="CLI shutdown")`
- `hermes_cli/cli_commands_mixin.py:227-258` `/stop` 同时打断 process + background delegation
- `hermes_cli/cli_commands_mixin.py:260-295` `/agents` 列表预览 goal
- `tui_gateway/server.py:5598-5600` TUI dedup 按 `delegation_id`（防 fallthrough 把每次完成都键作 `("", "async_delegation")` 互相抑制）
- `hermes_cli/config.py:1778 delegation.max_async_children = 3`（默认上限）
- `tests/tools/test_async_delegation.py` 473 行 15 测试，含 detach-at-dispatch + 并发 capacity race

### 1.6 模型契约

工具描述里**显式告诉模型**：dispatched 后不要 poll、不要再次 delegate 同个目标；后续 turn 收到的 `[ASYNC DELEGATION COMPLETE — ...]` 是完整自包含 payload，不需 cross-ref。

---

## 2. feat cluster

3 个独立 `feat` 提交（除 `c66ecf0bc` 外的全部 feat），salvage 自旧 PR：

### 2.1 `a376ca008` feat(hindsight): make observation scopes configurable on retain

- `plugins/memory/hindsight/__init__.py:21` 文档新 env var `HINDSIGHT_RETAIN_OBSERVATION_SCOPES`
- `:330 _load_config` 读 env 进 config key `observation_scopes`
- `:381 _OBSERVATION_SCOPE_KEYWORDS = {"per_tag", "combined", "all_combinations"}`
- `:384 _normalize_observation_scopes(value)` 接受 keyword string / JSON list-of-lists / 扁平 tag list（单 scope）
- `:1240-1243 HindsightMemoryProvider.__init__` 把 `config["observation_scopes"]` 或 env 解析进 `self._observation_scopes`
- `:1498-1499 _build_retain_kwargs` 非空时加 `kwargs["observation_scopes"] = self._observation_scopes`
- `:1614-1620` **tool retain 路径切换** `client.aretain(**kwargs)` → `client.aretain_batch(bank_id=..., items=[item])` —— 因 `aretain` 不接 `observation_scopes`，统一走 batch 路径，与 auto-retain / flush-on-switch 行为对齐
- `:920` schema 暴露默认 `""`，choices = keyword strings / JSON / 空

### 2.2 `ead38107a` feat(status): restore model and context in gateway status

- `gateway/slash_commands.py:395-509 _handle_status_command()`：
  - 优先用 live `status_agent`（`agent.model` / `agent.provider` / `agent.context_compressor.last_prompt_tokens` / `.context_length`）
  - 回退 SessionDB row + SessionStore `last_prompt_tokens`
  - 再回退用户配置
- `locales/en.yaml:284-288` 新键 `gateway.status.model` / `model_provider` / `context` / `context_used`；现有 `tokens` 标签澄清为 "Cumulative API tokens (re-sent each call)"
- 17 个 locale 同步翻译
- `tests/gateway/test_status_command.py` 73 行

### 2.3 `5035fa902` feat(display): show delegate_task goals in tool progress notifications

- `agent/display.py:180 _delegate_task_goal_parts` 抽取 goals
- `:212-222 build_tool_preview` 渲 `"{count} tasks: Goal A | Goal B | Goal C"` 批量 delegate_task
- `:1057-1060 _get_cute_tool_message_impl` 渲 `"3x: Goal A | Goal B"` 在 cute 行

### 2.4 `3e7e9b24d` fix: harden salvaged session and browser improvements

打磨上述三 feat + `2f2e3616b`（browser timeout） + `5b2604df9`（state v11 跳回填）的边缘：

- 新文件 `hermes_cli/session_listing.py`（97 行）共享 helper，被 `/sessions` 调用
- `scripts/release.py:97-98` 映射 Ravenwolf 作者

---

## 3. xAI OAuth

**5 commit + 1 follow-up**（PR #43589 salvage `8844e091c PR#46614`）：`f1d6f0436` 跨 profile 解析 + `497352bc4` write-through global root + `1227007ae` AUTHOR_MAP + `8844e091c` 合并 + `febdddb41` access token 早期 refresh。

全部落 `hermes_cli/auth.py` + `scripts/release.py`（attribution）。

### 3.1 跨 profile 解析（`f1d6f0436`）

- `:3899 _xai_oauth_state_from_store(auth_store)` 返回可用 xAI state：
  - 先看 `_load_provider_state(auth_store, "xai-oauth")`
  - tokens 缺失或空时扫 `auth_store["credential_pool"]["xai-oauth"]` 找含 `access_token + refresh_token` 的任意条目
  - 合并出 `auth_mode="oauth_pkce"` 的 state
- `:3937 _xai_oauth_state_has_usable_tokens(state)` 守：`tokens` dict 含非空 `access_token` AND `refresh_token`
- `:3946-3956 _read_xai_oauth_tokens()` 新行为：profile store 缺 → 回退 `_xai_oauth_state_from_store(_load_global_auth_store())`（global root `~/.hermes/auth.json`）

### 3.2 Write-through global root（`497352bc4`）

- `:1087 _save_auth_store` 加 `target_path: Optional[Path] = None` 参，允许定点持久（`O_EXCL + 0o600`）
- `:3996 _profile_has_own_xai_oauth_state(auth_store)` 仅当 `providers.xai-oauth` 在本 store 是 dict 时 True（区分 shadow 与 root-fallback 读）
- `:4007 _write_through_xai_oauth_to_global_root(state)`：
  - `_global_auth_file_path()` 为 None 时（classic 模式）直接 return
  - **`:4030` pytest 安全带**：`PYTEST_CURRENT_TEST` 存在时拒写真实 `$HOME/.hermes/auth.json`
  - 加载 root store → `_store_provider_state(global_store, "xai-oauth", dict(state), set_active=False)` → `_save_auth_store(global_store, global_path)`
  - 全部 exception 吞到 `logger.debug` —— root write 失败不破 profile save
- `:4061-4079 _save_xai_oauth_tokens` 在 save 前捕 `write_through_to_root = not _profile_has_own_xai_oauth_state(auth_store)`，profile save 完成后在 `_auth_store_lock()` 内调 `_write_through_xai_oauth_to_global_root(state)`

### 3.3 Access token 早期 refresh（`febdddb41`）

- `:111 XAI_ACCESS_TOKEN_REFRESH_SKEW_SECONDS = 3600`（原 `120`），inline 注释：gateway/cron workload 需更宽窗口因 xAI access token ~6h
- `:4379 refresh_skew_seconds` 默认 kwarg 用此常量

### 3.4 AUTHOR_MAP

- `scripts/release.py:1557 "andrewdmwalker@gmail.com": "capt-marbles"`（`1227007ae`）

---

## 4. MCP OAuth noninteractive

**1 commit `dcc321695`**：MCP OAuth fail fast for noninteractive without tokens

- `tools/mcp_oauth.py:756-763 build_oauth_auth()`：现在 `raise OAuthNonInteractiveError(...)` 当 `not _is_interactive() and not storage.has_cached_tokens()`（原行为是 log warn 继续）。消息含 `hermes mcp login {server_name}` hint
- `tools/mcp_oauth_manager.py:411` import `OAuthNonInteractiveError`
- `tools/mcp_oauth_manager.py:427-434 MCPOAuthManager` 同源镜像同检查 + 同提示
- `OAuthNonInteractiveError` 定义 `tools/mcp_oauth.py:84`（原仅用于 callback timeout / user_skipped）
- `tools/mcp_tool.py:2165` 已捕此异常，MCP setup 把 fail-fast 视为可恢复 skip

---

## 5. disk cleanup

**3 commit `c1a70a543` + `40699c329` + `a688d2a1b`**（plus test）

全部 `plugins/disk-cleanup/disk_cleanup.py`：

### 5.1 显式 frozenset

- `:147 _EMPTY_DIR_PROTECTED_TOP_LEVEL = frozenset({"logs","memories","sessions","cron","cronjobs","cache","skills","plugins","disk-cleanup","optional-skills","hermes-agent","backups","profiles",".worktrees"})` —— `HERMES_HOME` 下完全跳过的第一层
- `:153 _EMPTY_DIR_SWEEP_PRUNE_DIRS = frozenset({".git","node_modules","venv",".venv","site-packages","__pycache__"})` —— 任意深度剪枝（原 `hermes_home.rglob("*")` 全遍历会卡 gateway event loop 数分钟）

### 5.2 后序 DFS 替 rglob

- `:364-402 quick()` 空目录 sweep 重写：
  - `sweep_stack: List[Tuple[Path, bool]]` 显式 DFS
  - 顶层 entries 经 `hermes_home.iterdir()` 双 frozenset 过滤
  - 子节点只过 `_EMPTY_DIR_SWEEP_PRUNE_DIRS`
  - `visited` bool 是后序标记 → `rmdir()` 仅在子节点处理完后触发（修原 collect-then-rmdir-by-depth 偶发顺序错乱）

提交分工：`c1a70a543` 上线（内联 sets + stack）→ `40699c329` hoist 至 module 级 frozenset + 转完整后序 DFS → `a688d2a1b` 仅测试 `tests/plugins/test_disk_cleanup_plugin.py +12`。

---

## 6. debug dump redact

**2 commit `ad58dd51a` redact secrets in API request debug dumps + `aab2e99ba` test**

- `agent/agent_runtime_helpers.py:1228-1231 dump_api_request_debug()`：
  - import `from agent.redact import redact_sensitive_text`
  - JSON serialize dump → `redact_sensitive_text(_serialized, force=True)` → re-parse 得 `_redacted_payload`
  - `:1231 atomic_json_write(dump_file, _redacted_payload, default=str)`（aab2e99ba 把 ad58dd51a 的 tmp+`Path.replace` 改成共享 helper，保留 redact 不变）
- `:1236 HERMES_DUMP_REQUEST_STDOUT` 分支：`json.dumps(_redacted_payload, ensure_ascii=False, indent=2, default=str)`（原打印生原 `dump_payload`）
- `agent/redact.py:107 _PREFIX_PATTERNS` 新增 `r"ntn_[A-Za-z0-9]{10,}"`（Notion internal integration token），与现有 `mem0_` / `brv_` / `xai-` 并列
- 行为改变：dump 在 API 错误时**无条件**触发（非 `HERMES_DUMP_REQUESTS` 门），原行为是把请求 body / 系统 prompt / 工具 defs / 错误消息明文落盘
- `scripts/release.py:53 AUTHOR_MAP["tianying.x@eukarya.io"] = "xtymac"`（aab2e99b）

---

## 7. gateway scoped lock

**1 commit `ec05d2bc3`**：fix(gateway): evict scoped lock when PID+start_time match but process is not a gateway

- `gateway/status.py:646-660` 新第三 stale 分支在 `acquire_scoped_lock(scope, identity, metadata)` live-process 检查块内。触发条件：
  ```
  not stale
  and existing.get("start_time") is not None
  and current_start is not None
  and not _looks_like_gateway_process(existing_pid)
  ```
  四条全真时调 `_read_process_cmdline(existing_pid)`。cmdline 可读（非 None）→ `stale = True`。
- 内联注释解释：防 Linux/systemd 启动时确定性 PID + jiffy `start_time` 重用，让 `acquire_scoped_lock` 把冒名进程（cron / nginx / sshd）误判为 live gateway。**保守策略**：cmdline 不可读不 evict
- `_looks_like_gateway_process` 定义 `gateway/status.py:167`
- 补全已有 `:644` 第一 stale 检查（两 start_time 都 None 的情况）；新分支覆盖 "两个 start_time 都非 None 且相等"（原本掉落）

---

## 8. models

### 8.1 Bedrock `boto3>=1.34.59` 守（`2cddc9c89`）

- `agent/bedrock_adapter.py:61 _MIN_BOTO3_VERSION = (1, 34, 59)`
- `:64-88 _require_boto3()` 增强：
  - 现有 import boto3 / ImportError → 安装 hint 不变
  - 解析 `boto3.__version__` 为三 int tuple `tuple(int(x) for x in boto3.__version__.split(".")[:3])`
  - `AttributeError/ValueError` → 返回 boto3 不阻塞（`:80-81` 注释：can't parse — don't block on version check）
  - `version < _MIN_BOTO3_VERSION` → `RuntimeError(f"boto3 {boto3.__version__} does not support converse_stream (minimum 1.34.59 required). Upgrade with: pip install --upgrade boto3")`
- 动因：Ubuntu 24.04 系统 pkg `boto3==1.34.46` 可能 shadow venv pin，让 `converse_stream`（`:1059` 调用）裸 `AttributeError`

### 8.2 MiniMax M3 reasoning extra_body 入 ProviderProfile（`ba3883cd1 + 49e743985`）

- `ba3883cd1` 先在 `run_agent.py` 加 9 行 inline gate
- `49e743985` **重构**：移出 run_agent.py，搬入新 `plugins/model-providers/minimax/__init__.py` ProviderProfile：
  - `:16-22 _is_minimax_global_openai_base_url(base_url)` hostname `api.minimax.io`
  - `:24-26 _is_minimax_m3(model)` normalized `{"minimax-m3","minimax/minimax-m3"}`
  - `:40-50 MiniMaxProfile.extra_body_for(model, base_url)` 仅两 predicate 都真才发 `{"reasoning_split": True}` + thinking / reasoning_effort
  - Anthropic 格式 MiniMax（`/anthropic` 路由 `:67/77/90` 注册）与非 M3 不受影响
- `agent/transports/chat_completions.py` 加 1 行 hook 消费 profile 的 extra_body
- `scripts/release.py:1327` 作者映射

### 8.3 Models — Doctor nvidia + Inventory dedup

- `9df1a1a8d` fix(doctor): recognize nvidia as vendor-slug-accepting provider — `hermes_cli/doctor.py:791-802 providers_accepting_vendor_slugs` 集合加 `"nvidia"`（`:799`）抑制 NVIDIA NIM 模型 ID（如 `qwen/qwen3.5-…`）的伪 aggregator 警告。Provider def 在 `:389`（NVIDIA NIM → integrate.api.nvidia.com/v1）。
- `60cc42e38` fix(inventory): deduplicate models between user-defined and aggregator providers — `hermes_cli/inventory.py:160-188`。`list_authenticated_providers()` 返回 rows 后：`:177-178` 收集 user_models（`is_user_defined=True` 行，小写） → `_is_aggregator(slug)`（来自 `hermes_cli.providers`，`:170/182`）过滤每条 aggregator row 去重（`:184-188`）。大小写不敏感。修 #45954 —— picker 不再静默把 litellm-proxy 模型经 openrouter 路由

---

## 9. s6 supervisor

**5 commit**（全部修 s6-overlay 行为，主要 `hermes_cli/profiles.py` + `hermes_cli/service_manager.py` + `hermes_cli/main.py` + `hermes_cli/container_boot.py`）

### 9.1 `40d7c264f` profile gateway 注册不自动启动（#46266）

- `hermes_cli/profiles.py:1271 _maybe_register_gateway_service()` 现调 `mgr.register_profile_gateway(profile_name, start_now=False)`
- `hermes_cli/service_manager.py:80 / :186 / :904` `start_now: bool = True` 加入 `ServiceManager` Protocol / `_RegistrationUnsupportedMixin` / `S6ServiceManager.register_profile_gateway`
- `:964-965 start_now=False` 时写 `down` marker（`(tmp_dir / "down").touch()`）进新服务目录，让 s6-supervise rescan 后留 stopped
- 防共享 token gateway zombie 自动复活

### 9.2 `61ee2dbfd` logs 父目录可写（#46291）

- `hermes_cli/service_manager.py:756 _render_log_run` 加 `chown hermes:hermes "$HERMES_HOME/logs/gateways" 2>/dev/null || true`（非递归）在 leaf-dir chown 前 —— 后注册 profile 的 log 服务不再 EACCES on `mkdir -p`
- `docker/stage2-hook.sh:319` cont-init 的 `as_hermes mkdir -p` 块加 `"$HERMES_HOME/logs/gateways"`，让父目录在任何服务 boot 前归 hermes 所有。修 #45258

### 9.3 `b77096726` desired state 持久（#46292）

- `hermes_cli/service_manager.py:359 _write_gateway_desired_state(name, desired_state)` 写 `desired_state` + `updated_at` 进 `gateway_state.json`，经 tmp-file replace，best-effort（silent on OSError）
- `:337 _profile_dir_for_gateway_service(name)` 映 `gateway-<profile>` slot → 持久 profile dir（从 `HERMES_HOME` 剥 trailing `profiles/<name>`）
- `S6ServiceManager.start/stop/restart`（`:821/:873/:883`）调 `_write_gateway_desired_state(name, "running"|"stopped"|"running")`
- `hermes_cli/container_boot.py:258 _read_prior_state` 改名 `_read_desired_state`，优先 `desired_state` 字段，回退 legacy `gateway_state`。Reconciler `:110 / :145` 走它；legacy gateway-run 迁移 `:194` 同时写两键
- 防 transient runtime 状态跨容器重启抹掉运维意图

### 9.4 `c2b7669ad` 清陈旧 log lock（#46289）

- `hermes_cli/service_manager.py:758` log-run 脚本末加 `rm -f "$log_dir/lock"`（chown 后、s6-log exec 前）—— virtiofs 上不洁关停留下的 s6-log lockfile 在 re-lock 前删

### 9.5 `95715dcb0` reserved default 不跟 sticky active_profile（#46483）

- `hermes_cli/main.py:467 _apply_profile_override` 第 2 步守：
  ```python
  if profile_name is None and not os.environ.get("HERMES_S6_SUPERVISED_CHILD"):
  ```
- supervised `gateway-default` slot（其 s6 run-script 在 `service_manager.py:686` export `HERMES_S6_SUPERVISED_CHILD=1`）跳过 sticky `active_profile` 回退 —— dashboard 切换 active profile 不再劫持 default gateway

---

## 10. container dashboard

### 10.1 `4eb0ff639` 跨 dashboard 重启绕 `is_container` 检查（#46290）

- `hermes_cli/service_manager.py:86 detect_service_manager()`
- `:118` gate 从 `if is_container() and _s6_running():` 改为裸 `if _s6_running():`
- 移除 `from hermes_constants import is_container` import
- 原因（`:111-117` 注释）：`is_container()` 在 Fly Firecracker microVM 上即便 s6-overlay 是 PID 1 也返 False，使 s6 dispatch 路径全 inert —— `hermes gateway start/stop/restart` 掉落到 host 代码 spawn 重复前台 gateway
- `_s6_running()`（PID 1 comm == `s6-svscan` AND `/run/s6/basedir` 存在）已是 s6 专属判定

### 10.2 `aca11c227` Docker dashboard 容器 autodetect 跳 reconcile（#46293）

- `hermes_cli/container_boot.py:210 _strip_container_argv_prefix(argv)` 剥 PID 1 argv 前缀 `/init` → `main-wrapper.sh` → `hermes`
- `:229 _is_legacy_gateway_run_request` 重构走该 helper
- `:237 _is_dashboard_container(argv)` 第一非前缀 arg == `dashboard` 时 True
- `:439 main()` 在任何 reconcile 前 `if _is_dashboard_container(_read_container_argv()):` → 打 `"reconcile: skipping (dashboard container — does not need per-profile gateways)"` 然后 return 0
- 防两容器共享 bind-mount `HERMES_HOME` 互抢 `logs/gateways/<profile>/lock` flock
- 移除老 env-var gate `HERMES_SKIP_PROFILE_RECONCILE`

### 10.3 `80f8ffc74` machine-dashboard reroute 钉机器根（#46487）

- `hermes_cli/main.py:10785-10790 cmd_dashboard()` reroute 块：原 `env.pop("HERMES_HOME", None)` 替为：
  ```python
  from hermes_constants import get_default_hermes_root
  env["HERMES_HOME"] = str(get_default_hermes_root())
  ```
  仅 import / call 异常时回退原 `pop`
- `get_default_hermes_root()` 剥 trailing `profiles/<name>` 后 resolve 到 `/opt/data`（Docker image `ENV HERMES_HOME=/opt/data`）；原 `pop` 让子进程回退 `$HOME/.hermes` = `/opt/data/.hermes` —— 一个空自播 home，dashboard 只看到空 default profile 且 `detect_install_method()` 找不到 `.install_method`，"Update Hermes" 按钮绕过 Docker guard

### 10.4 `f79551378` Windows 重建 venv 前 kill hermes（#45120）

- `scripts/install.ps1:1431-1442 Install-Venv` 块 "Virtual environment already exists, recreating..."。在 `Remove-Item -Recurse -Force "venv"` 前加：
  ```powershell
  if ($env:OS -eq "Windows_NT") {
      $myPid = $PID
      Write-Info "Stopping any running hermes processes before recreating venv..."
      & taskkill /F /T /IM hermes.exe /FI "PID ne $myPid" 2>$null | Out-Null
      Start-Sleep -Milliseconds 800
      }
  ```
- 强杀 hermes.exe 进程树（排除安装器自身 PID） + 800ms sleep 让 Windows 卸载 `_bcrypt.pyd` 等 native DLL，否则 Windows 拒删映射 DLL

---

## 11. file tools session cwd

**PR #46552 salvage 5 commit**：`d6a8d9dca` + `ddf7c7af8` + `b0c99c12d` + `8fce54499` + `6cb88a087`（merge）

全部 `tools/file_tools.py` + `tools/terminal_tool.py`：

### 11.1 `d6a8d9dca` 文件工具尊重 session cwd

在文件工具 resolver 的 live-cwd 与 `$TERMINAL_CWD` 回退**之间**插入新"registered task/session cwd override"步。TUI / Desktop / ACP session 在任何 terminal command 跑之前注册 raw-keyed workspace cwd，文件工具必须**在首次写**时读 —— 原本回退到进程 cwd。

### 11.2 `ddf7c7af8` 抽取 task-override lookup 单 helper

- `tools/terminal_tool.py:1037-1052 resolve_task_overrides(task_id)` 作为 raw-key-first-then-collapsed override 查找的**单一源头**
- 路由 `file_tools._registered_task_cwd_override`（`tools/file_tools.py:127-143`）与 `_get_file_ops`（`tools/file_tools.py:642`）经它
- `:1040-1049` 注释解释 raw vs collapsed invariant

### 11.3 `b0c99c12d` 文档化 registered-cwd 步

- `_authoritative_workspace_root` docstring `file_tools.py:177-190`
- `_resolve_base_dir` docstring `file_tools.py:200-222` 现列四步顺序
- `_path_resolution_warning` docstring `file_tools.py:248-262`
- 无行为变化

### 11.4 `8fce54499` 抽 sentinel-free abs cwd validator

- `tools/file_tools.py:99-113 _sentinel_free_abs_cwd(raw)` 从 `_configured_terminal_cwd`（`:116-124`）与 `_registered_task_cwd_override`（`:127-143`）byte-identical sentinel + expanduser + isabs validation 尾段抽出
- 用 `_TERMINAL_CWD_SENTINELS` frozenset（`:96`）

### 11.5 最终优先级

`_authoritative_workspace_root`（`file_tools.py:191-197`）四步：
1. live cwd
2. registered task override
3. `$TERMINAL_CWD`
4. （在 `_resolve_base_dir` 内）回退 `os.getcwd()`

---

## 12. cron jobs lock

**2 commit `e5b4cf7be` + `733472952`**：cron `jobs.json` 跨进程安全写

- `cron/jobs.py:58-59 _jobs_file_lock = threading.RLock()`（原 `threading.Lock`）+ `_jobs_lock_state = threading.local()` 重入追踪
- `:70` 新 contextmanager `_jobs_lock()` 组合 in-process RLock 与跨进程文件锁（`<hermes_home>/cron/.jobs.lock`）：
  - Unix: `fcntl.flock(LOCK_EX)`
  - Windows: `msvcrt.locking(LK_LOCK, 1)`
  - 都不可 import → 降级 in-process only + log warn
- `:89-129` **重入支持**：`_jobs_lock_state.depth` 深度计数，同线程嵌套调用复用持有锁，让 `save_jobs` 嵌入 mutation 段不再重 acquire
- `:554 _save_jobs_unlocked(jobs)` 私有，caller-must-hold-lock
- `:575-576 save_jobs(jobs)` 公开 wrap `_save_jobs_unlocked` 在 `with _jobs_lock():`，兼容旧直接 caller
- 所有 mutation 路径包：
  - `create_job` `:762`
  - `update_job` `:836`
  - `remove_job` `:941`
  - `mark_job_run` `:969`
  - `advance_next_run` `:1043`
  - `get_due_jobs` `:1068`
  - `rewrite_skill_refs` `:1253`
- `agent/curator_backup.py:459/466 _restore_cron_skill_links` import `_jobs_lock` 替 `_jobs_file_lock`，curator 回滚也走跨进程锁

---

## 13. process completion causes

**1 commit `be7c919bf` fix(process): label background completion causes (#46659)**

- `tools/process_registry.py:103-104 ProcessSession` 加：
  - `completion_reason: str = "exited"` —— 枚举：`exited | killed | lost | failed_start | already_exited`
  - `termination_source: str = ""` —— 枚举：`process.kill | kill_all | backend_lost | failed_start`
- Setter 落点：
  - `failed_start`：`:725-726 / :731-732`
  - `exited`（若未已置 `killed`）：`:785 / :832 / :875 / :1006`
  - `lost / backend_lost`：`:840-841`
  - `killed` + caller-supplied source：`:1226-1227`
- `ProcessRegistry.kill_process(self, session_id, *, source="process.kill")` `:1162` 加 kwarg；`kill_all` `:1379` 传 `source="kill_all"`
- `_move_to_finished`（`:903-904`）/ `poll` 结果（`:1039-1040`）/ `wait_for_session`（`:1129-1130`）/ `kill_process` 返回（`:1233-1234`）payload 含两字段
- `format_process_notification` `:1619` 按 `completion_reason` 选 `_status`：
  - `"terminated by <source>"` | `"marked lost because the process backend disappeared"` | `"failed to start"` | `"completed normally"` | `"exited"`
  - exit code -15/143 时 append `", SIGTERM"`
- `gateway/run.py:12337` agent-notify 合成消息走 `format_process_notification({...})` 携 `completion_reason / termination_source`，替原硬编码 "completed (exit code…)" 串

---

## 14. whatsapp group jid

**2 commit `ea49a7963` + `0d82060c7`**：WhatsApp group JID 路由 target，不再误送 home DM

### 14.1 `ea49a7963` parser + alias overlay

- `tools/send_message_tool.py:47-49 _WHATSAPP_JID_RE` 正则匹配 `@g.us | s.whatsapp.net | lid | broadcast | newsletter`
- `tools/send_message_tool.py:520-525 _parse_target_ref` whatsapp 分支：返 `(target_ref, None, True)` 给原生 JID（原回退 `(None, None, False)` 导致路由进 home DM）
- **alias overlay** `gateway/channel_directory.py`：
  - `:26 CHANNEL_ALIASES_PATH = get_hermes_home() / "channel_aliases.json"`
  - `:29 _load_channel_aliases` + `:40 _apply_channel_aliases(platforms)` —— `:68` group chat 末缀 `@g.us` 标 `type="group"`
  - Rebuild 时应用（`:151/:305/:316`）AND 每次 load 时应用（`:312`）

### 14.2 `0d82060c7` 硬化

- malformed alias 处理硬化
- `channel_aliases.json` 加入备份快照（`hermes_cli/backup.py:1`）
- `scripts/release.py:1` 映射 Keiron McCammon

---

## 15. desktop cluster

10 commit（含 4 个 `@assistant-ui/store` 集群、3 macOS Electron pack salvage、3 misc desktop 修）：

### 15.1 `@assistant-ui/store` 0.2.13 pin（`eae3836eb + f02484feb + 30377e108 + 45e2f4fdc`）

- `package.json:38-41` 加 `"overrides": { "lodash": "4.18.1", "@assistant-ui/store": "0.2.13" }` —— pin 在 `@assistant-ui/tap@^0.5.x` 兼容窗口。避免 vite build 崩 `"./react-shim" is not exported from package @assistant-ui/tap`
- `tests/test_assistant_ui_tap_compat.py` —— lockfile invariant 测（不硬编码版本）
- `.github/workflows/typecheck.yml:33-42` 新 `desktop-build` job：`npm ci` → `npm run --prefix apps/desktop build`（tsc -b + vite build）。捕 `tsc` 漏过的 export 解析破
- `nix/lib.nix:24 npmDepsHash = "sha256-m9cjbjzi4SaFCjODfdrawS5e+1ag+MpRn528/upSNqo=";` 刷新匹配新 lockfile

### 15.2 macOS Electron pack 重命名前补缺（`f3b32e9f5 + c23a2eec1 + c33e0457d` PR #46836）

- `apps/desktop/package.json:23 "prebuilder": "node scripts/patch-electron-builder-mac-binary.cjs"`
- `:138 "electronDist": "../../node_modules/electron/dist"`（复用已装 dist，避免重 unpack）
- `apps/desktop/scripts/patch-electron-builder-mac-binary.cjs:1-60`：
  - darwin-only（`:4 if (process.platform !== 'darwin') process.exit(0)`）
  - 改 `node_modules/app-builder-lib/out/electron/electronMac.js`
  - 标记 `hermes-macos-electron-binary-fallback`（`:12`）保幂等
  - shape mismatch soft-fail（`:53-56`）
- 补丁包 `doRename(...)`：在 rename → `Hermes` 前从 runtime dist 复制缺失的 `Contents/MacOS/Electron`，修 `ENOENT: no such file or directory, rename .../MacOS/Electron -> .../MacOS/Hermes`
- `scripts/release.py:48 "chaslui@outlook.com": "ChasLui"` AUTHOR_MAP（c23a2eec1）

### 15.3 misc desktop（6 commit）

- `8fe334b05` fix(desktop): inset hover-reveal trigger past the adjacent scrollbar（#44159）—— `apps/desktop/src/components/pane-shell/pane-shell.tsx:88 HOVER_REVEAL_EDGE_GUTTER = 'calc(0.5rem + 2px)'` 让 14px hover-reveal 触发器避开 8px `.scrollbar-dt` 滚动条让 thumb 可点
- `ae433634d` fix(desktop): move tsconfig to es2023 —— `apps/bootstrap-installer/tsconfig.json:3,5` + `apps/shared/tsconfig.json:3,5` 设 `"target":"ES2023"` + `"lib":[..."ES2023"]`
- `fbabf438a + bee13817f` fix(desktop): sync `$connection` on profile switch so remote profiles attach images as bytes —— `apps/desktop/src/store/profile.ts:213 ensureGatewayProfile()`；`:248` 注释确认 gateway swap 后从 active profile descriptor resync `$connection`。无此则 remote pool profile 激活为后台后 `$connection.mode` 卡在 primary → 图片附件用基于路径的 `image.attach` 而非 `image.attach_bytes`，远端图破。failed descriptor fetch 留旧 connection 不动。测试 `apps/desktop/src/store/profile.test.ts +89` 行
- `0bbf325a8` fix(dashboard): scope chat sidebar model card to selected profile（#46665）—— dashboard 聊天侧栏 model card 现把 management profile 传给 `session.create`；触 `web/src/components/ChatSidebar.tsx / contexts/ProfileProvider.tsx / pages/ChatPage.tsx / pages/ProfilesPage.tsx`；让侧栏对齐 sticky active profile（Chat + management 页现与 Profiles 页一致）
- `ed20f5ed0` fix(desktop): let explicit model switches escape broken config providers（#42241/#46796）—— `tui_gateway/server.py:1957 _apply_model_switch()` 签名加 `parsed_flags: tuple[str, str, bool, bool] | None = None`；`:1969-1971` 复用 pre-parsed flags 避免双重解析。`:7013-7030 config.set` 预解析；显式 `--provider` 且无 agent 时经 `_apply_model_switch(parsed_flags=parsed_flags)` 绕开 broken-default agent build。Renderer 在 `use-model-controls.ts` 改：active-session 模型切换发 `config.set` 替 `slash.exec`
- `368fcf1ff` fix(desktop): read HERMES_HOME from the Windows registry when env is stale（#46772）—— `apps/desktop/electron/main.cjs:42 const { readWindowsUserEnvVar } = require('./windows-user-env.cjs')`；`:243-265 resolveHermesHome()` 在 Windows 上先 `readWindowsUserEnvVar('HERMES_HOME')`（HKCU\Environment 注册表），再回退 `%LOCALAPPDATA%\hermes`。修 #45471 `setx HERMES_HOME` 登录后对 Explorer 启动的 Electron `process.env` 不可见。新 `apps/desktop/electron/windows-user-env.cjs` parse `reg query` 输出 + 展开 `%VAR%` 引用 + 非 Windows / spawn 错 / 空时 safe-fail

---

## 16. install / deps

### 16.1 npm bundled-Node global prefix 重路（`a4ee1f223 + 98205da00 + 1db8f7ea8 + 39f479cba`）

- `scripts/install.sh:427-433 configure_managed_node_npm_prefix()`：`$HERMES_HOME/node/bin/npm` 存在 → 写 `prefix=$(dirname link_dir)` 进 `$HERMES_HOME/node/etc/npmrc`
- `scripts/lib/node-bootstrap.sh:65-71 _nb_configure_npm_prefix()`（同源 helper）
- 无条件调用——修复已装：
  - `scripts/install.sh:747` + `:878` invoke `configure_managed_node_npm_prefix`
  - `scripts/lib/node-bootstrap.sh:223` + `:241` invoke `_nb_configure_npm_prefix`
- 行为：`npm install -g <pkg>` 二进制落 on-PATH 命令链接目录而非 off-PATH `$HERMES_HOME/node/bin`，跨 Node 升级幸存。前缀本地 npmrc scope，用户 `~/.npmrc` 不动
- 测：`tests/test_install_sh_node_global_prefix.py`

### 16.2 `0bbff1fc7` websockets 入核 + dev setuptools 放宽（salvage #45486/#44693, PR #46744）

- `pyproject.toml:5 requires = ["setuptools>=77.0,<83"]`（build-system 窗口）
- `pyproject.toml:104 "websockets==15.0.1",` 声明为核心 dep
- `pyproject.toml:138 dev = [..., "setuptools==81.0.0"]`（从 82.0.1 降）inline 注释：`torch >=2.11 caps setuptools<82`。解 `.[dev]` + `torch>=2.11` 时 uv 解析失败

### 16.3 `5f6be7f31` Teams SDK installable extra（salvage #43945, PR #46764）

- `pyproject.toml:182 teams = ["microsoft-teams-apps==2.0.13.4", "aiohttp==3.13.4"]` optional extra
- `tools/lazy_deps.py:159 "platform.teams": ("microsoft-teams-apps==2.0.13.4", "aiohttp==3.13.4")` lazy-install 条目，与 telegram/discord/slack/matrix/dingtalk/feishu 持平
- `plugins/platforms/teams/adapter.py` 真 `check_teams_requirements()` 调 `ensure_and_bind('platform.teams', ...)`，于 `TeamsAdapter.connect()` 开头。Passive `check_requirements()` 保留作 registry check_fn 让 `gateway status` 探活不触 pip
- Teams **不在** `[all]` —— 遵循 2026-05-12 messaging SDK opt-in 政策

---

## 17. ci

### 17.1 `9eb0bcd60` Nix CI 临时拆

- 删 `.github/workflows/nix-lockfile-fix.yml` (-255) + `.github/workflows/nix.yml` (-105)
- `ls .github/workflows/ | grep -i nix` HEAD 上返空

### 17.2 `29c698559` Nix npmDepsHash 刷新（1 行 touch；后 `45e2f4fdcd` 又再刷为 C3 终值）

### 17.3 `92a456f71` esbuild audit-loop 清

- HEAD 上：
  - `apps/bootstrap-installer/package.json:45 "vite": "^8.0.16"`
  - `web/package.json:51 "vite": "^8.0.16"`
  - `ui-tui/package.json:34 "esbuild": "^0.28.1"`
  - `ui-tui/packages/hermes-ink/package.json:52 "esbuild": "^0.28.1"`
- 巨型 `package-lock.json` 重生成 (-15641 +12061)
- 加 `hermes_cli/main.py` autostash 逻辑（+6） + 测试 `tests/hermes_cli/test_update_autostash.py`（+57）保 update lockfile cleanup 时本地编辑

### 17.4 `30377e108` ci(desktop): build renderer on PRs

- 见 15.1 —— `desktop-build` job 在 PR 上跑 vite build，vite 破在 CI 中暴露

---

## 18. discord slash cap

**PR #46078 — 3 commit** `5e851bc6b + 8f4a718f9 + cffd6e3c8`(merge)

- `plugins/platforms/discord/adapter.py:39 _DISCORD_MAX_APP_COMMANDS = 100`
- `:3530 slot_cap = _DISCORD_MAX_APP_COMMANDS - 1`（保 `/skill` group 一个 slot）
- skip 逻辑 `if len(already_registered) >= slot_cap: dropped_over_cap += 1; continue` 插入 COMMAND_REGISTRY 循环与 plugin 循环
- `:3613-3620` 一条警告含 cap (100) 与 drop 计数
- 行为：原生命令 + `/skill` 总幸存；自动注册 + plugin 命令递加到 cap。修 Discord error 30032（"Maximum number of application commands reached"），原会整批 reject
- 回归测：`tests/gateway/test_discord_slash_commands.py:296-309` 注册 200 plugin commands 断言 tree 永远 ≤ 100

---

## 19. parallel revert

**1 commit `f3fe99863` revert(web): remove keyless Parallel search fallback (#46350)**

- 撤回 keyless Parallel Search MCP 路径
- `plugins/web/parallel/provider.py` 从 473 → pre-revert 大小（净 -345 行）
- 全删测试文件 `tests/plugins/web/test_parallel_keyless_mcp.py` + `tests/tools/test_web_keyless_default_fallback.py`
- `tools/web_tools.py` 减 234 行 keyless 注册
- `pyproject.toml` 撤回 Parallel SDK pin

---

## 20. misc

### 20.1 `a829e04d6` fix: migrate cloned profile configs (#46345)

`hermes_cli/profiles.py:458-486 _migrate_profile_config_if_outdated(profile_dir)`。用 `set_hermes_home_override(str(profile_dir))` scope `check_config_version()` / `migrate_config(interactive=False, quiet=True)` 到新 profile，然后清 override。防 profile 创建后伪 "v0 → latest" 告警。异常吞掉，profile 创建不破。

### 20.2 `b2a476646 + 2a08b8c86` fix(dump): report effective terminal backend in `hermes debug`

`hermes_cli/dump.py:255-272`。读 `terminal_cfg.backend`（配置）与 `os.environ["TERMINAL_ENV"]`。env override ≠ config 时输出 `"{env_backend}  (TERMINAL_ENV overrides config.yaml terminal.backend={config_backend})"`（`:267-270`），否则报配置值。输出行 `f"terminal:         {backend}"` `:298`。测试 `tests/hermes_cli/test_dump_terminal_backend.py` 80 行 3 case。

### 20.3 `2f2e3616b` fix(config): read browser inactivity timeout from config（salvage）

`tools/browser_tool.py:1180-1201 _get_session_inactivity_timeout()` 读 `browser.inactivity_timeout`（`:1193` 经 `cfg_get`），默 120 从 `DEFAULT_CONFIG`。`BROWSER_SESSION_INACTIVITY_TIMEOUT` 在 `:1201` 初始化。

### 20.4 `5b2604df9` fix(state): skip redundant trigram backfill before v11 FTS rebuild（salvage）

`hermes_state.py:1133-1135` v10 trigram 回填在将要 v11 FTS rebuild 时跳过（v11+ 重建两个 FTS 表，使回填冗余 + 浪费 WAL）。

### 20.5 `be7c919bf` 见 §13

### 20.6 `ea49a7963/0d82060c7` 见 §14

### 20.7 `49e743985/ba3883cd1` 见 §8.2

### 20.8 `975b9f0a5` docs: recommend standard installer for development (#46646)

`CONTRIBUTING.md:81` 加 "Install with the standard installer" 节。`:84` "run the standard installer, then work inside the repository it cloned."。`:92` 示 `curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash` 为推荐开发安装。`:146` 注释 installer 把 hermes 放 PATH。镜像进 `README.md / README.zh-CN.md / website/docs/developer-guide/contributing.md` + zh-Hans Docusaurus 翻译。

### 20.9 `f7c1cbe66` docs: point desktop download links to site root (deprecate /desktop) (#46795)

桌面下载链接指向站点根，弃用 `/desktop`：
- `website/docusaurus.config.ts:134 / :143 / :182`（"Desktop Download" 页脚）→ `https://hermes-agent.nousresearch.com/`
- `website/docs/getting-started/installation.md:13` → `(https://hermes-agent.nousresearch.com/)` 无 `/desktop`
- 同改：`apps/desktop/README.md / website/docs/getting-started/quickstart.md / website/docs/guides/run-nemotron-3-ultra-free.md / website/docs/index.mdx`

### 20.10 AUTHOR_MAP 4 chore

全部触 `scripts/release.py`（手维 author-email → display-name dict）：
- `f79b109f4 release.py:93 "tharushkadinujaya05@gmail.com": "0xneobyte"`
- `c17469cb1 release.py:94 "138671361+Veritas-7@users.noreply.github.com": "Veritas-7"`
- `1227007ae release.py:1557 "andrewdmwalker@gmail.com": "capt-marbles"`
- `c23a2eec1` salvaged contributor email（PR #38673 salvage attribution）

---

## 结束语 — 主线 wiki 上的影响

**v0.16.0 仍未发新版**（`pyproject.toml:10 / hermes_cli/__init__.py:17-18 __version__="0.16.0"` + `__release_date__="2026.6.5"` 不变）。

本期最显著为：
- ⭐ **`delegate_task(background=true)` 异步后台子代理**（PR #40946 单 commit 大新增 386 LOC `tools/async_delegation.py` + dispatcher / detacher / watcher / process_registry 集成 + TUI dedup + CLI shutdown 全 graceful），重新定义 multi-agent 调度模型
- s6 / supervisor 5 修硬化覆盖 Fly Firecracker / Docker / virtiofs / dashboard profile 切换全场景
- xAI OAuth 跨 profile 体验（profile 读不到时回退 global root + 写时 write-through 回 root + access skew 120s→3600s 适配 6h token）
- 文件工具 session cwd 第二步插入（修 TUI/Desktop/ACP session 首写就落到错误工作区）
- cron jobs.json 跨进程 fcntl/msvcrt 锁（多 worker / curator 不再 race）
- Disk-cleanup 显式 frozenset + 后序 DFS（防 gateway event loop 卡分钟级）
- Process 完成五态枚举 + termination_source（gateway 通知现在准确说"被谁/为何"终止）
- Bedrock `boto3>=1.34.59` 软守（Ubuntu 24.04 系统 pkg 不再静默 AttributeError）
- API 请求 debug dump 全字段经 `redact_sensitive_text(force=True)` + Notion `ntn_` 加入模式
- Gateway scoped lock 第三 stale 分支（PID + start_time 完全一致但 cmdline 不像 gateway 时清，防 systemd 启动期 PID/jiffy 重用）
- Desktop `@assistant-ui/store` 0.2.13 pin + vite build CI 守 + macOS Electron pack 重命名前补缺 + 远端 profile 切 `$connection` resync 让远端 image 走 bytes attach
- npm bundled-Node global prefix 重路（前缀本地 npmrc，跨升级不丢，用户 `~/.npmrc` 不动）
- WhatsApp group JID 不再误送 home DM；`channel_aliases.json` overlay 入备份
- MiniMax M3 reasoning extra_body 从 inline gate 重构进 ProviderProfile（与现有 30 个 provider plugin 对齐）

按主线 wiki 现有 concept 分类，本期变更主要影响：
- `concepts/multi-agent-architecture.md`（async background delegation）
- `concepts/credential-pool-and-isolation.md`（xAI OAuth profile 行为）
- `concepts/configuration-and-profiles.md`（s6 profile gateway + dashboard reroute + Windows registry HERMES_HOME）
- `concepts/cron-scheduling.md`（jobs.json 跨进程锁）
- `concepts/security-defense-system.md`（debug dump redact + Notion `ntn_` + MCP OAuth fail-fast + Disk-cleanup 保护）
- `concepts/gateway-session-management.md`（scoped lock 第三分支 + s6 dispatch 容器跳 `is_container`）
- `concepts/large-tool-result-handling.md`（process completion 五态）

——下一次同步将以 `cffd6e3c8` 为基线，监测：（a）async delegation 后续 follow-up（hardening / model 契约调整）；（b）s6 reserved-default 行为在多 dashboard 切换场景下是否稳定；（c）xAI OAuth 全 fleet 推广后是否再有 profile 路径回归；（d）Bedrock `boto3>=1.34.59` 守是否触发 Ubuntu 24.04 主机的安装回归。
