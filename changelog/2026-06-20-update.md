---
title: 2026-06-20 单日同步 — hermes-agent 01f581d8d → 5a53e0f0（69 commits, ~22h 单日窗口；v0.17.0 维护）— ⭐ Compression in-place compaction 6 commit 保留单 session_id（active/compacted 3 态 + archive_and_compact 非破坏性 + session_search 收回归档轮）+ ⭐ Background delegation fan-out PR #49734（_execute_and_aggregate batch 异步 + is_batch + 单 consolidated 完成块 + 644 行 delegate_tool 重写）+ ⭐ Blank Slate setup mode PR #36733（第三 setup 模式 + 235 行 wizard + agent.disabled_toolsets 全减集 + platform_toolsets.cli=["file","terminal"] + 全 opt-out cluster）+ ⭐ Gateway adapters → bundled plugins 9 个迁移（slack/dingtalk/whatsapp/matrix/feishu/telegram/wecom/email/sms 124 文件 +3647/-2583 移至 plugins/platforms/<name>/adapter.py + plugin.yaml + __init__.py register；signal 不迁）+ Desktop 浮动 composer popout PR #49488 renderer-only nanostore + 拖拽-停靠状态机 + DOCK_ZONE 区域 + Cmd+K 无快捷 + 持久化 localStorage + Provider 失效转移 5 commit（aux 摘要 401/403 abort 不进入降级会话 + agent loop is_auth 失败转移到 fallback + rewrite_prompt_model_identity 系统提示模型身份同步 + clear_model_endpoint_credentials 中央 helper + heal poisoned Nous inference URL → DEFAULT_NOUS_INFERENCE_URL）+ Signal 加固 14 commit（_validate_send_result + _markdown_to_signal 共享 signal_format 模块 + ADTS AAC 识别 + ffmpeg remux to MP4 + OrderedDict echo ring LRU+TTL 50→512 + 5min TTL + quote-detection FIFO 排队 + reply-to-own-message 追踪 _sent_message_timestamps + 群组自身 mention 剥除 + stop-typing RPC + env_float 安全 cast）+ Teams 原生 send_video/send_voice/send_document PR #49308（_send_media_attachment 共享 helper + URL ref / data URI 双路径 + microsoft_teams.api Attachment SDK）+ Language-aware title generation PR #45296（auxiliary.title_generation.language config + _TITLE_PROMPT 双模板 detection-by-instruction）+ Creative-ideation skill v2.1.0 PR #42402（22 新方法 + method-catalog + heuristics + anti-slop）+ Dashboard 隐藏 sidecar 会话 PR #49269（HERMES_SESSION_SOURCE ContextVar + source="tool" 标记）+ inline keyboard model picker 默 persist PR #49176 salvage + 终止 busy session internal-event 中断 PR #49738 + 工具 model whitelist 不剥 prompt/source images + gateway resume restart loop 修 _strip_dangling_tool_call_tail + slash exec dispatch payload TUI/Desktop 双修 + 5 条 AUTHOR_MAP salvage
created: 2026-06-20
updated: 2026-06-20
type: changelog
tags: [changelog, update, v0.17.0-maintenance, in-place-compaction, compression-in-place-flag, archive-and-compact, active-compacted-3-state, session-search-compacted, compaction-non-destructive, last-compaction-in-place, background-delegation-fan-out, dispatch-async-delegation-batch, execute-and-aggregate, consolidated-completion-event, is-batch-flag, blank-slate-setup-mode, blank-slate-walkthrough, agent-disabled-toolsets, platform-toolsets-cli-minimal, set-bundled-skills-opt-out, gateway-adapter-bundled-plugin-migration, plugins-platforms-slack, plugins-platforms-dingtalk, plugins-platforms-whatsapp, plugins-platforms-matrix, plugins-platforms-feishu, plugins-platforms-telegram, plugins-platforms-wecom, plugins-platforms-email, plugins-platforms-sms, plugin-register-platform, plugin-yaml-manifest, desktop-composer-popout, composer-popout-nanostore, use-popout-drag, dock-zone-bottom-px, popout-secondary-disabled, auth-failover-on-401-403, auth-failover-attempted-guard, sync-failover-system-message, rewrite-prompt-model-identity, compress-abort-on-auth-failure, last-summary-auth-failure, clear-model-endpoint-credentials, nous-picker-reload-config, heal-poisoned-nous-inference-url, default-nous-inference-url, signal-validate-send-result, signal-rate-limit-bubble, signal-markdown-shared-format, signal-format-module, signal-adts-aac-remux, signal-ffmpeg-remux-to-m4a, signal-echo-ring-ordereddict, signal-echo-ring-ttl-5min, signal-echo-ring-512-cap, signal-quote-cache-fifo, signal-reply-to-own-message, signal-self-mention-strip-all-groups, signal-stop-typing-rpc, signal-quoted-reply-context-preserve, env-float-helper, env-int-float-cast-guards, teams-native-send-video, teams-native-send-voice, teams-native-send-document, teams-attachment-shared-helper, language-aware-titles, auxiliary-title-language, title-prompt-pinned-language, creative-ideation-v2-1-0, creative-ideation-22-methods, method-catalog, anti-slop-reference, dashboard-sidecar-hide, hermes-session-source-contextvar, session-source-tool-tag, model-picker-persist-default, model-picker-config-write-coerce-dict, gateway-restart-loop-fix, strip-dangling-tool-call-tail, gateway-busy-internal-event-skip, image-whitelist-keep-prompt, image-whitelist-keep-image-urls, slash-exec-dispatch-tui, slash-exec-dispatch-desktop, raft-check-fn-silent, desktop-link-title-window-mute, desktop-right-sidebar-tooltips, desktop-gpu-disabled-banner, remote-display-banner, get-remote-display-reason-ipc, memory-shutdown-hook-warning, cli-active-agent-ref-publish, cron-live-adapter-error-bubble, salvage-49264-picker-persist, salvage-49260-signal-live-delivery, salvage-49269-dashboard-sidecar, salvage-49337-tui-slash-dispatch, salvage-49358-desktop-slash-dispatch, salvage-49308-teams-attachments, salvage-49488-composer-popout, salvage-49530-signal-trio, salvage-49558-env-var-guards, salvage-49563-signal-quote, salvage-49583-signal-mention-typing, salvage-49678-signal-echo-ring, salvage-49680-signal-quote-cache, salvage-49697-tooltips, salvage-49734-async-fanout, salvage-49738-internal-interrupt, author-map-salvage-multiple]
sources:
  - hermes-agent/main 01f581d8d..5a53e0f0
total_commits: "69 个 commit（61 个 non-merge + 8 个 merge；~22h 单日窗口 committer dates 2026-06-20T02:32 +0530 → 2026-06-20T11:38 -0700；250 文件 +12225/−3725；约 34 fix / 8 feat / 6 chore / 5 test / 3 refactor / 3 docs / 1 style + 8 merge）"
---

# 2026-06-20 单日同步：hermes-agent main `01f581d8d → 5a53e0f0`

> **基线**：wiki `8e72d3e` 对齐 hermes `01f581d8df2c3a18d2a5edf7d35dde55a9f9f3f5`（2026-06-20T02:18 +0530，"Merge pull request #49254 from kshitijk4poor/salvage-windows-managed-node-49239"，v0.17.0 已发）
> **目标**：hermes-agent/main HEAD `5a53e0f0f487d3d383e2a7b2eae8f260e9bf1090`（2026-06-20T11:38 -0700，"fix(compression): abort on auth failure instead of rotating into a degraded session"，**v0.17.0 维护**未发新版）
> **跨度**：约 22 小时单日窗口（`committer dates 2026-06-20T02:32 +0530 → 2026-06-20T11:38 -0700`；多数活动集中在 2026-06-20 早上至傍晚北美时区）
> **版本**：仍 **v0.17.0**（`pyproject.toml:10 version = "0.17.0"` + `hermes_cli/__init__.py:17-18 __version__ = "0.17.0"` + `__release_date__ = "2026.6.19"` 不变；本期为 v0.17.0 第一个维护性合并日，无版本号 bump）
> **commit 类型分布**：**34 fix / 8 feat / 6 chore / 5 test / 3 refactor / 3 docs / 1 style** + **1 unscoped** + **8 merge**（合计 69）
> **diff 体量**：**250 文件改动，+12225 / −3725** 行（其中 `56001054` 单次大重构占 124 文件 +3647/-2583，是体量最大的 commit）
> **验证基线**：`/tmp/hermes-agent` clone @ `5a53e0f0` 全 checkout，所有 commit hash / file:line / 函数名 / 行数经 `git show <sha> --stat` / `Grep -n` / `Read` 实证；4 个并行 research agent 分主题分担（A: Compression in-place + 6 个 auth/failover commit / B: Blank Slate + delegation fan-out + 浮动 composer + 语言标题 + Teams 原生附件 + Creative-ideation v2.1.0 / C: gateway adapter→bundled plugin 124 文件迁移 + Signal 14 commit / D: 23 个 misc commit + AUTHOR_MAP + merges），全部返回 file:line + 函数名 / 常量名 / 类名 / dataclass 字段名 + 行数实证

---

## 章节索引

1. [⭐ Compression in-place compaction — 保留单 session_id 选项 + `archive_and_compact` 非破坏性软归档 + 3-态 active/compacted 列 + `session_search` 含归档轮（6 commit, #38763）](#1-in-place-compaction)
2. [⭐ Background delegation fan-out — `dispatch_async_delegation_batch` + `_execute_and_aggregate` 单 consolidated 完成块 + 模型 `background` 入参作废 + 644 行 `delegate_tool.py` 重写（PR #49734）](#2-background-delegation-fanout)
3. [⭐ Blank Slate setup mode — 第三 setup 模式 + 235 行 wizard + `agent.disabled_toolsets` 全减集 + `platform_toolsets.cli=["file","terminal"]` + 全 opt-out cluster（PR #36733）](#3-blank-slate-setup)
4. [⭐ Gateway adapters → bundled plugins — 9 个适配器从 `gateway/platforms/<name>.py` 迁移到 `plugins/platforms/<name>/{adapter,plugin.yaml,__init__}.py`（124 文件 +3647/-2583, `56001054`）+ 测试源码路径重写补丁（`c3292794`）](#4-gateway-adapter-migration)
5. [Provider 失效转移 5 commit — aux 摘要 auth 失败 abort 不进入降级会话（`5a53e0f0`）+ agent loop is_auth 真失败转 fallback（`f22dd8a7`）+ `rewrite_prompt_model_identity` 系统提示模型身份同步（`c884ff64`）+ `clear_model_endpoint_credentials` 中央 helper（`c253b073`）+ Nous picker 重 load_config（`95a3affc`）+ heal poisoned Nous inference URL → `DEFAULT_NOUS_INFERENCE_URL`（`37a4dd49`）](#5-provider-failover-cluster)
6. [Desktop 浮动 composer popout — renderer-only nanostore 状态 + `use-popout-drag` 拖拽-停靠区域 + DOCK_ZONE 常量 + 双击切换 + 持久化 localStorage + 二级窗口禁用（4 commit, PR #49488）](#6-desktop-composer-popout)
7. [Signal 加固 14 commit — `_validate_send_result` 解 results[]/`SignalRateLimitError` 抛 + 共享 `signal_format` 模块 markdown→Signal 偏移 + ADTS AAC 识别 + ffmpeg remux to MP4 + OrderedDict echo ring LRU+TTL 50→512 + 5min TTL + quote-cache FIFO + `_sent_message_timestamps` reply-to-own 追踪 + 群组自身 mention 全剥 + stop-typing RPC + `env_float` helper（11 commit + 3 test, PR #49260 #49530 #49558 #49563 #49583 #49678 #49680）](#7-signal-hardening-cluster)
8. [Teams 原生附件 — `_send_media_attachment` 共享 helper + URL 引用 / data URI 双路径 + `microsoft_teams.api.Attachment` SDK + `send_video` / `send_voice` / `send_document`（PR #49308）](#8-teams-native-attachments)
9. [Language-aware title generation — `auxiliary.title_generation.language` config + `_TITLE_PROMPT` / `_TITLE_PROMPT_PINNED_LANGUAGE` 双模板 + detection-by-instruction（PR #45296）](#9-language-aware-titles)
10. [Creative-ideation skill v2.1.0 — 22 个新方法 + `method-catalog.md` + `heuristics.md` + `anti-slop.md` + `exercises.md`（PR #42402, 28 文件 +2269）](#10-creative-ideation-v2-1-0)
11. [Dashboard 隐藏 sidecar 会话 — `HERMES_SESSION_SOURCE` ContextVar + `_session_source` 解析 + `source="tool"` 标记 + 会话历史过滤（PR #49269）](#11-dashboard-sidecar-hide)
12. [Gateway 杂项 — 内联键盘 model picker 默认持久化（PR #49176 salvage `2fe78d1a` + 2 test）+ 终止 busy session internal-event 中断（PR #49738 `680732c1`）+ resume restart loop 修 `_strip_dangling_tool_call_tail`（`75ed07ac`）+ Teams native attachments 已计 §8](#12-gateway-misc)
13. [Tools / Memory / CLI — 图像生成 model whitelist 不剥 `prompt`/`image_urls`（`d45addc2`）+ memory shutdown hook 失败 warning（`013f9c87`）+ CLI agent ref publish 让 `on_session_end` 生效（`64b21e50`）+ slash exec dispatch payload TUI/Desktop 双修（`857d0244`/`1b7b4d13`）+ Raft check_fn 静默（`8cf7df86`）](#13-tools-memory-cli)
14. [Desktop misc — GPU 加速被禁横幅（`8ebe37f6`）+ 隐藏 link-title 窗口静音（`ae8db1ab` + test `7eb9678c`）+ 右侧栏 tooltip（`404fe730` + 格式 `838daca9`）+ 浮动 composer follow-up polish（已计 §6）](#14-desktop-misc)
15. [Cron + 文档 + AUTHOR_MAP — 空白清理（`c1a0b6a5`）+ #32848 audit 文档清理（`1e40b21b`）+ 5 条 AUTHOR_MAP salvage + 6 merge commits 索引](#15-misc-cron-docs-author-map)

---

## 1. in-place compaction

> 本期最有架构意义的变化。"上下文压缩 = 切新 session_id" 这个延续多版本的根设计被解开：新增 opt-in `compression.in_place` flag 让压缩**原地**重写消息列表 + 重建系统提示但**保留同一 session_id**，对齐 Claude Code / Codex 的会话模型。

### 1.1 背景：会话 id 旋转引起的 bug cluster

> 引用 PR #38763 commit body（`47fadc24`）："Context compression today rewrites the message list AND rotates the session id — it ends the session, forks a parent_session_id child, and renumbers the title (name -> name #2). That moving identity key is the root cause of a whole bug cluster: /goal lost (#33618), pending response lost at the split (#14238), orphan sessions (#33907), TUI sid desync (#36777), FTS search gaps + duplicate sidebar entries (#45117), null continuation cwd (#42228), and title-rename dead-ends (#48989). It also forced a large defensive apparatus (compression lock, contextvar/env/logging triple-sync, orphan finalization, gateway SessionEntry re-propagation, tip projection) whose only job is surviving a mid-conversation id change."

### 1.2 commit 簇（按 author date 升序，但 6 个全都被 teknium1 rebase 到 `committer date 2026-06-20T10:57:07 -0700`）

| sha | subject | files | +/- |
|---|---|---|---|
| `47fadc24` | feat(compression): in-place compaction option (#38763) | 4 | +249/-51 |
| `1fbf48d4` | fix(compression): make in-place compaction durable + rotation-independent | 4 | +141/-41 |
| `4f9485a9` | refactor(compression): tidy in-place compaction path (simplify pass) | 3 | +93/-47 |
| `46634569` | fix(compression): in-place compaction is non-destructive (soft-archive, not delete) | 3 | +167/-90 |
| `854d7572` | fix(compression): keep compaction-archived turns discoverable in session_search | 2 | +92/-11 |
| `69716a2e` | docs(compression): fix stale 'discarded' wording on in_place config flag | 1 | +6/-3 |

### 1.3 新 config flag

- `hermes_cli/config.py:1290 "in_place": False,` —— `compression` 块新键；comment block at `:1290-1305` 说明 "Non-destructive: the live context is compacted (lossy for what the model reloads), but the pre-compaction turns are soft-archived under the same id (active=0, compacted=1) — still searchable via session_search and recoverable, not deleted."
- 设置方式：`hermes config set compression.in_place true`，或直接编辑 `config.yaml` 的 `compression:` 块
- 默认 False，**staged rollout 后续 PR 会反转默认**并删除旋转支路

### 1.4 agent 加载点

- `agent/agent_init.py:1347-1349`：`compression_in_place = is_truthy_value(_compression_cfg.get("in_place"), default=False)`（走 `utils.is_truthy_value` 统一真值 coerce）
- `agent/agent_init.py:1569`：`agent.compression_in_place = compression_in_place` 挂 agent 实例

### 1.5 双分支结构 — `compress_context()` 内

- `agent/conversation_compression.py:337`：`in_place = bool(getattr(agent, "compression_in_place", False))`
- `agent/conversation_compression.py:281`：原 rotation 路径整体移到 `else:` 支路；新 `if in_place:` 支路并立
- in-place 不预 flush 当前轮（紧随的 `archive_and_compact` 会"覆写"那些 insert，预 flush 无意义）；rotation 仍预 flush（`agent/conversation_compression.py:562-565` `agent._flush_messages_to_session_db(messages)`）

### 1.6 `archive_and_compact` 软归档 —— `hermes_state.py:2700+`

> 关键：**第一版 in-place 用 `replace_messages`（DELETE）摧毁了 pre-compaction 历史，导致归档轮不可恢复也不可搜索**。`46634569` 改造为**软归档**：

- `hermes_state.py:2700` `def archive_and_compact(...)`：原子 `UPDATE messages SET active = 0, compacted = 1 WHERE session_id = ? AND active = 1`（`:2734-2737`），随后通过新 helper `_insert_message_rows()`（`:2589`）插入 `compacted_messages` 为 `active=1` 新行
- `hermes_state.py:2670-2698 replace_messages()` 保留 DELETE 语义（用于 `/retry`、`/undo`），docstring 提示 compaction 改用 `archive_and_compact`
- in-place 支路在 `agent/conversation_compression.py:546` 切到 `archive_and_compact`
- 数据保留承诺：`get_messages_as_conversation()` / `get_messages()` 默认 `active=1` 过滤 — 复原会话只载入 compacted 集；pre-compaction 轮以 `active=0, compacted=1` 留盘，`get_messages(include_inactive=True)` 可取
- FTS 触发器 `messages_fts_insert/delete/update`（`hermes_state.py:607-624`）只挂 INSERT/DELETE — `active=0` 是内容保留的 UPDATE，FTS 索引完整无缺

### 1.7 `compacted` 列 + 3 态语义 —— `854d7572`

> 第二版 bug：软归档用了 `active=0` 这个**和 `/undo` 共享**的 flag；`search_messages` 默认 `WHERE active=1` 把归档轮也藏掉了 — "归档可搜索" 的承诺只对裸 FTS / `include_inactive=True` 成立，对实际 `session_search` 工具是假的。

`854d7572` 引入**专属 `compacted` 列**:
- `hermes_state.py:570 compacted INTEGER NOT NULL DEFAULT 0` 加入 `SCHEMA_SQL`；存量 DB additive 重锁（无 version bump）
- 3 态语义文档化在 `hermes_state.py:3489-3494`：
  - `active=1, compacted=0` → live
  - `active=0, compacted=1` → 压缩归档：**OUT** of live，**IN** search
  - `active=0, compacted=0` → rewind/undo：**OUT** of live，**OUT** of search
- `archive_and_compact` 更新写双列 `active=0, compacted=1`（`hermes_state.py:2734-2737`）
- `search_messages` WHERE 放宽 — 主 FTS5 在 `hermes_state.py:3528-3532` 加 `(m.active = 1 OR m.compacted = 1)`；trigram CJK 路径同处理 `hermes_state.py:3611-3614`；`include_inactive=True` 仍返全集
- 短 CJK LIKE fallback 已经返全行，未改

### 1.8 持久化 + rotation-independent —— `1fbf48d4`

- in-place 后 `agent._flushed_db_message_ids = set()` + `agent._last_flushed_db_idx = 0`（`agent/conversation_compression.py:552`/`:616`）：flush 标识 reset，使后续 turn 的增量 flush 计算正确
- `agent._last_compaction_in_place = compacted_in_place`（`agent/conversation_compression.py:691`）+ event 上 `"in_place": in_place`（`:681`）：下游可区分两种压缩
- **边界 hook 双模都触发** —— compaction 是 lossy 的，无论是否旋转 id，都应通知 ContextEngine / MemoryProvider 切边界：
  - `agent.context_compressor.on_session_start(... boundary_reason="compression" ...)`（`agent/conversation_compression.py:635-641`）
  - `agent._memory_manager.on_session_switch(..., reason="compression")`（`agent/conversation_compression.py:651-658`）
  - 触发门 `_is_boundary = bool(_old_sid) or in_place`（`agent/conversation_compression.py:625`）
- gateway 自动 compress 读 flag `_compacted_in_place = bool(getattr(agent, "_last_compaction_in_place", False)) if agent else False`（`gateway/run.py:15818`），强制 `history_offset = 0 if (_session_was_split or _compacted_in_place) else len(agent_history)`（`gateway/run.py:15872-15874`）—— 避免把归档轮当增量再压一次
- 结果 dict 携带 `"compacted_in_place": _compacted_in_place`（`gateway/run.py:15891`, `:15992`）
- 手动 `/compress`：`gateway/slash_commands.py:2636-2659` 把 `if rotated: rewrite_transcript(...)` 扩展为 `if rotated or _in_place:`（`_in_place = bool(getattr(tmp_agent, "compression_in_place", False))` `:2637`），确保 in-place 也写持久化

### 1.9 测试 — `tests/run_agent/test_in_place_compaction.py` 单文件 +340 累计

涵盖：单 session_id 保持、边界 hook 在双模都触发、`active=0/compacted=1` 双列回填、`session_search` 默认含归档轮 + `include_inactive=True` 含 rewind 轮、gateway `history_offset=0` 强制、`/compress` slash 在 in-place 模式下走 durable rewrite path。

### 1.10 影响 —— 14 个开放 issue 的统一解

PR body 列：`/goal lost #33618` / `pending response lost #14238` / `orphan sessions #33907` / `TUI sid desync #36777` / `FTS gaps + duplicate sidebar #45117` / `null continuation cwd #42228` / `title-rename dead-ends #48989` —— 全部源于 mid-conversation id 改变。flag 开关切到 True 后这些都消失（消息列表写、系统提示重建、prefix-cache 失效仍发生，但 id/title/cwd 不变）。

---

## 2. background delegation fanout

> PR #49734 `ea8a8b4a feat(delegation): background fan-out — parallel subagents, one consolidated return`。

### 2.1 stat

5 文件 +721/-333，**`tools/delegate_tool.py` dispatch 路径净重写 644 行**：

| file | +/− |
|---|---|
| `run_agent.py` | +14 |
| `tools/async_delegation.py` | +170 |
| `tools/delegate_tool.py` | +644/-333 |
| `tools/process_registry.py` | +64 |
| `tests/tools/test_async_delegation.py` | +162 |

### 2.2 核心 — `dispatch_async_delegation_batch()`

- `tools/async_delegation.py:337 def dispatch_async_delegation_batch(...)`：把 N 任务批量当**一个**异步单元提交，占用单一异步槽位（不论 N 多少）
- runner `_execute_and_aggregate()`：`tools/delegate_tool.py:2247` 实现 + `:2511-2512` / `:2569` / `:2572` 三次调用：等所有子代理 join，返 `{"results": [...], "total_duration_seconds": N}`
- 完成事件单条 `is_batch=True` + 全部 results 进 `process_registry.completion_queue`（`tools/async_delegation.py:389` 设 `"is_batch": True`，`:488` mirror）
- 渲染为单个 consolidated 块：`tools/process_registry.py:1549-1628 _format_async_delegation`，header at `:1586`，每子任务 header `TASK i/N` at `:1614`

### 2.3 模型级 `background` 参数作废

- tool schema `tools/delegate_tool.py:2065-2076 delegate_task(goal, context, toolsets, tasks, max_iterations, acp_command, acp_args, role, background, parent_agent)` —— 接口未变但语义反转
- 真正调度门：`run_agent.py:5210-5222 _dispatch_delegate_task` 显式忽略模型传的 `background`，强制 `background = (not _is_subagent)`：
  - depth 0（顶层模型调）→ 异步 fan-out
  - depth ≥ 1（orchestrator subagent 调）→ 仍同步（需在自己 turn 内拿 worker 结果）
- 直 Python 调用走 `tools/delegate_tool.py:2111 is_truthy_value(...)` reset 保留历史 sync 行为

### 2.4 并发限制

- `delegation.max_concurrent_children`：每批并行上限（`tools/delegate_tool.py:362-397`）
- `delegation.max_async_children`：同时背景异步上限，默 **3**（`tools/async_delegation.py:100`；reader `tools/delegate_tool.py:403-429`）
  - **满槽行为：拒绝而非排队** —— `delegate_task` 退化为 inline 同步执行（`tools/delegate_tool.py:2561-2569`）
- `delegation.max_spawn_depth`：深度天花板（`tools/delegate_tool.py:2115-2128`）

### 2.5 后续 docs — `a6f08ff0` 单行澄清

`tools/delegate_tool.py:2901` 加一句 "Subagent model is NOT selectable per call: children inherit the parent model (plus its fallback chain) unless you pin all subagents to a model via `delegation.provider` / `delegation.model` in config.yaml."

子代理凭证解析路径：`tools/delegate_tool.py:2660 _resolve_delegation_credentials`。

### 2.6 与 PR #40946 异步背景 delegate 的关系

之前的 PR #40946（v0.16.0，2026-06-15 同步覆盖）让 `delegate_task(background=true)` 成为模型可选；本期则**作废模型选择权**：所有顶层模型调的 delegate_task 都自动走异步。Orchestrator 内部递归调仍同步。

---

## 3. blank slate setup

> PR #36733 `11c6f4c7 feat(setup): Blank Slate setup mode — minimal agent, opt in to everything`。

### 3.1 stat

3 文件 +376/0：
- `hermes_cli/setup.py` +235
- `tests/hermes_cli/test_setup_blank_slate.py` +131（新）
- `website/docs/getting-started/quickstart.md` +10

### 3.2 入口 —— 第三 setup 模式

- 在 `hermes setup` 初装 prompt 加第三选项（**不是**新 subcommand）：`hermes_cli/setup.py:2853-2861` "Blank Slate — everything off except the bare minimum; opt in to each capability"
- 选 `setup_mode == 2` 分支至 `_run_blank_slate_setup`：`hermes_cli/setup.py:2866-2867`
- wizard 主体：`hermes_cli/setup.py:3056-3134`

### 3.3 写入的 config keys —— `_blank_slate_minimize_config` + `_blank_slate_minimal_toolsets`

- `hermes_cli/setup.py:2989 _blank_slate_minimal_toolsets`：`platform_toolsets.cli = ["file", "terminal"]`（`:3006`，权威列表）
- `agent.disabled_toolsets`：所有已知 toolset 集合减去 `{file, terminal}`，从 `CONFIGURABLE_TOOLSETS + plugin keys + plain TOOLSETS` 三源合集计算（`:3026`）
- `hermes_cli/setup.py:3031 _blank_slate_minimize_config`：
  - `agent.max_turns = 90`（`:3037`）
  - `compression.enabled = False`（`:3040`）
  - `memory.memory_enabled = False`, `memory.user_profile_enabled = False`（`:3043-3045`）
  - `checkpoints.enabled = False`（`:3048`）
  - `smart_model_routing.enabled = False`（`:3049`）
  - `session_reset.mode = "none"`（`:3050`）
  - `display.tool_progress = "all"`（`:3053`）
- bundled-skill opt-out marker：`tools.skills_sync.set_bundled_skills_opt_out(True)`（`hermes_cli/setup.py:3117-3118`）

### 3.4 默 opt-out 的子系统

web / browser / code execution / vision / **memory**（含 user_profile）/ **delegation** / cronjob / **skills**（无 bundled seed）/ image gen / kanban / 全 **plugins** / 全 **MCP** server（walkthrough 只指点 `hermes plugins` / `hermes mcp add`，`setup.py:3186-3200`）/ **messaging gateway**（默 off，walkthrough 才提示 accept，`:3204`）/ compression / checkpoints / smart_model_routing / session_reset。

### 3.5 单一 gating invariant

- `hermes_cli/tools_config.py:1528-1536 _get_platform_tools`：`agent.disabled_toolsets` 在 resolved set 上**末尾**减集，**覆盖非配置化 platform-toolset recovery** —— Blank Slate writer 依此 invariant（docstring at `setup.py:2992-3003`）

### 3.6 walkthrough 分叉

`_run_blank_slate_setup` fork at `setup.py:3103-3110`：
- "finish now"：记录 skill opt-out 直接返（`:3112-3131`）
- `_blank_slate_walkthrough`（`:3137-3214`）：逐节 opt-in skills / tools（`hermes tools`）/ plugins / MCP / gateway

Full 装后亦可逆向通过 `hermes tools` / `hermes skills opt-in --sync` / `hermes mcp add` / `hermes plugins` / `hermes setup agent` 重新加（`:3123-3128` 打印）。

### 3.7 docs

`website/docs/getting-started/quickstart.md:100-105` 文档化三模式 chooser + `platform_toolsets.cli` + `agent.disabled_toolsets` writeback 契约。

---

## 4. gateway adapter migration

> 单次大重构 `56001054 refactor(gateway): migrate slack/dingtalk/whatsapp/matrix/feishu/telegram/wecom/email/sms adapters to bundled plugins`（salvage PR #41284），124 文件 +3647/-2583。

### 4.1 迁移表 —— 9 个适配器

每个适配器从 **`gateway/platforms/<name>.py`** 单文件，迁到 **`plugins/platforms/<name>/`** 目录，含 `adapter.py` + `plugin.yaml` + `__init__.py`：

| Adapter | Old | New | 卫星文件 |
|---|---|---|---|
| slack | `gateway/platforms/slack.py` | `plugins/platforms/slack/adapter.py` | — |
| dingtalk | `gateway/platforms/dingtalk.py` | `plugins/platforms/dingtalk/adapter.py` | — |
| whatsapp | `gateway/platforms/whatsapp.py` | `plugins/platforms/whatsapp/adapter.py` | — |
| matrix | `gateway/platforms/matrix.py` | `plugins/platforms/matrix/adapter.py` | — |
| feishu | `gateway/platforms/feishu.py` | `plugins/platforms/feishu/adapter.py` | + `feishu_comment.py` + `feishu_comment_rules.py` + `feishu_meeting_invite.py` 同址 |
| telegram | `gateway/platforms/telegram.py` | `plugins/platforms/telegram/adapter.py` | + `telegram_network.py` |
| wecom | `gateway/platforms/wecom.py` | `plugins/platforms/wecom/adapter.py` | + `callback_adapter.py` + `wecom_crypto.py` |
| email | `gateway/platforms/email.py` | `plugins/platforms/email/adapter.py` | — |
| sms | `gateway/platforms/sms.py` | `plugins/platforms/sms/adapter.py` | — |

**Signal、Bluebubbles、webhook、qqbot、weixin、whatsapp_cloud、yuanbao 等不迁** —— 仍留 `gateway/platforms/`。

### 4.2 旧路径 100% 删除

- 验证：`gateway/platforms/{slack,dingtalk,whatsapp,matrix,feishu,telegram,wecom,email,sms}.py` 全部 "No such file"
- 无 stub / re-export

### 4.3 注册机制

- 每个 plugin 的 `__init__.py` re-export `register` from `adapter.py`（如 `plugins/platforms/slack/__init__.py:1`）
- adapter 加载时 host 调 `register(ctx)`，`ctx` 是 `PluginContext`（`hermes_cli/plugins.py:290`）
- plugin 调 `ctx.register_platform(...)`（`hermes_cli/plugins.py:770`）构造 `PlatformEntry`（`gateway/platform_registry.py:39`）并调 `platform_registry.register(entry)`（`hermes_cli/plugins.py:816`）
- 发现：walk `<repo>/plugins/platforms/` 一级，`_scan_directory`（`hermes_cli/plugins.py:1186-1190`），读每个 `plugin.yaml` manifest（如 `plugins/platforms/slack/plugin.yaml:1` 声明 `kind: platform`）

### 4.4 register() 入口表

`plugins/platforms/<adapter>/adapter.py` register 行：dingtalk:1688 / email:964 / feishu:5491 / matrix:4354 / slack:4077 / sms:475 / telegram:7098 / wecom:(grep verified) / whatsapp:(grep verified)。slack 实例：
```python
plugins/platforms/slack/adapter.py:4077
register(
    adapter_factory=_build_adapter,
    setup_fn=interactive_setup,
    apply_yaml_config_fn=_apply_yaml_config,
    cron_deliver_env_var="SLACK_HOME_CHANNEL",
    standalone_sender_fn=_standalone_send,
    max_message_length=39000,
)
```

### 4.5 Matrix YAML 补 4 键

PR body 提到的 `_apply_yaml_config`（`plugins/platforms/matrix/adapter.py:4288`）补回 main 在 PR 等 1340 commit 期间往 `gateway/config.py` matrix 块加的 4 键：`allowed_users`（`:4297`）/ `ignore_user_patterns`（`:4312`）/ `process_notices`（`:4317`）/ `session_scope`（`:4319`）。

### 4.6 测试 retarget

~60 个 `tests/gateway/test_*.py` 文件原地改路径引用 + 共享 fixture：
- `tests/gateway/test_feishu.py`（482 行改）/ `test_matrix.py`（148）/ `test_dingtalk.py`（108）/ `test_email.py`（87）/ `test_sms.py`（26）
- 20+ `test_telegram_*.py` 文件
- 7 个 `test_slack*.py` 文件
- 共享 `tests/gateway/conftest.py` / `tests/gateway/feishu_helpers.py` / `tests/e2e/conftest.py`
- 单一新增：`tests/gateway/test_slack_plugin_setup.py`（+57）

### 4.7 follow-up — `c3292794 test: retarget source-path refs to migrated plugin paths`

2 文件 +2/-2，盖 56001054 blanket rewrite 漏的 2 处字符串字面量：
- `tests/gateway/test_telegram_webhook_secret.py:34`：`gateway/platforms/telegram.py` → `plugins/platforms/telegram/adapter.py`（`_get_source()` 扫 adapter source 找 webhook-secret check）
- `tests/tools/test_windows_native_support.py:769`：`gateway/platforms/whatsapp.py` → `plugins/platforms/whatsapp/adapter.py`（npm bare-spawn parametrize 列表）

### 4.8 影响

`plugins/platforms/` 目录现 20 个 entry（含 signal 之外几乎全 messaging adapter；signal 仍在 `gateway/platforms/`）。第三方插件作者得到一致的 onboarding contract：`plugin.yaml + adapter.py + __init__.py:register`。

---

## 5. provider failover cluster

> 5 个相关 commit，把 auth / provider 切换从"打补丁"提升为有可观测语义的"原子动作"。

### 5.1 `5a53e0f0 fix(compression): abort on auth failure instead of rotating into a degraded session`

- **Bug**：aux summary call 401/403 在每个 transient-error 分支都 fall through 到 `return None`；默认 `compression.abort_on_summary_failure=False` 让 `compress()` 走 static-fallback 路径**仍旋转 session**。用户被甩到全新但已坏的 session，还在为全 context API call 付费。
- 新 flag：`agent/context_compressor.py:771 self._last_summary_auth_failure: bool = False`；reset at `:1535` + `:2210`
- 分类：`agent/context_compressor.py:1593-1602`：
  ```python
  _is_auth_error = _status in {401, 403} or "invalid api key" in _err_str or "invalid x-api-key" in _err_str or ("api key" in _err_str and ("invalid" in _err_str or "blocked" in _err_str)) or "unauthorized" in _err_str or "authentication" in _err_str
  ```
  匹配则 `self._last_summary_auth_failure = True`
- abort gate：`agent/context_compressor.py:2335 if not summary and (self.abort_on_summary_failure or self._last_summary_auth_failure): ...` —— 不管 config flag 都 abort；auth 专用 warning at `:2341-2349`
- Aux-model one-shot recovery 保留 —— `agent/context_compressor.py:1614-1646` 仍跑 fallback-to-main；abort 仅当主 model 自身 auth 失败或 fallback 也 auth 失败才生效
- 已有的 `_last_compress_aborted` 处理在 `agent/conversation_compression.py:470-483` 把 session 原样保留 + 给用户发警告

stat：2 文件 +161/-8（`agent/context_compressor.py` +65/-8 + `tests/agent/test_context_compressor.py` +104）。

### 5.2 `f22dd8a7 fix(agent): fail over to fallback provider on persistent auth failure (401/403)`

- **Bug**：generic failover dispatch 只在 `{rate_limit, billing}` 触发；`auth`/`auth_permanent` fall through 到 "switch providers manually" 指导，**从不调** `_try_activate_fallback()`。Aux task（compression/vision/title-gen）继承坏 main provider。
- 新 retry-state 字段：`agent/turn_retry_state.py:65 auth_failover_attempted: bool = False`（每 turn 一次性 guard）
- auth-failure failover block：`agent/conversation_loop.py:2842-2858`：
  ```
  if (classified.is_auth and not _retry.auth_failover_attempted and agent._fallback_index < len(agent._fallback_chain)):
      _retry.auth_failover_attempted = True
      emit status "🔐 Authentication failed and could not be refreshed — switching to fallback provider..."
      agent._try_activate_fallback(reason=classified.reason)
      _sync_failover_system_message(agent, api_messages, active_system_prompt)  # :2853
      retry_count = 0
      compression_attempts = 0
      _retry.primary_recovery_attempted = False
      continue
  ```
- `classified.is_auth` 解：`agent/error_classifier.py:89 return self.reason in {FailoverReason.auth, FailoverReason.auth_permanent}`
- 无 fallback 配置时行为不变（fall through 到 terminal 处理）

stat：4 文件 +166/0（`agent/conversation_loop.py` +33 / `agent/turn_retry_state.py` +6 / `tests/agent/test_turn_retry_state.py` +1 / `tests/run_agent/test_auth_provider_failover.py` +126 新）。

### 5.3 `c884ff64 fix(agent): keep system-prompt model identity in sync across provider failover`

- **Bug**：session-stable 系统提示嵌 `Model:` / `Provider:` 身份行；mid-turn failover 切 runtime 但没碰 prompt，fallback model 被问"你是什么 model？"会**冒充主 model**
- 新 `rewrite_prompt_model_identity(agent, model, provider)` `agent/chat_completion_helpers.py:1045-1071`：`re.finditer(rf"(?m)^{label}: .*$", sp)` 只改**最后**一次出现（之前的可能是用户内容），mutate `agent._cached_system_prompt`。**不**持久化到 session DB，主 model 的 prefix-cache 副本字节恒等
- 调用点：failover activation `agent/chat_completion_helpers.py:1321` + primary restore undo `agent/agent_runtime_helpers.py:1055-1056`（两侧共享 prompt 状态，restored primary == 字节恒等原版）
- 新 `_sync_failover_system_message(agent, api_messages, active_system_prompt)` `agent/conversation_loop.py:469-492`：patch in-flight `api_messages[0]["content"]` 用刷新后的 cached prompt（+ 任何 `ephemeral_system_prompt`），返新 prompt 供后续 call-block 重建用
- 8 个 failover 点接：`agent/conversation_loop.py` lines 969 / 1296 / 1369 / 1514 / 2820 / 2853 / 3258 / 3407 / 4355（每 successful `_try_activate_fallback()` 后立即调）

stat：4 文件 +184/0（`agent/agent_runtime_helpers.py` +5 / `agent/chat_completion_helpers.py` +33 / `agent/conversation_loop.py` +42 / `tests/agent/test_failover_identity.py` +104 新）。

### 5.4 `37a4dd49 fix(auth): heal poisoned Nous inference URL on refresh instead of retaining it`

- **Bug**：存的 `inference_base_url` 过主机 allowlist 失败（如旧 `stg-inference-api.nousresearch.com`）时，原代码 `if refreshed_url:` 只在有值时替换；validator 拒掉时**留 poisoned 值原位**。"falling back to default" 警告打了但从不生效，后续每次 call 都打死的 staging 端点。
- 常量：`hermes_cli/auth.py:71 DEFAULT_NOUS_INFERENCE_URL = "https://inference-api.nousresearch.com/v1"`
- 修在 `resolve_nous_runtime_credentials` 和 `refresh_nous_oauth_pure` 两处：
  - `hermes_cli/auth.py:5440-5441 refreshed_url = _validate_nous_inference_url_from_network(refreshed.get("inference_base_url"))` 后 `state["inference_base_url"] = refreshed_url or DEFAULT_NOUS_INFERENCE_URL`
  - `hermes_cli/auth.py:5719-5720` 同 pattern：`inference_base_url = refreshed_url or DEFAULT_NOUS_INFERENCE_URL`
- 验证返 `None` 现在重置到生产默认 —— poisoned `auth.json` 下次 refresh 自愈，与 proxy adapter 行为对齐

stat：2 文件 +94/-4（`hermes_cli/auth.py` +18/-4 + `tests/hermes_cli/test_nous_inference_url_validation.py` +80 新）。

### 5.5 `c253b073 fix(model): clear stale endpoint credentials across switches`

- 新中央 helper `clear_model_endpoint_credentials(model_cfg, *, clear_api_key=True, clear_api_mode=True)` at `hermes_cli/config.py:3934`：pop `model_cfg["api_key"]` / `["api"]` / 可选 `["api_mode"]` —— 这些只对显式 custom endpoint 赋值有效。Built-in provider 从 env / `auth.json` / credential pool 解 creds，遗留 inline 值会**把 secret 留在 `config.yaml`** 且污染后续 custom-resolution 路径
- 替代 `_update_config_for_provider` 内的 inline pop（`hermes_cli/auth.py:6402-6404` 现 import + 调中央 helper）
- 接到全部 model picker flow：`hermes_cli/model_setup_flows.py` 调用点 lines 27（import）/ 128 / 347 / 1256 / 1678 / 1804 / 1894 / 2008 / 2092 / 2286 / 2580 / 2735 —— 一 provider flow 一调（openrouter / nous / azure_foundry / copilot / copilot_acp / kimi / stepfun / bedrock_api_key / bedrock / api_key_provider / anthropic 等）
- Gateway `/model` 路径：`gateway/slash_commands.py:1243` + `:1435` —— 条件 `if str(result.target_provider or "").strip().lower() != "custom": clear_model_endpoint_credentials(model_cfg)`。Custom 保留 inline；其他全擦
- Web UI：`hermes_cli/web_server.py:907,909`（`clear_api_mode=False` / `clear_api_key=False` 做部分清）+ `:3879` / `:3901`（slot config）

stat：9 文件 +187/-17。

### 5.6 `95a3affc fix(model): keep Nous picker from restoring stale custom keys`

- **Bug**：Nous picker flow 跑时 in-memory `config` 字典仍可能持上一次 custom-endpoint session 的字段（`api_key` / `api_mode` 等）。`_update_config_for_provider("nous", inference_url)` 写盘 provider 状态，**但 caller 继续 mutate stale in-memory 副本并存盘**，覆盖刚清掉的 config
- 修在 `_model_flow_nous` `hermes_cli/model_setup_flows.py:331-333`：`_update_config_for_provider("nous", inference_url)` 后 `config = load_config()` 重读盘。函数继续从 reloaded `current_model_cfg` 构 `model_cfg`（`:334-340`），设 `provider="nous"` / `default=selected` / 可选 `base_url`，调 `clear_model_endpoint_credentials(model_cfg)`（`:347` —— 来自 5.5 的 cascade）后写
- 该 commit 早于 5.5 cascade rollout；两者一并确保 stale custom key 既不能在 in-memory 也不能在盘上穿过到 Nous 切换

stat：2 文件 +73/0（`hermes_cli/model_setup_flows.py` +3 + `tests/cli/test_cli_provider_resolution.py` +70）。

---

## 6. desktop composer popout

> PR #49488，4 commit。**全部 renderer-only —— 不开新 Electron `BrowserWindow`，无 IPC channel**。

### 6.1 commit 簇

| sha | subject | files | +/- |
|---|---|---|---|
| `236f0597` | feat(desktop): pop the composer out into a draggable floating window | 12 | (前缀) |
| `f697c97e` | fix(desktop): keep floating composer radius consistent with docked | 1 | +1/-1 |
| `eed78d6e` | fix(desktop): composer popout polish — peel-off placement, panels, chip editing | 多 | (跟进) |
| `15852722` | feat(desktop): pop the composer out into a draggable floating window (#49488) | 12 | +775/-77 |

### 6.2 state container — `apps/desktop/src/store/composer-popout.ts`

- 两 nanostore：`$composerPoppedOut` + `$composerPopoutPosition`
- setter：`setComposerPoppedOut`（line 48）/ `setComposerPopoutPosition`（line 64）
- 持久化 `localStorage` keys：`hermes.desktop.composerPopout.enabled` / `hermes.desktop.composerPopout.position`（lines 5-6）
- 常量：
  - `POPOUT_WIDTH_REM = 19.5`（line 19）
  - 默认位置 `{bottom: 24, right: 24}`（line 23）
  - clamping at line 57-60 防 off-screen strand

### 6.3 component wiring — `apps/desktop/src/app/chat/composer/index.tsx`

- 消费两 store at lines 197-198
- `popoutAllowed = !isSecondaryWindow()`（line 196）—— **次级窗（Cmd+Shift+N 小窗、subagent watch 窗）强制 docked**
- 切换：`handleComposerToggle`（`index.tsx:233-235`）双击触发
- 拖拽 → popout：`handleComposerPopOut`（`index.tsx:221-224`）
- 拖拽 → dock：`handleComposerDock`（`index.tsx:226-229`）

### 6.4 peel-off / dock 状态机 — `apps/desktop/src/app/chat/composer/hooks/use-popout-drag.ts`

- 入口 hook `useComposerPopoutGestures` line 93
- dock-zone 常量 lines 19-24：
  - `DOCK_ZONE_BOTTOM_PX = 72`
  - `DOCK_ZONE_CENTER_TOLERANCE_PX = 150`
  - `DOCK_VERTICAL_FALLOFF_PX = 260`
  - `DOCK_HORIZONTAL_FALLOFF_PX = 220`
- 接近度评分 `dockProximityOf`（line 72）
- hook 返 `{dockProximity, dragging, onPointerDown}`

### 6.5 无键盘快捷

Grep `apps/desktop/src/app/chat/composer/` keydown / shortcut handler 只见 Cmd/Ctrl+K / Enter / Shift+K —— **无 popout toggle 快捷键**，纯手势：拖上→peel，拖到 dock zone→redock，双击 grab 区→toggle。

### 6.6 polish — `eed78d6e`

- peel-off 把 box 中心对齐光标（保持纵向 grab offset）（hook diff）
- completion drawer + attach menu 统一到 `composerPanelCard` primitive
- directive-chip Backspace 原子性 in `apps/desktop/src/app/chat/composer/rich-editor.ts`

### 6.7 `f697c97e` 1 行边框修

`apps/desktop/src/app/chat/composer/index.tsx` —— 浮动 composer 的 border-radius 与 docked 一致。

---

## 7. signal hardening cluster

> 14 个 commit（按时间序，跨 PR #49260 / #49530 / #49558 / #49563 / #49583 / #49678 / #49680）。
>
> **重要：Signal adapter 在 §4 大迁移中**未**移到 `plugins/platforms/`** —— 仍在 `gateway/platforms/signal.py`（1701 行）+ 卫星 `signal_format.py` + `signal_rate_limit.py`。

### 7.1 `5649b864 Fix silent delivery failures in Signal live adapter (#49260)`

- 新 `_validate_send_result(self, result) -> (success, error_message)` at `gateway/platforms/signal.py:1020` —— 扫 signal-cli `results[]` 找任何 `type != "SUCCESS"` 或 `success == False` 的条目
- wire 到单发路径 line 1079 + 多附件批 path line 1278
- `_rpc`（line 983-989）：`raise_on_rate_limit=True`（line 930）时，`result.results` 内 `RATE_LIMIT_FAILURE` 抛 `SignalRateLimitError(retry_after=...)` 而非静默返字典
- **行为**：signal-cli 之前回 HTTP 200 + per-recipient failure 对象，旧代码视任何 non-None 为成功；现在 per-recipient 失败 bubble 为 `SendResult(success=False, error=...)`，rate-limit 抛出让限流调度器退避

stat：3 文件 +161/-9。

### 7.2 `3a6c171e fix(gateway): log signal transport response and bubble cron live adapter errors`

- info 日志：`gateway/platforms/signal.py:1075 "[Signal] Sending response (%d chars) to %s"`
- `cron/scheduler.py::_deliver_result` 引 `target_errors` 列表（line 813）收 live-adapter 失败（`TimeoutError` / 通用 `Exception` / `send_result.success is False`）at lines 834 / 837 / 842，extend 进 `delivery_errors`（line 904 / 910）—— cron caller 终于看得到 live 投递**为什么**失败而非只 "fell back to standalone"

stat：2 文件 +24/-9。

### 7.3 `c1a0b6a5 style: strip trailing whitespace in cron scheduler live-adapter block`

`cron/scheduler.py:839` 纯空白，跟 7.2 同 PR。stat：1 文件 +1/-1。

### 7.4 `905820b5 fix(signal): share markdown formatting across send paths`

- **新文件** `gateway/platforms/signal_format.py`（140 行），export `markdown_to_signal(text) -> tuple[str, list[str]]` —— 把 markdown 转 plain text + Signal `textStyles`（BOLD / ITALIC / STRIKETHROUGH / MONOSPACE）在 UTF-16 偏移
- `_normalize_bullet_markers`：把 `- ` / `* ` / `+ ` bullet 在围栏代码块外正规化为 Unicode `• `
- `gateway/platforms/signal.py` 删掉 in-class 140 行复制；`_markdown_to_signal` 变 wrapper（`gateway/platforms/signal.py:1007`）
- `tools/send_message_tool.py` 也走共享 helper（+25）

stat：5 文件 +306/-140。

### 7.5 `da34fca2 fix(signal): detect ADTS AAC voice notes and remux to MP4`

- `_guess_extension` at `gateway/platforms/signal.py:83` 加 ADTS 区分 at lines 105-109：`if (data[1] & 0xF6) == 0xF0: return ".aac"`（ADTS：`ID=0, layer=00`；MP3：`ID=1, layer∈{01,10,11}`）
- 新 helper `_remux_aac_to_m4a(aac_data)` at `gateway/platforms/signal.py:141` —— `ffmpeg -c:a copy -movflags +faststart`，10s subprocess timeout，返 `(m4a_bytes, ".m4a")` 或 `None`。也检查 `/opt/homebrew/bin/ffmpeg` 和 `/usr/local/bin/ffmpeg`
- 调用：`gateway/platforms/signal.py:906` 附件下载路径 `if ext == ".aac": remuxed = await asyncio.to_thread(_remux_aac_to_m4a, raw_data)`
- **Why**：Android Signal 发原始 ADTS AAC 语音注，与 MP3 共享 `0xFF 0xFx` sync 头；Groq / OpenAI STT 嗅 codec 拒错扩展文件

stat：2 文件 +155/-1。

### 7.6 `06ca1e99 fix(utils): add env_float helper for safe float env var parsing`

`utils.py:326 env_float(key, default=0.0)` —— 镜像 `env_int` at `utils.py:315`：strip、empty 或 `ValueError/TypeError` 返默。salvage 自 #48735。stat：1 文件 +11。

### 7.7 `abafba07 refactor(signal): correct STT-fallback comment, type the markdown wrapper, make AAC test portable`

- `gateway/platforms/signal.py:803-804`：纠正一处不实评论（声称有下游 sniff-and-remux fallback 实际没有）
- `gateway/platforms/signal.py:1007`：包装器 type 为 `tuple[str, list[str]]`（原裸 `tuple`）
- `tests/gateway/test_signal.py`：替换 hardcoded `/home/pi/.hermes/audio_cache/audio_fcfc38390b47.mp3` fixture 为运行时 ffmpeg 生成 0.5s ADTS 样本（`anullsrc -t 0.5 -c:a aac -f adts`），CI 有 ffmpeg 时实测往返

stat：2 文件 +37/-11。

### 7.8 `a7dd98c8 fix(env): guard remaining malformed int/float env var casts with utils helpers`

把 22 个裸 `int(os.getenv(...))` / `float(os.getenv(...))` 换成 `env_int` / `env_float`。涉及：`agent/auxiliary_client.py` / `agent/chat_completion_helpers.py` / `gateway/config.py`（WECOM_CALLBACK_PORT / BLUEBUBBLES_WEBHOOK_PORT）/ `gateway/platforms/email.py`（IMAP/SMTP_PORT / EMAIL_POLL_INTERVAL）/ `gateway/platforms/feishu.py` / `gateway/platforms/telegram.py` / `gateway/platforms/wecom.py` / `gateway/platforms/whatsapp.py` / `hermes_cli/auth.py` / `plugins/platforms/discord/adapter.py` / `run_agent.py`。

**注**：`gateway/platforms/email.py` 等是迁移前位置 —— 这个 commit author date 早于 §4 56001054 在 topo 序里更早，路径仍是 pre-move。

stat：11 文件 +41/-38。

### 7.9 `96b10327 fix(signal): strip bot self-mention from group messages before agent dispatch`

`require_mention=true` 分支内：剥 inbound group `text` 中的 `@<account_norm>` 和 `@<bot_uuid>`，LLM 不再把自己渲染出的 mention 当 addressee 读。stat：1 文件 +11。

### 7.10 `40b6ac9a fix(signal): send explicit stop-typing RPC when cancelling indicator`

- `gateway/platforms/signal.py:1517-1538`：取消 typing-indicator task 时发显式 `sendTyping` RPC `params["stop"] = True`（rpc_id `typing-stop`，`log_failures=False`）。包 try/except 让 RPC / recipient-resolution 失败不阻 backoff-state cleanup（line 1540 紧随）
- **Why**：之前 recipient 设备需等 Signal 内建 ~5s timeout 才丢 indicator

stat：2 文件 +133/-2。

### 7.11 `96db7c68 fix(signal): preserve quoted reply context`

- 新实例属性 `_sent_message_timestamps: set[str]` + 上限 `_max_sent_message_timestamps = 500` —— **与 echo ring 区分**的有界 outbound timestamp 缓存。让用户回复 assistant 消息后仍能 detect 为"reply-to-own"（echo filter 已 consume timestamp）
- 新 helper：`_extract_quote_author` at `gateway/platforms/signal.py:773` / `_quote_references_own_message` at line 790 / `_remember_sent_message_timestamp` at line 809
- `MessageEvent` at line ~715 加 `reply_to_author_id` / `reply_to_author_name` / `reply_to_is_own_message`；`gateway/platforms/base.py MessageEvent` 加同 3 字段。`raw_message` 携 `quote: quote_data`

stat：5 文件 +179/-5。

### 7.12 `32a97a20 fix(signal): strip self-mention in all groups, not just require_mention`

7.9 的扩展 —— hoist self-mention strip 出 `require_mention` 分支应用到**所有**群消息：`gateway/platforms/signal.py:640 if is_group and text:` 后 `account_norm = self._account_normalized`（line 641）。剥 `@<account>` 和 `@<uuid>` 后用 `text.replace("  ", " ").strip()` 折叠双空格（保意图换行）。stat：1 文件 +17/-7。

### 7.13 `332f88f6 fix(signal): harden recently-sent echo ring with LRU + TTL`

- 转 `_recent_sent_timestamps` 从 `set` 到 `OrderedDict[int, float]` 映 timestamp → 插入 `time.monotonic()`
- Cap 抬 **50 → 512**（`_max_recent_timestamps`，`gateway/platforms/signal.py:315`）；加 `_recent_sent_ttl_seconds = 300.0`（5 min）at line 316
- `_track_sent_timestamp`（line 1093+）：re-insert 标 MRU、最老优先丢 TTL 过期、最后硬 cap evict 最老（`popitem(last=False)`）
- 新 `_consume_sent_timestamp(ts)` at `gateway/platforms/signal.py:1110` —— pop 并 hit 返 True。Sync-sent path at line 549 切 `if sent_ts in ring: ring.discard(...)` → `if self._consume_sent_timestamp(sent_ts):`
- **Why**：聊天群可推 >50 outbound 把仍待 pending 的 echo timestamp evict 掉，bot 自身消息被再处理

stat：2 文件 +263/-8。

### 7.14 `2f862832 test(signal): update echo-discard test for OrderedDict ring`

`tests/gateway/test_signal.py:1576`：换 `adapter._recent_sent_timestamps.discard(111222333)` 为 `adapter._consume_sent_timestamp(111222333)`（set.discard 在 OrderedDict 不存在，需让 #31250 quote-cache 回归测过）。stat：1 文件 +3/-1。

### 7.15 `26d9a3c7 fix(signal): FIFO-evict the quote-detection timestamp cache`

7.13 在姊妹缓存上的镜像修：
- `_sent_message_timestamps` 从 `set[str]` 转 `OrderedDict[str, None]` at `gateway/platforms/signal.py:324`。Cap 仍 500
- `_remember_sent_message_timestamp` at `gateway/platforms/signal.py:809` 现 re-insert 标 MRU（lines 816-817）+ FIFO-evict via `popitem(last=False)`（line 820）。旧用 `set.pop()` 删任意元素 —— 一条新 timestamp 可能被 evict 而老的留，漏掉真 reply-to-own
- 加回归测 `test_sent_message_timestamps_evicts_oldest_first` 验最老优先 evict + MRU promotion

stat：2 文件 +30/-4。

### 7.16 cluster 总结

14 commit，主线索：**bound-cache 老化语义（OrderedDict / TTL / FIFO）+ 群消息自身 mention 剥除一致性 + Android Signal AAC 语音 codec 区分 + signal-cli per-recipient 失败可观测性 + markdown 渲染收编到共享 module**。

---

## 8. teams native attachments

> PR #49308 `5f55f0ff feat(teams): native send_video/send_voice/send_document attachments`。

### 8.1 stat & file

2 文件 +144/-10：`plugins/platforms/teams/adapter.py` +96 + `tests/gateway/test_teams.py` +58。

> Teams adapter 早就在 `plugins/platforms/teams/adapter.py`（不在 §4 迁移列表里 —— 它本来就是 bundled plugin），无 `gateway/teams/`。

### 8.2 新方法

- `_send_media_attachment(self, chat_id, source, default_mime, caption, media_label)` —— 共享 helper at `plugins/platforms/teams/adapter.py:1192-1240`
- `send_image` at `plugins/platforms/teams/adapter.py:1242-1256`（重构走 helper）
- `send_image_file` at `:1258-1271`（委派 `send_image`）
- `send_video` at `:1273-1288`（默 MIME `video/mp4`）
- `send_voice` at `:1290-1305`（默 MIME `audio/mpeg`）
- `send_document` at `:1307-1323`（默 MIME `application/octet-stream`）

### 8.3 SDK surface

用 `microsoft_teams.api` SDK —— `Attachment(content_type, content_url)` + `MessageActivityInput().add_attachments(...)`，dispatch via `self._app.activity_sender.send(activity, conv_ref)`（有 conversation reference 时）或 `self._app.send(chat_id, activity)`（`adapter.py:1214` / `:1226-1235`）。**无直接 Graph HTTP** —— payload 全是 SDK object。

### 8.4 媒体传输 — 双路径

- `http://` / `https://` source → 引用附（URL），MIME 由 URL path 猜（lines 1216-1218）
- 本地路径（可选 `file://` 前缀）→ 读 + **base64 编码为 data URI**（lines 1219-1224）

### 8.5 大小 / 格式上限

**不在 adapter 内 enforce** —— 无显式 `MAX_BYTES` 检查；Teams SDK 拒掉时由 line 1238 的宽 `except Exception` 包成 `SendResult(success=False, error=str(e), retryable=True)`。MIME best-effort `mimetypes.guess_type` + per-method fallback。

---

## 9. language aware titles

> PR #45296 `cf58f1a5 feat(titles): support language-aware title generation`。

### 9.1 stat

4 文件 +74/-1：`agent/title_generator.py` +27 + `hermes_cli/config.py` +1 + `tests/agent/test_title_generator.py` +37 + `website/docs/user-guide/configuration.md` +10。

### 9.2 实现

- `agent/title_generator.py` 定义两 prompt 模板：
  - `_TITLE_PROMPT`（lines 22-27）—— auto-match 用户语言
  - `_TITLE_PROMPT_PINNED_LANGUAGE`（lines 29-34）—— 带 `{language}` 占位
- 选择逻辑 `generate_title` at lines 73-74：`prompt = _TITLE_PROMPT_PINNED_LANGUAGE.format(language=language) if language else _TITLE_PROMPT`
- 默认行为：**detection-by-instruction** —— prompt 本身说 "Write the title in the same language the user is writing in"。**无 code-level locale detect** —— 模型被信任镜像

### 9.3 新 config key

- `auxiliary.title_generation.language`（字符串，默 `""`）—— schema 默认 at `hermes_cli/config.py:1457`，reader at `agent/title_generator.py:37-48`
- 空串 ⇒ auto-match；非空（如 `"English"` / `"Japanese"`）⇒ pinned
- docs at `website/docs/user-guide/configuration.md:1010-1018`

---

## 10. creative ideation v2 1 0

> PR #42402 `d799284b feat(optional-skills/creative-ideation): expand to v2.1.0 method library`。

### 10.1 stat

28 文件 +2269/-190。

### 10.2 skill 目录

`optional-skills/creative/creative-ideation/`。

### 10.3 version bump

`optional-skills/creative/creative-ideation/SKILL.md:5 version: 2.1.0`（原 2.0.0）。frontmatter 也加 `name: creative-ideation`（line 2，对齐 dir slug）。

### 10.4 22 个新方法

`optional-skills/creative/creative-ideation/references/methods/` 下：

`affinity-diagrams.md` / `analogy-and-blending.md` / `biomimicry.md` / `chance-and-remix.md` / `compression-progress.md` / `creative-discipline.md` / `defamiliarization.md` / `derive-and-mapping.md` / `first-principles.md` / `jobs-to-be-done.md` / `lateral-provocations.md` / `leverage-points.md` / `oblique-strategies.md` / `oulipo.md` / `pataphysics.md` / `pattern-languages.md` / `polya.md` / `premortem-and-inversion.md` / `scamper.md` / `story-skeletons.md` / `triz-principles.md` / `volume-generation.md`。

### 10.5 references 配套

- `method-catalog.md`（+88）
- `heuristics.md`（+85）
- `anti-slop.md`（+106）
- `exercises.md`（+71）
- `full-prompt-library.md` 重构（+206/-?）

---

## 11. dashboard sidecar hide

> PR #49269 `a7983d5a fix(dashboard): hide sidecar sessions from history`。

### 11.1 stat & 关键文件

8 文件 +129/-11。

### 11.2 新 `HERMES_SESSION_SOURCE` ContextVar

- `gateway/session_context.py:52` / `:73` / `:105` / `:128` / `:160` —— 添加 ContextVar + env var 双源穿透
- helper `_session_source_for_agent`（`run_agent.py:92-101`）
- 用在 `AIAgent._ensure_session_db_row`（`run_agent.py:528`）+ session-switch start_context（`run_agent.py:594`）

### 11.3 TUI server 端

- `tui_gateway/server.py:1151-1156 _session_source(session)` 读 `session["source"]` 默 `"tui"`
- 用在 3 个 `db.create_session` 调用点（`tui_gateway/server.py:1258`, `:5774`）+ `_set_session_context`（`:1427-1433`）

### 11.4 Web `ChatSidebar`

`apps/desktop/.../ChatSidebar.tsx:202` 把 sidecar `session.create` 标 `source: "tool"`。

### 11.5 行为

dashboard 生出的 sidecar gateway session 在 DB 行创建时标 `"tool"`（不再是 `"tui"`），history filter 能据此从 session list 隐藏 sidecar 用例。

---

## 12. gateway misc

### 12.1 内联键盘 model picker 默 persist — PR #49176 salvage（4 commit）

- `2fe78d1a fix(gateway): persist inline-keyboard model-picker selections by default`：镜像文本 `/model` 的 persist 块到 inline-keyboard callback。callback 现传 `is_global=persist_global`（`gateway/slash_commands.py:1155`），tap 成功后写 model 到 `config.yaml` via `save_config`，把扁平 `model:` 字符串 coerce 成嵌套 dict（`:1219-1247`）。确认消息分支 `gateway.model.saved_global` / `gateway.model.session_only_hint`（`:1282-1285`）。**行为**：Telegram / Discord / Matrix 的 model tap 现在能跨 gateway 重启留存，不再 session-only。stat：1 文件 +33/-2
- `10fea06c test(gateway): cover inline-keyboard model-picker persistence`：1 文件 +176（`tests/gateway/test_model_picker_persist.py` 新）
- `2099c7b5 test(gateway): make picker-persist tests hermetic and parametrized`：1 文件 +53/-34。stub `list_picker_providers` + `resolve_display_context_length`，运行时从 ~11s → ~0.4s；parametrize 收正例
- `1a0ef131 Merge PR #49264`

### 12.2 终止 busy session internal-event 中断 — `680732c1 fix(gateway): never interrupt a busy session with an internal completion event (#49738)`

- `_handle_active_session_busy_message` at `gateway/run.py:4172-4184` 顶加 early guard：`if getattr(event, "internal", False): return False`
- **行为**：`delegate_task(background=true)` + 终端 `notify_on_complete` 回调以 internal MessageEvent 重入 busy session 时，base adapter **静默排队**，不再用 "Interrupting current task" ack 中止活跃轮

stat：2 文件 +165。

### 12.3 resume restart loop 修 — `75ed07ac fix(gateway): break the restart loop at the source on session resume`

- 新 helper `_strip_dangling_tool_call_tail`（`gateway/run.py:940-980`），从 `_build_gateway_agent_history` line 813 调用 —— 剥尾部 `assistant(tool_calls)` 无 `tool` 响应的孤立配对
- 重写 resume-pending system note（`gateway/run.py:15755-15776`），禁止再执行 restart 命令；为 empty-auto-resume vs new-message 用不同 copy
- **行为**：杀掉 gateway 的工具调用（如 `docker restart`）resume 时不再无限循环，因 unanswered tool call 在 replay 前被移除

stat：3 文件 +208/-12。

---

## 13. tools memory cli

### 13.1 工具 — 图像生成 model whitelist 不剥 `prompt`/`image_urls` — `d45addc2 fix(tools): never let a model whitelist strip the prompt / source images`

- `_build_fal_payload`：`prompt` 无条件保留 `if k in supports or k == "prompt"`（`tools/image_generation_tool.py:615`）
- `_build_fal_edit_payload`：`prompt` + `image_urls` 双保留 `_required = {"prompt", "image_urls"}`（`:669-672`）
- **行为**：省略 mandatory key 的 FAL model config 不能再静默生空 / 坏 request

stat：2 文件 +50/-2。

### 13.2 memory — CLI shutdown hook 失败 warning — `013f9c87 fix(memory): log CLI shutdown hook failures`

- `MemoryManager.on_session_end` provider-hook 失败 debug→warning + `exc_info=True`（`agent/memory_manager.py:724`）
- `cli._run_cleanup` 加 info "CLI cleanup calling memory shutdown"（`cli.py:1034-1046`），原 silent `except: pass` 改 `logger.warning(... exc_info=True)`
- `AIAgent.shutdown_memory_provider`（`run_agent.py:3037-3038`）同 warn-with-traceback 提升
- **行为**：CLI memory shutdown 失败现在可观测，不再吞掉

stat：3 文件 +15/-5。

### 13.3 CLI — agent ref publish 让 on_session_end 生效 — `64b21e50 fix(cli): publish agent ref to cli module so memory on_session_end fires on exit`

- 替代 bare `global _active_agent_ref`（Phase-4 重构后绑 mixin module 不是 `cli.py`）为 `import cli as _cli; _cli._active_agent_ref = self.agent`（`hermes_cli/cli_agent_setup_mixin.py:403-404`）
- **行为**：`on_session_end` hook（custom memory provider 的 end-of-session 抽取）现在真的在 `/exit` 触发 —— 之前 `cli._run_cleanup` 的 `if _active_agent_ref:` 是死分支

stat：2 文件 +81/-3。

### 13.4 slash exec dispatch payload TUI 修 — `857d0244 fix(tui): handle dispatch payloads from slash exec (#49337)`

- 抽 dispatch 处理 switch 为共享 `handleDispatch`（`ui-tui/src/app/createSlashHandler.ts:77-117`）
- `slash.exec` 成功后**先**检查 `asCommandDispatch(r)`（line 124-125），让直接由 `slash.exec` 返的 skill / send / alias / prefill payload 被尊重，不渲染为裸文本
- **行为**：TUI 现在能正确执行那些**从 `slash.exec` 本身**返 dispatch object 的 slash 命令，不再只识 `command.dispatch` fallback

stat：2 文件 +82/-39。

### 13.5 slash exec dispatch payload Desktop 修 — `1b7b4d13 fix(desktop): handle slash exec dispatch payloads (#49358)`

- 镜像 13.4 到 desktop：`handleDispatch`（`apps/desktop/src/app/session/hooks/use-prompt-actions.ts:918-967`）
- 在 `slash.exec` 响应上**先**试 `parseCommandDispatch(result)`（line 981），fallback 到 `command.dispatch`（line 1006）
- 同行为 diff

stat：2 文件 +99/-25。

### 13.6 Raft check_fn 静默 — `8cf7df86 fix(plugins): silence raft check_fn log spam for users without raft CLI`

- 删 `check_raft_requirements`（`plugins/platforms/raft/adapter.py:100-116`）内两 `logger.warning` 调用；函数现在静默返 False
- **行为**：无 raft CLI 用户不再每 ~10s 每 `load_gateway_config()` 警告；create-adapter 处仍 per-connect 警告一次

stat：2 文件 +86/-3。

---

## 14. desktop misc

### 14.1 GPU 加速被禁横幅 — `8ebe37f6 feat(desktop): notify renderer when GPU acceleration is disabled due to remote display`

- IPC handler `ipcMain.handle('hermes:get-remote-display-reason', ...)`（`apps/desktop/electron/main.cjs:153`）返已有 `REMOTE_DISPLAY_REASON`
- 暴露 `window.hermesDesktop.getRemoteDisplayReason`（`apps/desktop/electron/preload.cjs:143`）
- 挂可解散 `<RemoteDisplayBanner>`（新文件 `apps/desktop/src/components/remote-display-banner.tsx`）在 `desktop-controller.tsx:960`
- 新 i18n key `remoteDisplayBanner` 在 en / ja / zh / zh-hant / types
- **行为**：RDP / SSH / X11 用户现在见 banner 而非只 console log GPU 加速被强制关

stat：10 文件 +75。

### 14.2 隐藏 link-title 窗口静音 — `ae8db1ab fix(desktop): mute hidden link-title window so historical links don't autoplay audio`

- 抽 offscreen BrowserWindow 构造为 `createLinkTitleWindow`（`apps/desktop/electron/link-title-window.cjs:29-40`），构造后立即调 `window.webContents.setAudioMuted(true)`
- `main.cjs:2984` 调 helper 不再 inline 构造
- **行为**：tier-2 title resolution 在 autoplay 页面（YouTube `watch` URL）不再泄 ~2s 音频，会话重渲时也安全

stat：2 文件 +44/-14。

### 14.3 测试覆盖 — `7eb9678c test(desktop): cover link-title window audio muting`

新 `link-title-window.test.cjs`：断言 `setAudioMuted(true)` 调到 + offscreen defaults 保留；注册到 `apps/desktop/package.json`。stat：2 文件 +57/-1。

### 14.4 右侧栏按钮 tooltip — `404fe730 fix: add tooltips to right sidebar header buttons`

`apps/desktop/src/app/right-sidebar/index.tsx:176,186,197` 加 `title={...}` 到 refresh / open-folder / collapse-all 按钮。stat：1 文件 +3。

### 14.5 tooltip 缩进 + AUTHOR_MAP — `838daca9 chore(desktop): format tooltip indentation + author map for #49697`

- 三 `title=` 行从 tab 改 spaces（`apps/desktop/src/app/right-sidebar/index.tsx:176,186,197`）
- `AUTHOR_MAP` 加 `"alelpoan@proton.me": "alelpoan"`（`scripts/release.py:181`）

stat：2 文件 +4/-3。

---

## 15. misc cron docs author map

### 15.1 cron — 空白清理 `c1a0b6a5`

`cron/scheduler.py:839` 单 trailing whitespace。已在 §7.3 列。

### 15.2 docs — `1e40b21b docs: clean up three stale comments from the #32848 audit (#45638)`

3 文件 +5/-6：
- `tools/memory_tool.py:20`：docstring drop 已删的 `read` action
- `tools/memory_tool.py:141-144` + `:197-200`：去掉指向不存在 action 的 `memory(action=read)` stray ref
- `tools/fuzzy_match.py:9`：`"8-strategy"` → `"9-strategy"`
- `run_agent.py:1531`：`See #<TBD>.` 替 backfill-note 占位

### 15.3 AUTHOR_MAP salvage（5 commit，单行加入 `scripts/release.py`）

- `92d40c25` add `IamSanchoPanza`（PR #43872 salvage）
- `6504f51c` add `@hakanpak`（PR #49282 salvage）
- `467c879b` map legacy `lkz-de@users.noreply.github.com` → `lkz-de`
- `ef7e7169` map `rratmansky` contributor email
- `e49272fe` map `w31rdm4ch1nZ` contributor email

### 15.4 Merge commits 索引

- `2ab09a6c` — PR #49680 `NousResearch/fix/signal-quote-cache-eviction`（§7.15）
- `85ad7c9b` — PR #49678 `NousResearch/salvage/signal-echo-ring`（§7.13）
- `b88d0007` — PR #49583 `NousResearch/salvage/signal-mention-typing`（§7.10, §7.12）
- `65561e9d` — PR #49563 `NousResearch/salvage/signal-quote-history`（§7.11）
- `ff50a886` — PR #49558 `NousResearch/salvage/env-var-guards-48735`（§7.8）
- `834bbae8` — PR #49530 `NousResearch/salvage/signal-trio`（§7.4-§7.7）
- `1a0ef131` — PR #49264 `kshitijk4poor/salvage-picker-persist-49176`（§12.1）

---

## 验证摘要

| 检查项 | 路径 | 状态 |
|---|---|---|
| `compression.in_place` flag | `hermes_cli/config.py:1290` | ✓ |
| 3-态 `active/compacted` 列 | `hermes_state.py:570` | ✓ |
| `archive_and_compact` 软归档 | `hermes_state.py:2700+` | ✓ |
| `compacted = 1` UPDATE | `hermes_state.py:2734-2737` | ✓ |
| `search_messages` 含归档 WHERE | `hermes_state.py:3528-3532` | ✓ |
| Blank Slate setup 选项 | `hermes_cli/setup.py:2853-2861` | ✓ |
| `_run_blank_slate_setup` | `hermes_cli/setup.py:3056-3134` | ✓ |
| `agent.disabled_toolsets` 减集 | `hermes_cli/setup.py:3026` | ✓ |
| `dispatch_async_delegation_batch` | `tools/async_delegation.py:337` | ✓ |
| `_execute_and_aggregate` | `tools/delegate_tool.py:2247` | ✓ |
| `is_batch=True` 完成事件 | `tools/async_delegation.py:389` | ✓ |
| `_format_async_delegation` consolidated | `tools/process_registry.py:1549-1628` | ✓ |
| 9 adapter `plugins/platforms/<x>/` 全在 | (Glob) | ✓ |
| `gateway/platforms/{9 names}.py` 全删 | (Bash) | ✓ |
| Signal 仍在 `gateway/platforms/signal.py` | (Bash) | ✓ |
| `_validate_send_result` | `gateway/platforms/signal.py:1020` | ✓ |
| OrderedDict echo ring + TTL 5 min | `gateway/platforms/signal.py:315-316` | ✓ |
| `_remux_aac_to_m4a` | `gateway/platforms/signal.py:141` | ✓ |
| `markdown_to_signal` 共享 module | `gateway/platforms/signal_format.py` (新) | ✓ |
| `_send_media_attachment` Teams helper | `plugins/platforms/teams/adapter.py:1192-1240` | ✓ |
| `auxiliary.title_generation.language` config | `hermes_cli/config.py:1457` | ✓ |
| `_TITLE_PROMPT_PINNED_LANGUAGE` | `agent/title_generator.py:29-34` | ✓ |
| `composer-popout.ts` nanostore | `apps/desktop/src/store/composer-popout.ts` | ✓ |
| `use-popout-drag.ts` dock zone | `apps/desktop/src/app/chat/composer/hooks/use-popout-drag.ts` | ✓ |
| `rewrite_prompt_model_identity` | `agent/chat_completion_helpers.py:1045-1071` | ✓ |
| `_sync_failover_system_message` | `agent/conversation_loop.py:469-492` | ✓ |
| `auth_failover_attempted` retry flag | `agent/turn_retry_state.py:65` | ✓ |
| `clear_model_endpoint_credentials` | `hermes_cli/config.py:3934` | ✓ |
| `DEFAULT_NOUS_INFERENCE_URL` | `hermes_cli/auth.py:71` | ✓ |
| `HERMES_SESSION_SOURCE` ContextVar | `gateway/session_context.py:52,73,105,128,160` | ✓ |
| internal-event guard | `gateway/run.py:4172-4184` | ✓ |
| `_strip_dangling_tool_call_tail` | `gateway/run.py:940-980` | ✓ |
| `creative-ideation` SKILL.md version 2.1.0 | `optional-skills/creative/creative-ideation/SKILL.md:5` | ✓ |
| 22 个新 method MD（references/methods/） | (Glob) | ✓ |

**结论**：上 35 项关键 file:line citation 全在 HEAD `5a53e0f0` 实证存在。本期 69 commit / 250 文件 +12225/-3725 全部经源码验证，无残留猜测。
